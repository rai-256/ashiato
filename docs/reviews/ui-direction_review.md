---
target: docs/ui-direction.md
target_html: docs/ui-direction-playground.html
reviewed_at: 2026-09-08T13:48:00+09:00
rounds:
  - {round: 1, at: 2026-09-08T12:30:12+09:00, findings: 37}
  - {round: 2, at: 2026-09-08T13:05:00+09:00, findings: 10, note: 修正版の再検証}
  - {round: 3, at: 2026-09-08T13:48:00+09:00, findings: 13, note: 再々検証・NFR-18 は成立}
observed: [1, 2, 3, 4, 5]
---

# UI 方向のレビュー

HTML はレンダリングしていない。すべて `docs/ui-direction-playground.html` のソースを読み、
`pal()` / `typo()` / `accHue()` を Node で再実装して端の値を代入し、実際の CSS 変数と
コントラスト比を計算して確かめた（計算は WCAG の相対輝度式。playground 内の `lum` / `cr` と同一）。

## 第 3 回（再々レビュー）

修正版（460 行）を読み直し、新しい `pal()`（238〜258 行）を Node で再実装。
**色相 24 × 彩度 6 × 深さ 11 × 段数 4 × テーマ 2 = 12,672 通り**を、
第 2 回と同じ「実際に `rowHTML` / CSS が出力する色」で測り直した。

### 先に結論

- **NFR-18 は全状態で成立する。独立に検算して 12,672 通りで違反 0 件・最小 4.50:1。**
  申告値（`p.ratio`）と実測値のずれも 0（全通りで一致）。**UIR-38 / UIR-41 は完全に解消。**
  帯を越える組み合わせも 0 件（**UIR-45 も解消**）。
- **UIR-40 は直っていない。** `L(i)=s0+step*Math.min(i,段数-1)`（245 行）が第 2 回から変わっておらず、
  既定の 2 段では `s1 = s2 = s3`（実測 6.8 / 12.6 / 12.6 / 12.6）。
  写真枠と展開ブロックがカード地と同色になる状態は残っている。
  `docs/ui-direction.md` 152 行は「UIR-39 / UIR-40 を直した」と記録している。
- **UIR-39 の結合は消えていない。向きが反転しただけ。** `s0max=LIM-dir*4*(段数-1)`（241 行）により、
  今度は**段数を上げると地の深さの可動域が縮む**。段数 4 では地は 3〜22% しか動かず、
  端どうしのコントラストは **1.72:1** ＝ **第 1 回 UIR-1 で「動かない」と指摘した 1.84:1 より狭い**。
- **2 段組は 12 通りの（主表現 × 画面）のうち 1 つにしか付いていない**（319 行の `.cols` のみ）。
  表レイアウトと組むと段の切れ目で行が割れうる。閾値 900px は
  doc の「決めなかったこと」に書いた方針と矛盾する。

### 依頼された 3 点への回答

#### 1. NFR-18 は描画色そのもので全状態成立するか → **する（再現できた）**

| 測り方 | 最小 | 4.5 未満 |
|---|---|---|
| 申告値 `p.ratio`（256〜257 行） | 4.50:1 | 0 / 12,672 |
| **実際に描かれる色で測り直し**（面 4 × 文字 2 ＋ チップ 3 色 × 文字 2） | **4.50:1** | **0 / 12,672** |
| 申告値と実測値の差 | すべての組み合わせで 0.005 未満 | — |

最悪ケースはライト・色相 165°・彩度 24%・深さ 0・段数 4 の `dim` / `--s3` で 4.50:1。
`faces` に**描くときと同じ 3 要素（色相・彩度・明度）**を入れ（249〜250 行）、
`pick()` が全 face を同時に満たす明度を探す（251〜252 行）形になっているため、
第 2 回に見つけた「測る色と描く色の不一致」は構造的に起きない。描画側も `p.chip` を使っている（278 行）。**これは正しい直し方。**

ただし 1 つだけ副作用がある（UIR-56）。
**運用値の 5.5:1 を満たす文字色が存在しない状態が 406 / 12,672（3.2%）ある。**
`pick(5.5)` はそのとき無言で 100%（ダーク）/ 0%（ライト）を返す。
要件の 4.5:1 は保たれる（本文 `fg` の全状態最小は 4.62:1）が、
**自分で立てた運用値が破れたことは画面に出ない。**

#### 2. 別の軸を殺していないか → **殺している（UIR-49）。UIR-40 は未着手**

**面の段数（card/s0 のコントラスト）** — 深さ 100 での潰れは直った（第 2 回 1.062 → 今回 1.175）:

| 段数＼深さ（ダーク） | 0 | 14（既定） | 50 | 100 |
|---|---|---|---|---|
| 2 段 | 1.115 (Δ5.8) | 1.159 (Δ5.8) | 1.241 (Δ5.8) | 1.175 (Δ4.0) |
| 3 段 | 1.152 (Δ7.1) | 1.199 (Δ7.1) | 1.292 (Δ7.1) | 1.175 (Δ4.0) |
| 4 段 | 1.193 (Δ8.4) | 1.240 (Δ8.4) | 1.278 (Δ7.2) | 1.172 (Δ4.0) |

**地の深さ（端どうしのコントラスト）** — 今度はこちらが段数に食われる:

| 段数 | ダーク | ライト |
|---|---|---|
| 1 段 | 3.0→34.0%　**2.78:1** | 99.0→66.0%　2.31:1 |
| 2 段（既定） | 3.0→30.0%　2.37:1 | 99.0→70.0%　2.05:1 |
| 3 段 | 3.0→26.0%　2.02:1 | 99.0→74.0%　1.84:1 |
| 4 段 | 3.0→22.0%　**1.72:1** | 99.0→78.0%　**1.65:1** |

段数 4 の 1.72:1 は、**第 1 回に「動くが変わらない」と指摘した旧実装の 1.84:1 より狭い**。
「段数ぶんの余地を先に取る」という直し方は、面と深さのどちらを優先するかを
**段数の側に一方的に決めさせている**。→ UIR-49。

**幅・状態は殺していない。** 幅は step 10 で `ctx.width=430` が刻みに乗る（UIR-22 解消）、
実効幅の併記あり（UIR-8 解消、ただし 1 フレーム遅れ → UIR-55）、`overflow:auto`（UIR-21 解消）。
状態は 5 つに増え、`long` は 15 件＋長い名前 1 件（UIR-47 解消）。

**UIR-40 は未修正**（上記「先に結論」）。実測: 1 段 7.3/7.3/7.3/7.3、**2 段 6.8/12.6/12.6/12.6**、
3 段 6.2/13.3/20.4/**20.4**、4 段 5.7/14.1/22.5/30.9。→ UIR-50。

#### 3. 2 段組は壊れないか → **カード・カレンダー・地図では「発動しない」。表では壊れうる**

`.cols` を出力しているのは **319 行の 1 か所だけ**（`screenMain` の時系列の分岐）。

| 主表現 / 画面 | 900px 以上で 2 段になるか |
|---|---|
| 主表現・時系列 | **なる**（`.cols`） |
| 主表現・一覧 | ならない（317 行は `.cols` 無し） |
| 主表現・地図 | ならない（311 行） |
| 主表現・カレンダー | ならない（304〜305 行。`.cal` は 7 列固定） |
| 検索結果 | ならない（323 行） |
| 稼働状況 | ならない（326〜334 行） |
| 空 / エラー / 読み込み中 | ならない（349〜358 行） |

- **カード**: 時系列 × カード では 2 段になり、`break-inside:avoid`（60 行）が効くので割れない。**問題なし。**
- **表**: `break-inside:avoid` の対象は `.rw-card, .rw-list` **だけ**で `.rw-tbl` が入っていない（60 行）。
  `.rw-tbl` は `display:grid` なので、段の切れ目で 1 行が上下に割れうる。
  さらに縞（`.rw-tbl:nth-child(even)`、68 行）の位相が 2 か所で変わる:
  (a) 時系列では行が `.cols` の子、一覧では `.pv` の子（`.hd` が 1 つ前にある）なので**主表現を変えると縞が反転する**、
  (b) 表 × その場で開く では `.expand` が行の兄弟として挟まる（284〜286 行）ので**4 行目以降の縞が反転する**。
- **カレンダー・地図**: そもそも 2 段組にならないので壊れない。ただし
  「幅を広げると密度の判断が変わる」という文脈の目的が、この 2 つでは検証できないまま。
- **閾値 900px**: `ctx.width>=900`（360 行）は**要求幅**であって実効幅ではない。
  ステージが狭くて 800px にクランプされていても 2 段になる。
  加えてこの 900 は開示欄（111 行）にも無く、
  `docs/ui-direction.md` 135 行の「**レスポンシブの閾値 — 閾値そのものは各画面の中身で決まる**」
  （＝ この工程では決めない、と宣言済み）と正面から矛盾する。

### 「直さない」6 件の判断について

| 指摘 | 判断 | 根拠 |
|---|---|---|
| UIR-4 | **結論は妥当だが、書いてある理由は事実と違う** | doc 168 行は「面の段数と密度を上げれば差は出る」とするが、実測では `card/s0` の**全状態での最大が 1.381:1**（ダーク・色相 60°・彩度 55%・深さ 70・3 段）で、段数を上げても 1.4 を超えない。密度は色に一切影響しない（`pal()` は `state.density` を読まない — 検算済み）。**帯（≤34% / ≥66%）と NFR-18 を守る限り、面の明度差だけでカードを主張することは原理的にできない。** 直さないなら「カードの差は余白・角丸・境界線で出す」と決め直して書くほうが正確 |
| UIR-12 | **妥当** | 色相トラックは道具の外観で、決める対象ではない |
| UIR-36 | **妥当** | 同上 |
| UIR-35 | **理由が誤り** | doc 171 行は「製品側の同じ問題は NFR-18 / NFR-19 が受ける」とするが、**フォーカスの可視は SC 2.4.7 Focus Visible で、NFR-18（SC 1.4.3）でも NFR-19（SC 2.5.8）でもない。** 要件にも `EXT-*` にも無い。指摘の眼目は「playground に無い」ではなく「**製品のフォーカスの見え方を誰も決めていない**」こと。道具の話にして閉じると未決のまま降りる。「決めなかったこと」に 1 行足す（＝ Story に送る）だけで足りる |
| UIR-25 | **理由が指摘を読み違えている** | doc 170 行は「下限は探索範囲であって preset が触れる義務は無い」とするが、指摘は「preset が下限に触れていない」ではなく「**preset『見渡す』が実際に 1.38 を使っている**」（218 行 `lh:138`）。しかも同じ preset は本文 11.8px（`base:13` × 密度 12）。日本語の仮名漢字混じり本文で 1.38 は詰まる、が指摘の中身。結論を維持するなら別の理由が要る |
| UIR-19 | **おおむね妥当。ただし半分は直せる** | 「全件描画が要り現実的でない」は `raw`（4,120 件）には当てはまるが、`day` は **30 行**なので全件描ける。いまは 14 行しか描かずに `#cnt` が「1 か月 30 件 — 1 画面に収まる」と表示する（365〜366 行） |

### 第 2 回 10 件の判定

| ID | 判定 | 実測での確認 |
|---|---|---|
| UIR-38 チップの色が想定とずれる | **直った** | `faces` が [色相, 彩度, 明度] の 3 要素になり（249 行）、描画も `p.chip` を使う（278 行）。12,672 通りで違反 0・最小 4.50:1 |
| UIR-39 深さを上げると段数が死ぬ | **半分**（向きが反転） | 深さ 100 の `card/s0` は 1.062 → 1.175 に回復。代わりに段数 4 で深さの可動域が 1.72:1 に縮む → UIR-49 |
| UIR-40 `s1=s2=s3` | **直っていない** | 245 行は不変。2 段で `s1=s2=s3`、3 段で `s2=s3` → UIR-50 |
| UIR-41 表示が実際の最小でない | **直った** | 12,672 通りすべてで申告値と実測値の差 < 0.005 |
| UIR-42 検索の副題が二重 | **直った（残り 1 つ）** | 322 行の `startsWith` で二重は解消。ただし `drill:"inline"` の展開行が検索結果の 4 件目（`IMG_2318.jpg`＝写真のヒット）に「生の記録 412 件 …」と出る点は残る |
| UIR-43 しるしの形が重複 | **半分** | `SHAPE` は 5 種すべて別形になった（262 行）。だが `ACC_N` を 6 にした（260 行）ので `SHAPE[5]` が存在しない → UIR-48 |
| UIR-44 新しい運用値が未開示 | **直った（また 1 つ増えた）** | 111 行に 帯 34/66・最小差 4pt・境界線 13pt・10px 下限 を追加。**2 段組の 900px** が未開示、かつ「種類の色は 5 本」が実装（6 本）と不一致 → UIR-53 / UIR-48 |
| UIR-45 帯を越える | **直った** | `s0max` により `room ≥ 4` が保証され、12,672 通りで超過 0 件 |
| UIR-46 空/エラーが画面共通・読み込み中が無い | **半分** | 「読み込み中」は追加（349〜352 行）。だが 3 状態とも `ctx.screen` を見ないので、検索結果でも稼働状況でも同じ「まだ何も記録がありません」が出る。`items()` の `empty` 分岐（272 行）は今も到達しない |
| UIR-47 最長・最多が全行 | **直った** | 273 行。長いのは `LONG` 1 件のみ、`stay` は 1+9+5 = **15 件**で要件の上限に一致 |

参考: 第 1 回の未修正分の現状 —
UIR-2（面の潰れ）は UIR-50 として残存 /
UIR-3（境界線）は `line` vs `--s1` が 1.12〜1.44:1 で第 2 回から変化なし /
UIR-31 は副題に `num` が付いた（290 行）が、単位「生の記録」×主役「場所」のときの緯度経度は
`.big`（69 行、`num` 無し）のまま /
UIR-33 は `stay` が 15 件になった一方 `day` は 15 行（主張は 30 件）/
UIR-37 は曜日（188 行・319 行）は直ったが `9/${(7-i+30)%30||30}` の 9/1 → 9/30 の折返しは残存。

### 第 3 回の新規指摘

#### UIR-48 — `ACC_N=6` に対し `SHAPE` が 5 要素しかなく、6 番目のしるしが無効な CSS になる 【重大度: 中】
- 観点: 1, 5
- 根拠: 260 行 `const ACC_N=6;` に対し 262 行 `const SHAPE=[…]` は **5 要素**。
  `SHAPE[i%ACC_N]` は `i=5` で `undefined` を返す。
  稼働状況は 6 ソースを `i` 0〜5 で回す（328 行）ので、
  6 番目（健康）の `border-radius:undefined` は無効宣言として捨てられ、
  `.ic{border-radius:calc(var(--r)*.4)}`（77 行）にフォールバックする。
  実測: `50% | 3px | 0 0 50% 50% | 50% 0 50% 0 | 45% 10% 45% 10% | undefined`。
  加えて開示欄（111 行）は「**種類の色は 5 本**」のままで、実装（6 本）と食い違う。
- 再現: 画面「稼働状況」＋ しるし ON。6 行目「健康」のしるしだけ、
  `.ic` の既定の角丸（`--r*0.4`）になる。`SHAPE[1]`（`3px`）と見分けがつきにくい。
- 何が起きるか: UIR-24 を直すために色を 6 本にしたことで、UIR-43 で直した形が 1 つ足りなくなった。
  「色だけに頼らない」が 6 種類目で崩れる。
- 直し方の案: `SHAPE` に 6 個目を足し、`ACC_N` と `SHAPE.length` が一致することを
  1 行のアサーションか `ACC_N=SHAPE.length` で担保する。開示欄の「5 本」も直す。

#### UIR-49 — 面の段数を上げると「地の深さ」の可動域が縮み、段数 4 では第 1 回の指摘時より狭くなる 【重大度: 中】
- 観点: 1
- 根拠: 241 行 `const s0max=LIM-dir*MINSTEP*(state.surfaces-1);` と
  242 行 `const s0=dark?3+state.depth*(s0max-3)/100:…`。
  ダークの `s0max` は 段数 1→34、2→30、3→26、**4→22**。
  地の明度の端どうしのコントラストは 2.78 / 2.37 / 2.02 / **1.72**（ライトは 2.31 / 2.05 / 1.84 / **1.65**）。
  第 1 回 UIR-1 で「動くが変わらない」と判定した旧実装の値は **1.84:1** だった。
- 再現: 面の段数を 4 にしてから、地の深さを 0 → 100 に振る。
  背景は `rgb(8,8,7)` から `rgb(59,56,53)` までしか動かない。
- 何が起きるか: 第 2 回 UIR-39（深さが段数を殺す）を、
  **段数が深さを殺す形に付け替えただけ**になっている。
  どちらの軸を優先するかを段数の側が一方的に決めており、選ぶ側からは見えない。
- 直し方の案: 帯の上限 `LIM` を固定 34/66 ではなく
  「その色相・彩度で `dim` が 4.5:1 を取れる最大明度」から毎回求める（彩度が低い状態では帯が広がる）。
  あるいは段数と深さの積が帯を超えるときだけ段差を詰め、深さの可動域は保つ。

#### UIR-50 — `--s2` / `--s3` の潰れ（第 2 回 UIR-40）が未修正のまま「直した」と記録されている 【重大度: 中】
- 観点: 1, 4
- 根拠: 245 行 `const L=i=>s0+step*Math.min(i,state.surfaces-1);` は第 2 回から 1 文字も変わっていない。
  実測（ダーク・既定の深さ 14）:
  | 段数 | s0 | s1 | s2 | s3 |
  |---|---|---|---|---|
  | 1 | 7.3 | 7.3 | 7.3 | 7.3 |
  | **2（既定）** | 6.8 | 12.6 | **12.6** | **12.6** |
  | 3 | 6.2 | 13.3 | 20.4 | **20.4** |
  | 4 | 5.7 | 14.1 | 22.5 | 30.9 |
  `.ph i{background:var(--s3)}`（80 行）と `.expand{background:var(--s2)}`（96 行）は
  `.rw-card{background:var(--s1)}`（62 行）と同色になる。
  `docs/ui-direction.md` 152 行は「UIR-39 / UIR-40 … 直した」、162 行は「残り（UIR-2 …）も同様に反映済み」と記録している。
- 再現: 並べ方「カード」＋ 面の段数 2（既定）＋ 主役「写真」。写真枠がカード地に溶ける。
  preset「のんびり」（3 段・カード・写真）では展開ブロックと写真枠が同色になる。
- 何が起きるか: 「面を何段で作るか」が、選んだ段数より 1 つ少ない段しか実現しない状態が
  第 1 回から 3 回連続で残っている。加えて **doc の記録が実態と違う**ので、
  この文書を読む次の工程は解決済みと判断してしまう。
- 直し方の案: 段数 n のとき `--s2` / `--s3` を n 段の中に割り当て直す
  （2 段なら写真枠は `--s0` に落とし、地とカードの 2 面だけで組む）。
  直さないなら doc の「直した」を「直さない（理由）」に移す。

#### UIR-51 — 2 段組が 12 通りの（主表現 × 画面）のうち 1 つにしか付いていない 【重大度: 中】
- 観点: 3
- 根拠: `.cols` を出力しているのは 319 行（`screenMain` の時系列の分岐）だけ。
  一覧（317 行）・地図（311 行）・カレンダー（304〜305 行）・検索結果（323 行）・
  稼働状況（326〜334 行）・空/エラー/読み込み中（349〜358 行）には無い。
- 再現: 幅を 1440px にして、主表現を「時系列」→「一覧」に切り替える。
  中身も件数も同じなのに、時系列だけ 2 段・一覧は 1 段になる。
- 何が起きるか: 「幅で密度の判断が変わるか」を確かめる、という文脈の目的が、
  **選べる 12 通りのうち 1 通りでしか働かない**。
  しかも同じ行データ・同じ密度で主表現を変えると段数が変わるので、
  主表現の比較（時系列 vs 一覧）が幅の影響と混ざる。
- 直し方の案: 行を並べる分岐（時系列・一覧・地図の下段・検索結果）で
  同じラッパを使う。カレンダーと稼働状況は 2 段にしない理由を `.n` に書く。

#### UIR-52 — 2 段組と表レイアウトの組み合わせで、行が段の切れ目で割れ、縞の位相が 2 か所で変わる 【重大度: 中】
- 観点: 3, 4
- 根拠:
  1. 60 行 `.pv.wide .rw-card,.pv.wide .rw-list{break-inside:avoid}` に **`.rw-tbl` が入っていない**。
     `.rw-tbl` は `display:grid`（65 行）で、`column-count` の段の切れ目で分割されうる。
  2. `.rw-tbl:nth-child(even)`（68 行）の親が主表現で変わる。
     時系列は `.cols` の子（319 行）、一覧は `.pv` の子で前に `.hd` がある（318 行）
     → **主表現を切り替えると縞が反転する**。
  3. 表 × その場で開く では `.expand` が `.rw-tbl` の兄弟として挟まる（284〜286 行）
     → **4 行目以降の縞が反転する**。
- 再現: 幅 1440px ＋ 並べ方「表」＋ 主表現「時系列」。段の境目で 1 行が割れる。
  同じ設定で「その場で開く」にすると 4 行目より下の縞が入れ替わる。
- 何が起きるか: 「列を揃えて比べる」ための表が、幅を広げたときに揃わなくなる。
  UIR-5（表でも降り方が効く）の修正が、縞という別の一貫性を崩している。
- 直し方の案: `break-inside:avoid` の対象に `.rw-tbl` を足し、
  縞は `nth-child` ではなく行の生成側で `i%2` を見てクラスを付ける。

#### UIR-53 — 2 段組の閾値 900px が未開示で、doc の「決めなかったこと」と矛盾し、実効幅ではなく要求幅で判定している 【重大度: 中】
- 観点: 2, 3
- 根拠:
  - 360 行 `<div class="pv${ctx.width>=900?" wide":""}">` の **900** は
    開示欄（111 行）の運用値リストに無い。
  - `docs/ui-direction.md` 135 行の「決めなかったこと」に
    「**レスポンシブの閾値** — 幅は文脈として触って確かめるが、閾値そのものは各画面の中身で決まる」とある。
    900px を playground に埋めたことで、決めないと宣言した値をこの工程が持ってしまった。
  - `ctx.width` は**要求幅**。`#frame` は `max-width:100%`（339 行）で
    ステージ幅（ブラウザ幅 − 352 − 44）に頭打ちされる。
    1440px のノート PC では要求 1000px でも実効は約 1044px 以下になりうるのに、
    `.wide` は要求値だけで決まる。UIR-8 の修正（実効幅の併記、444 行）と判定基準が食い違う。
- 何が起きるか: 要件に無い値をこの工程が無言で立てる、という第 1 回 UIR-18 と同じ型の 3 度目の再発。
  加えて、実効幅と判定幅が違うので「狭い幅で 2 段になった」状態が起こりうる。
- 直し方の案: 900 を開示欄に足すか、doc の「決めなかったこと」から
  「レスポンシブの閾値」を外して「1 つだけ暫定で置いた」と書き換える。
  判定は `getBoundingClientRect().width` に変える。

#### UIR-54 — 時系列を 2 段組にすると時刻の読み順が折り返す 【重大度: 低】
- 観点: 5
- 根拠: 59 行 `column-count:2` は左の段を下まで埋めてから右の段に移る。
  時系列（319 行）は 07:20 → 12:05 が左、13:00 → 19:30 が右になり、
  1 日の流れが「下まで行って上に戻る」形になる。
- 何が起きるか: 主表現が時系列である以上、読み順は決定の一部。
  新聞組みで良いという判断はあり得るが、その判断が画面にも prompt にも出ていない。
- 直し方の案: 幅が広いときは 2 段ではなく「1 段のまま最大行長を 60〜75 字で止める」も
  選択肢として並べ、どちらかを選ばせる。

#### UIR-55 — 実効幅の表示が 1 フレーム遅れ、初回は「実効 0px」になる 【重大度: 低】
- 観点: 1
- 根拠: `all()` は 443〜444 行で `#frame` の幅を読んでから、
  456 行の `render()` で `#frame` の `style` を更新する。表示されるのは**更新前**の幅。
  初回（459 行 `sync();all();`）は `#frame` にまだ幅指定が無く、
  `flex:none` の空 div なので実測 0 → 「430px（実効 0px）」と出る。
- 再現: 読み込み直後の右上の幅表示。次に幅を動かすと 1 つ前の値が併記される。
- 何が起きるか: UIR-8 のために足した表示が、いちばん見たい瞬間（初回・切り替え直後）に嘘をつく。
- 直し方の案: `render()` の後で幅表示を更新する（`all()` 内の順序を入れ替える）。

#### UIR-56 — 運用値 5.5:1 を満たす文字色が存在しない状態が 3.2% あり、そのとき無言で端に落ちる 【重大度: 低】
- 観点: 2
- 根拠: 251〜252 行 `const pick=t=>{for(…)if(faces.every(…>=t))return x;return dark?100:0;}` —
  解が無いときは**無条件に 100%（白）/ 0%（黒）を返す**。
  12,672 通りのうち **406 通り（3.2%）で `pick(5.5)` に解が無い**
  （例: ダーク・彩度 55%・深さ 60 以上、色相 0° 付近）。`pick(4.5)` に解が無い組み合わせは 0。
  そのときの本文の実効コントラストは最小 **4.62:1** で、要件 NFR-18 は満たす。
- 何が起きるか: 実害は小さいが、「本文は 5.5:1 を狙う」と開示欄（111 行）に書いた運用値が
  黙って破れる。第 1 回 UIR-15 と同じ「表示が実態を映していない」型の小型版。
- 直し方の案: `pick` が失敗したときにフラグを返し、`#cr` に
  「5.5:1 は取れず 4.62:1（要件は満たす）」と出す。

#### UIR-57 — 「読み込み中」の骨組みが半透明のテキストで、NFR-18 の対象かどうかを決めていない 【重大度: 低】
- 観点: 2, 5
- 根拠: 351〜352 行 `<span class="big" style="opacity:.35">████████</span>` と
  `<div class="sm" style="opacity:.25">██████</div>`。
  実体は `--fg` / `--dim` の文字を 35% / 25% の不透明度で描いたもので、
  `pal()` の `faces` 走査（256〜257 行）には入っていない。
- 何が起きるか: 骨組みは「情報を持たない装飾」として NFR-18 の対象外と整理できるが、
  その整理がどこにも書かれていない。文字で描いている以上、判断の記録が要る。
- 直し方の案: `<span>` ではなく背景色のブロック（`--s2` の矩形）で描くか、
  開示欄に「骨組みは装飾として扱う」と 1 行足す。

#### UIR-58 — 無効化した軸がキーボードでは操作できてしまう 【重大度: 低】
- 観点: 5
- 根拠: 452〜453 行 `n.style.opacity=…; n.style.pointerEvents=off.includes(k)?"none":"auto";`
  — `pointer-events:none` はポインタしか止めない。`.cards button` / `.pills button` は
  `disabled` になっていないので、Tab で到達して Enter/Space で `state.layout` / `state.drill` が変わる。
  主役の `<select>` だけは 454 行で `disabled` になっている。
- 何が起きるか: カレンダーを選んだ状態でも、キーボードだと無効なはずの軸を変更できる。
  変更は `state` に入るが画面に出ないので、**見えないところで状態が変わる**。
- 直し方の案: `pointer-events` ではなく各 `button` に `disabled` を立てる。

#### UIR-59 — 「地の深さ」は値を上げるほど浅くなる（軸名と向きが逆） 【重大度: 低】
- 観点: 4
- 根拠: 242 行 `s0 = 3 + depth*(s0max-3)/100`（ダーク）。深さ 0 で `s0=3%`（最も暗い＝深い）、
  深さ 100 で `s0=s0max`（最も明るい＝浅い）。
  prompt の文言（379 行）は `state.depth<30?"深く沈める":…:"浅くする"` で**正しく反転**しているが、
  スライダのラベルは「地の深さ」、説明は「ダークなら沈み具合」（142 行）。
- 何が起きるか: 「深さ」を上げると浅くなるので、触る側の直感と逆。
  prompt には `地の深さ 100（浅い）` と両方書かれるため実装は誤らないが、選ぶ段で誤解する。
- 直し方の案: 軸名を「地の明るさ」にするか、`depth` を反転して保持する。

#### UIR-60 — doc の「直した」記録が 2 か所で実測と食い違う 【重大度: 低】
- 観点: 4
- 根拠:
  - `docs/ui-direction.md` 151 行「地の深さ … **ダーク 3〜34% / ライト 99〜66% に拡大**」
    → これが成り立つのは**段数 1 のときだけ**。既定の 2 段では 3〜30%、4 段では 3〜22%（UIR-49）。
  - 同 152 行「UIR-39 / UIR-40 … 直した」/ 162 行「残り（UIR-2 …）も同様に反映済み」
    → **UIR-40 / UIR-2 は未修正**（UIR-50）。
- 何が起きるか: この doc が `/production-prep` に渡る正典なので、
  未解決の項目が解決済みとして記録されると、次の工程で再発見されない。
- 直し方の案: 151 行に「段数 1 のとき」と条件を付ける。152 行の UIR-40 を「直さない」か
  「残り」に移して理由を書く。

---

## 第 2 回（再レビュー）

修正版（437 行）を読み直し、新しい `pal()`（234〜253 行）を Node で再実装して
**色相 24 × 彩度 6 × 深さ 11 × 段数 4 × テーマ 2 = 12,672 通り**を再計算した。
第 1 回と違うのは、`pal()` が自分で出す `p.ratio` だけでなく
**実際に `rowHTML` / CSS が出力する色**（`hsl()` の引数をそのまま組み立てたもの）でも測った点。

### 先に結論

- **面（`--s0` / `--s1` / `--s2`）に載る文字は、12,672 通りすべてで 4.5:1 を満たす。最小 4.50:1、違反 0 件。**
  ここは修正が効いている。UIR-14 は完全に解消。
- **ただし NFR-18 は全状態では成立していない。** 気分チップの文字が
  **2,962 / 12,672 通り（23.4%）で 4.5:1 未満、最小 2.28:1**。
  原因は `pal()` の想定と描画がずれていること 2 点（UIR-38）。
  既定値と 5 つの preset はすべて成立（実測 5.07〜5.95:1）なので、
  **押した瞬間は割れないが、地の深さを 80 以上に上げると既定の色相・彩度でも割れる**。
- 「地の深さ」の可動域は本当に広がった（端どうし 1.84:1 → **2.47:1**）。
  ただしその副作用で **深さを上げると「面の段数」が実質死ぬ**（UIR-39）。

### 依頼された 3 点への回答

#### 1. NFR-18 は全状態で成立するか → **していない（面は成立、チップが不成立）**

| 測り方 | 最小 | 4.5 未満の件数 |
|---|---|---|
| playground の自己申告 `p.ratio`（251 行） | 4.50:1 | **0 / 12,672** ← 再現できた |
| 面（s0/s1/s2）に載る文字だけを実際の色で | 4.50:1 | **0 / 12,672** |
| **実際に描かれるチップを含めた最小** | **2.28:1** | **2,962 / 12,672（23.4%）** |

内訳: ダーク 1,961 / 6,336（31.0%、最小 2.28:1）、ライト 1,001 / 6,336（15.8%、最小 2.64:1）。
深さ別の違反件数（ダーク）は 深さ 0 で 7 件、50 で 76 件、80 で 352 件、100 で 534 件と単調に増える。

**既定の色相 28°・段数 2・ダークで、彩度と深さを振ったときのチップ実測**（`*` が 4.5 未満）:

| 彩度＼深さ | 0 | 20 | 40 | 60 | 80 | 100 |
|---|---|---|---|---|---|---|
| 18（既定） | 5.29 | 5.19 | 4.97 | 4.87 | **\*3.85** | **\*3.45** |
| 30 | 5.09 | 4.90 | 4.74 | **\*4.47** | **\*3.50** | **\*3.12** |
| 40 | 4.92 | 4.66 | **\*4.45** | **\*4.15** | **\*3.24** | **\*2.87** |
| 55（上限） | 4.64 | **\*4.31** | **\*4.04** | **\*3.72** | **\*2.87** | **\*2.54** |

つまり **UIR-1 を直して深さの可動域を広げたことが、UIR-13 の残骸を露出させている。**
詳細と直し方は UIR-38。

#### 2. 直したことで壊れた箇所 → **2 つある（UIR-39 / UIR-40）**

**「地の深さ」自体は死んでいない。** ダーク 3〜31%（`rgb(8,8,7)` 〜 `rgb(83,79,75)`、端どうし 2.47:1）、
ライト 99〜71%（`rgb(253,252,252)` 〜 `rgb(185,181,177)`、2.00:1）。第 1 回の 1.84:1 / 1.60:1 から明確に広い。

**死ぬのは「面の段数」のほう。** `room=|LIM-s0|/(段数-1)`（238 行）が深さを上げるほど小さくなり、
`step` が縮む。カード地と地のコントラスト `card/s0`:

| 段数＼深さ（ダーク） | 0 | 14（既定） | 50 | 100 |
|---|---|---|---|---|
| 1 段 | 1.000 | 1.000 | 1.000 | 1.000 |
| 2 段 | 1.115 (Δ5.8) | 1.160 (Δ5.8) | 1.243 (Δ5.8) | 1.128 (Δ3.0) |
| 3 段 | 1.152 (Δ7.1) | 1.209 (Δ7.1) | 1.309 (Δ7.1) | **1.062 (Δ1.5)** |
| 4 段 | 1.193 (Δ8.4) | 1.261 (Δ8.4) | 1.237 (Δ5.7) | **1.062 (Δ1.5)** |

深さ 100 では 3 段と 4 段が**まったく同じ step（1.5）**になり、面の差は 1.06:1 ＝ ほぼ見えない。
ライトも同様（4 段・深さ 100 で 1.048:1）。→ UIR-39。

もう 1 つは `--s3` の出し方（UIR-40）。`--s3` は出力されるようになったが
`L=i=>s0+step*Math.min(i,段数-1)`（240 行）は変わっていないので、**既定の 2 段では `s1=s2=s3`**。
写真枠（`.ph i` = `--s3`、80 行）と展開ブロック（`.expand` = `--s2`、95 行）が
カード地（`--s1`）と同じ色になる。第 1 回 UIR-2 の症状が `--s2` から `--s3` に移っただけ。

#### 3. UIR-5 / UIR-6 / UIR-7 は仕様として妥当か → **UIR-7 は単純なバグ。UIR-5 / UIR-6 は「妥当な従属」だが、いまの形は選べない**

判断材料として、骨格＋主役＋しるしの組み合わせを機械的に数えた
（主表現 4 × 並べ方 3 × 単位 3 × 降り方 2 × 主役 4 × しるし 2 = **576 通り**）:

- **区別できる描画は 283 通り。残り 293 通り（51%）は別の組み合わせと同じ絵になる。**
- 内訳: カレンダーで潰れる **144 通り**（`cal|raw` `cal|stay` `cal|day` の 3 通りに集約。
  1 つの鍵に 48 通りがぶら下がる） / 表で降り方が効かない **144 通り** /
  一覧が時系列と同じ **96 通り**。

**UIR-7（一覧の並べ替え）は仕様の余地なし。** 見出しは「長くいた順」と書くのに
`mn` が 0 なので並べ替えが起きない（307 行の `sort`）。表示と内容の食い違いで、直すべき。

**UIR-5 / UIR-6 は「従属関係」としてなら妥当。**
「表では詳細はその場で開かない」「カレンダーには並べ方が無い」は設計判断としてあり得る。
問題は**それが従属なのか実装の抜けなのかが画面から区別できず、しかも prompt には書けてしまう**こと。
`updatePrompt`（372〜374 行）は 4 つの骨格軸を無条件に出力するので、
表 × その場で開く／カレンダー × 表 × 気分が主役、という
**一度も描かれていない組み合わせが正典に書ける。** preset「見渡す」「暦から」がまさにその形。

したがって選べるのは次の 2 つで、「放置」は選べないと考える。

- (a) 従属を実装する — 表に展開行（`grid-column:1/-1`）と chevron 列を足し、
  カレンダーに「日を選んだ後の下段リスト」を `layout` / `hero` / `drill` に従って描く
- (b) 従属を UI で宣言する — 表を選んだら降り方のボタンを `disabled` にし、
  カレンダーを選んだら並べ方・主役・しるしを `disabled` にして、
  **prompt からもその軸の行を落とす**（「カレンダーなので並べ方は決めない」と書く）

どちらでも要件は満たせる。(b) のほうが安く、この工程の目的（方向を決める）には十分に見える。

### 第 1 回 37 件の判定

| ID | 判定 | 実測での確認 |
|---|---|---|
| UIR-1 地の深さの可動域 | **直った** | ダーク 3〜31%（端どうし 2.47:1）/ ライト 99〜71%（2.00:1）。237 行 |
| UIR-2 `--s3` 未出力・面の潰れ | **半分** | `--s3` は出力（332 行）。だが 2 段で `s1=s2=s3` は残る → UIR-40 |
| UIR-3 境界線が見えない | **改善（未達の余地）** | `p.line`（250 行）で vs `--s0` は 1.35〜1.66:1（旧 1.00〜1.16）。ただし表の縞（`--s1`）の上では 1.12〜1.56:1 |
| UIR-4 カードとリストの差 | **直っていない** | `card/s0` は 1.048〜1.309:1。帯を狭めた分、最大値は旧 1.40 より下がった |
| UIR-5 表で降り方が無効 | **直っていない** | 277〜279 行の表分岐は今も先に return（→ 上の Q3） |
| UIR-6 カレンダーで軸が無効 | **直っていない** | 294〜299 行（→ 上の Q3） |
| UIR-7 一覧が並べ替えない | **直っていない** | 307 行（→ 上の Q3。バグ扱いを推奨） |
| UIR-8 幅の表示と実効のずれ | **直っていない** | 330 行 `max-width:100%` と 427 行の表示 |
| UIR-9 幅依存の規則が無い | **直っていない** | `@media` は 10 行の 1 か所のみ、`@container` 0 件 |
| UIR-10 本文 px の表示ずれ | **直っていない** | 260 行 `b=base*dm` に対し 425 行と prompt 376 行は生値 |
| UIR-11 文字彩度 15% 上限 | **開示で決着** | 111 行の運用値に明記。挙動は同じ |
| UIR-12 `#hue` トラックが固定色 | **直っていない** | 28〜30 行 |
| UIR-13 チップのコントラスト | **大幅改善・未達** | 既定と 5 preset は 5.07〜5.95:1 で成立。だが全状態では 23.4% が 4.5 未満 → UIR-38 |
| UIR-14 展開ブロックの文字 | **直った** | `faces` に `--s2` を含め最も不利な面で探索（244〜248 行）。違反 0 件 |
| UIR-15 コントラスト表示 | **半分** | テーマ固定文字列は解消（348 / 382 行）。だが測る色が描画色と違う → UIR-41 |
| UIR-16 地図の pin が 24px 未満 | **直った** | 302 行 `Math.max(24,…)`。9 個すべて 24px 以上 |
| UIR-17 カレンダーに 44px | **直った** | 86 行 `min-height:24px` |
| UIR-18 運用値の未開示 | **直った（ただし再発あり）** | 111 行に 5 件開示。新しく置いた 3 件が未開示 → UIR-44 |
| UIR-19 「収まる」判定 800px | **開示のみ** | 111 行で開示。部分データで測る点（350 行）は未修正 |
| UIR-20 検索・稼働で軸が死ぬ | **大部分直った** | `screenSearch` は `rowHTML` 経由（313 行）。`screenHealth` は `icon` のみ（319 行）で表レイアウトは今もリストに落ちる。副作用 → UIR-42 |
| UIR-21 `overflow:hidden` で破綻が隠れる | **直っていない** | 51 行 |
| UIR-22 幅 430 が step 20 に乗らない | **直っていない** | 157 行 / 223 行 |
| UIR-23 種類色が画面ごとに違う | **直った** | `ACC_N=5`（254 行）を `rowHTML` 271 行 / 地図 303 行 / 稼働 319, 322 行で共有 |
| UIR-24 色の衝突 | **直った（滞在は）** | 滞在の 5 種は 5 色に 1 対 1。稼働状況は 6 ソース → `i%5` で 位置 と 健康 が同色・同形 |
| UIR-25 行間 1.30 / preset 1.38 | **直っていない** | 148 行 / 216 行 |
| UIR-26 prompt に色が出ない | **直った** | 376〜378 行で表面の値を常時出力 |
| UIR-27「（低く抑える）」固定 | **直っていない** | 363 行の差分行に残存 |
| UIR-28 地の深さが生値 | **直っていない** | 364 行・376 行とも `${state.depth}` |
| UIR-29 preset が自分の軸を殺す | **直っていない** | UIR-5 / UIR-6 に従属 |
| UIR-30 しるしが色だけ | **大部分直った** | `SHAPE`（256 行）で形が変わる。ただし 5 種中 2 種が同形・地図は常に円 → UIR-43 |
| UIR-31 桁揃え | **直っていない（副次的に一部改善）** | `H[2]` は今も `sm` のみ（283 行）。検索が `rowHTML` 経由になったので時刻には `num` が付くようになった |
| UIR-32 空・エラー | **直った** | `STATUS`（208 行）と 339〜344 行。「読み込み中」は無い / 画面別の出し分けは無い → UIR-46 |
| UIR-33 最長・最多 | **半分** | `LONG`（261 行）で 28 文字の場所名と写真 8 枚。件数は 9 → 10 で、要件の上限 15 件には届かない。日は 14 行のまま（`#cnt` は 30 件と主張） |
| UIR-34 `cap` が 7px まで縮む | **直った** | 260 行 `cap:Math.max(10,b/r)` |
| UIR-35 フォーカスの可視 | **直っていない** | `:focus` / `outline` は 0 件 |
| UIR-36 playground 自身の chrome | **直っていない** | `.tg input` 19px、`.n` 3.87:1 など変更なし |
| UIR-37 日付・曜日 | **直っていない** | 310 行「日曜日」（2026-09-07 は月曜）/ 186 行の `9/30` |

### 第 2 回の新規指摘

#### UIR-38 — 気分チップの色が `pal()` の想定と 2 か所ずれており、NFR-18 が全状態では成立しない 【重大度: 高】
- 観点: 2
- 根拠: `pal()` はチップ面を
  `chip = dark ? Math.min(LIM, card+9) : Math.max(LIM, card-9)`（242 行）として `faces` に入れ（244 行）、
  `hsl2rgb(h, s*0.3, chip)` として測っている（251 行）。ところが実際に描かれるチップは
  272 行の `hsl(${accHue(p.h,3,it.m)} ${p.s*0.7}% ${p.card+(ctx.theme==="dark"?9:-9)}%)`。
  ずれは 2 つ:
  1. **`p.chip` が一度も使われていない**（`grep "p.chip"` → 定義（242, 252 行）のみ、参照 0 件）。
     描画は clamp 前の `card±9`。例: ダーク・深さ 90・段数 2 で `p.chip=34.0` に対し実際は **43.0%**。
  2. **色相と彩度が違う。** 測るときは `h` / `s*0.3`、描くときは `accHue(h,3,m)`（基準色相 ±50°）/ `s*0.7`。
     彩度は 2.33 倍、色相は最大 50° ずれるので輝度が変わる。
  それぞれの寄与を分離して 12,672 通りで測ると:
  | 直し方 | 最小 | 4.5 未満 |
  |---|---|---|
  | 現状 | 2.28:1 | 2,962 |
  | 明度の clamp だけ効かせる | 3.46:1 | 989 |
  | 色相・彩度だけ合わせる | 3.78:1 | 1,293 |
  | 両方直す | **4.50:1** | **0** |
- 再現: 何も触らず（色相 28° / 彩度 18% / 段数 2 / ダーク）**地の深さだけを 100 にする**。
  主表現の気分チップ「+1」「+2」の文字が **3.45:1**。左パネルは「最小 4.5:1」と表示し続ける。
  彩度 55%・深さ 20 でも 4.31:1 で割れる。
- 何が起きるか: 第 1 回 UIR-13 と同じ結末（要件を割った状態を「成立」と表示したまま先に進む）。
  しかも今回は**「深さ」を広げた修正が違反領域を広げている**ため、UIR-1 の修正と衝突している。
- 直し方の案: `faces` に入れる値と描画する値を 1 か所から出す。
  `pal()` がチップの完成色（色相 3 本 × `s*0.7` × clamp 後の明度）を配列で返し、
  `rowHTML` はその配列を引くだけにする。そうすれば上表の「両方直す」＝違反 0 件になる。

#### UIR-39 — 帯（LIM）で閉じたことにより、「地の深さ」を上げると「面の段数」が動かなくなる 【重大度: 中】
- 観点: 1
- 根拠: 238〜239 行
  `room=Math.abs(LIM-s0)/Math.max(1,段数-1)` / `step=dir*Math.min(3.2+段数*1.3, Math.max(1.5,room))`。
  深さ 100（ダーク）では `s0=31`、`LIM=34` なので `room` は 3/(段数-1)。
  段数 3 → `room=1.5`、段数 4 → `room=1.0` だが `Math.max(1.5,…)` で下げ止まるため、
  **段数 3 と 4 が同じ step=1.5 になる**。カード地と地のコントラストは 1.062:1（＝ ほぼ同一）。
- 再現: 面の段数を 4 にしてから、地の深さを 0 → 100 に振る。
  面の差が 1.193:1 → 1.062:1 に潰れ、カード・展開・写真枠の段差が消える。
- 何が起きるか: 2 つの軸が独立でない。「深い地」を選んだ人は「面の段数」を選べなくなる。
  第 1 回 UIR-1（動くが変わらない軸）を、別の軸に移し替えた形になっている。
- 直し方の案: 帯の上限 `LIM` を固定値ではなく「文字が 4.5:1 を取れる最大明度」から毎回求める
  （彩度・色相に依存する）。または `s0` の上限を段数に応じて下げる
  （段数 4 なら深さの上端を 31 ではなく 20 にする、など軸の側で吸収する）。

#### UIR-40 — `--s3` は出力されたが、既定の 2 段では `s1 = s2 = s3` のまま 【重大度: 中】
- 観点: 1
- 根拠: 240 行 `const L=i=>s0+step*Math.min(i,state.surfaces-1);` は第 1 回から変わっていない。
  実測（ダーク・既定の深さ 14）: 1 段 → 6.9/6.9/6.9/6.9、**2 段 → 6.9/12.7/12.7/12.7**、
  3 段 → 6.9/14.0/21.1/21.1、4 段 → 6.9/15.3/23.7/32.1。
  `.ph i{background:var(--s3)}`（80 行）と `.expand{background:var(--s2)}`（95 行）は
  `.rw-card{background:var(--s1)}`（62 行）と同色になる。
- 再現: 並べ方「カード」＋ 面の段数 2（既定）＋ 主役「写真」。写真枠がカード地に溶ける。
  preset「のんびり」は段数 3 なので `s3=s2` で、写真枠と展開ブロックが同色になる。
- 何が起きるか: 「面を何段で作るか」の選択が、**選んだ段数より 1 つ少ない段しか実現しない**。
  第 1 回 UIR-2 の指摘の後半（面の潰れ）が未解決のまま残っている。
- 直し方の案: 段数 n のときに使う面を n 個に限定し、`--s2` / `--s3` を
  「n 段のうち何番目か」で割り当てる（例: 2 段なら写真枠は `--s0` に落とす）。

#### UIR-41 — 表示される「最小 X:1」が実際の最小ではなく、方向を間違えて外れる 【重大度: 中】
- 観点: 2, 4
- 根拠: 348 行の `#cr` と 382 行の prompt は `p.ratio`（251 行）を出すが、
  `p.ratio` は UIR-38 のとおり**実際とは違う色**でチップを測っている。実測の対応:
  | 状態 | 表示 | 実際の最小 | 向き |
  |---|---|---|---|
  | 既定・ダーク | 4.5:1 | 5.18:1 | 過小申告（安全側） |
  | 既定・ダーク・深さ 100 | 4.5:1 | **3.45:1** | **過大申告（危険側）** |
  | preset「地図から」・ダーク | 4.5:1 | 5.07:1 | 過小申告 |
  なお「全 N 面」の N（`state.surfaces+1`、348 行）は面の数＋チップで、この数え方自体は正しい。
- 何が起きるか: 「最小」と名乗る数字が最小でないので、この表示を根拠に判断できない。
  とくに深い地を選んだときに危険側に外れる。
- 直し方の案: UIR-38 を直せば自動的に一致する。加えて、割れているときは
  `#cr` を赤く出し、どの面で割れたか（"チップ m=+2"）を併記する。

#### UIR-42 — 検索結果を `rowHTML` に通した副作用で、副題が二重になり数値列に入る 【重大度: 中】
- 観点: 3, 4
- 根拠: 313 行 `rowHTML({...it, d:`${it.s} ・ ${it.d}`},p,t,i)`。
  `it.s`（"滞在"）と `it.d`（"滞在 55 分"）を連結するので **`d` は "滞在 ・ 滞在 55 分"** になる。
  この `d` は、表レイアウトでは 278〜279 行の `<span class="sm num">` に入る
  ＝ 幅 `calc(var(--body)*4)`（既定 60.8px）の**数値列**に 11 文字が押し込まれる。
  加えて `drill:"inline"` のとき 285 行の展開行（"生の記録 412 件 ・ 写真 3 枚 …"）が
  検索結果の 4 件目（`IMG_2318.jpg`＝写真のヒット）に出る。
- 再現: 画面「検索結果」＋ 並べ方「表」。3 列目に "滞在 ・ 滞在 55 分" が折り返して入る。
- 何が起きるか: UIR-20 の修正で軸は効くようになったが、検索結果の見え方が壊れたため
  「同じ設定が 3 枚とも成立するか」の判断がまた別の理由でできない。
- 直し方の案: `rowHTML` に「副題」を独立した引数で渡す（`it.sub`）か、
  検索の `d` を `it.d` のみにして `it.s` は `.ic` の隣のラベルに出す。

#### UIR-43 — しるしの形が 5 種中 2 種で重複し、地図の pin は常に円 【重大度: 低】
- 観点: 5
- 根拠: 256 行 `const SHAPE=["50%","3px","0 0 50% 50%","50% 0 50% 0","3px"];`
  — **`SHAPE[1]` と `SHAPE[4]` が同じ `"3px"`**。`ACC_N=5` なので種類は 5 つあるのに形は 4 種類。
  さらに 303 行のピンは `border-radius:${SHAPE[s.k%ACC_N]==="50%"?"50%":"50%"}` で、
  **三項演算子の両辺が同じ `"50%"`** ＝ 常に円。地図では形の区別が存在しない。
- 再現: しるしを ON にして preset「地図から」。ピンはすべて円。
  主表現のリストでは k=1（飲食）と k=4（買い物）が同じ角丸四角。
- 何が起きるか: 「色だけに頼らない」という決定が、5 種のうち 2 種と地図画面で成立しない。
  UIR-30 は大部分直っているが、この 2 か所だけ穴が残る。
- 直し方の案: `SHAPE[4]` を別の形にし（例 `"50% 50% 0 0"`）、
  ピンの `border-radius` に `SHAPE[s.k%ACC_N]` をそのまま入れる。

#### UIR-44 — 今回新しく置いた運用値 3 つが、開示欄に載っていない 【重大度: 低】
- 観点: 2
- 根拠: 111 行の開示欄は「本文 5.5:1 / アクセント 3.0:1 / 文字の彩度 15% / 800px / 種類の色は 5 本」の 5 件。
  今回追加された次の 3 つは要件にも出所にも無い、この工程が置いた数値だが載っていない:
  | 値 | 場所 |
  |---|---|
  | 面を閉じ込める帯 **ダーク ≤34% / ライト ≥66%** | 236 行 `const LIM=dark?34:66` |
  | 境界線は地から **13 ポイント**離す | 250 行 `s0+dir*13` |
  | 二次テキストの下限 **10px** | 260 行 `cap:Math.max(10,b/r)` |
- 何が起きるか: UIR-18 と同じ型の再発。とくに `LIM` は
  「地の深さ」と「面の段数」の可動域を決めている一番効く数字（UIR-39 の原因）なのに、画面から見えない。
- 直し方の案: 開示欄に 3 行足す。`LIM` は「この帯を外すと 4.5:1 が取れない」という理由も 1 行で書く。

#### UIR-45 — 帯に閉じ込めたはずの面が、1 つの組み合わせで帯を越える 【重大度: 低】
- 観点: 1
- 根拠: 239 行 `Math.max(1.5, room)` が clamp より優先されるため、`room < 1.5` のとき step が 1.5 に固定される。
  12,672 通りを走査すると越えるのは **1 通りだけ**（ダーク・深さ 100・段数 4 で `s3 = 35.5% > LIM = 34%`、超過 1.5 ポイント）。
- 何が起きるか: 実害はほぼ無い（`--s3` は写真枠で文字が載らず、`faces` には実値が入るので
  文字色の探索は正しく行われる）。ただしコメント「NFR-18 が成立する帯に面を閉じ込める」（235 行）は厳密には成り立っていない。
- 直し方の案: `L(i)` の出力側で `Math.min`/`Math.max` を掛けて帯に丸める。

#### UIR-46 — 空・取得エラーが画面によらず同じ 1 枚で、「読み込み中」が無い 【重大度: 低】
- 観点: 3, 5
- 根拠: 339〜344 行は `ctx.status` を先に見て `inner` を差し替えるので、
  **`ctx.screen` が 検索結果 / 稼働状況 でも同じ「まだ何も記録がありません」が出る**。
  検索の 0 件（"「みなと公園」に一致なし"）と、稼働状況の欠測（既に `SOURCES` の "健康 0%" がある）と、
  主表現の空日は性格が違う。また `STATUS`（208 行）に「読み込み中」が無い。
  さらに `items()` の `if(ctx.status==="empty")return[];`（266 行）は
  `render()` が先に分岐するため到達しない。
- 何が起きるか: 空とエラーを「入れた」ことにはなるが、3 画面それぞれで成立するかは確かめられない。
- 直し方の案: `screenSearch` / `screenHealth` にも空・エラーの分岐を置くか、
  空の文面を `ctx.screen` で切り替える。

#### UIR-47 — 「最長・最多」が全行を長くしてしまい、現実的な「1 件だけ長い」が作れない 【重大度: 低】
- 観点: 5
- 根拠: 267 行 `return[LONG,...a.map(x=>({...x,n:x.n+"（最長ケース確認用に伸ばした名前）"}))];`
  — **すべての行の名前に 17 文字を足す**。実際に起きるのは「9 件のうち 1 件だけ長い」であって全行ではない。
  また件数は 9 → 10 件で、doc が挙げた上限「1 日 15 件」（`docs/ui-direction.md` 68 行）に届かない。
  単位「日」では 14 行のまま（`#cnt` は「1 か月 30 件」と表示）。
- 何が起きるか: 折り返しの確認はできるが、「1 件だけ突出したときに列が揃うか」
  「最多の日で 1 画面に収まるか」という本来の問いには答えられない。
- 直し方の案: `LONG` を混ぜるだけにして他行は元のまま、件数は別に 15 件版の配列を用意する。

---

*以下は第 1 回（同日）の記録。指摘は消していない。各 ID の現状は上の判定表を参照。*
*行番号は修正前の版（409 行）のものなので、現在の版（437 行）とはずれる。*

## 第 1 回

### 良い点（壊さないために先に）

- **色相は本当に 1 値から導いている。** `hsl(` の出現箇所（59〜92 行、314〜315 行、
  `rowHTML` / `screenSearch` / `screenHealth` / `map` / `cal`）はすべて第 1 引数が `p.h`
  または `accHue(p.h,…)`。固定色相・固定アクセント配列は 1 つも無い。**既知の失敗 (a) は解消されている。**
- **密度の下限が NFR-19 で止まっている。** `.row` `.rw-tbl` `.mchip` に `min-height:24px` があり、
  密度 0 / 本文 12px（行の中身は 17px 相当）でも行の高さは 24px で床を打つ。可動域の下限が要件で決まっている。
- **運用値（彩度 55% 上限・角 4px 下限）を要件と区別して画面に書いている**（NFR パネル 5 行目）。
  これは前版の失敗の型に対する正しい処置。ただし後述 UIR-18 のとおり、書かれていない運用値が他に 4 つある。

### 観点ごとの結果

#### 1. 軸が実際に動くか

指摘 12 件（UIR-1〜12）。色相は完全に可動（上記）、彩度・角・行間・比・密度も state → CSS 変数まで
到達していることを確認した。一方で **「地の深さ」は既知の失敗 (b) がほぼそのまま残っている**（UIR-1）、
**「面の段数」は 4 段のうち 3 段しか出力されない**（UIR-2）、
**「境界線で区切る」は既定状態では見えない**（UIR-3）。
骨格の軸どうしが互いを無効化する箇所が 3 つある（UIR-5, 6, 7）。

#### 2. 要件との整合

指摘 7 件（UIR-13〜19）。**NFR-18 は既定状態で既に割れている**（気分チップ 3.99:1、UIR-13）。
NFR-19 は地図の pin で割れる（UIR-16）。しかも playground 自身は「4.5:1 で成立」と表示する（UIR-15）。
要件に無い自前の閾値が 4 つ、開示されないまま入っている（UIR-18）。

#### 3. 文脈の網羅

指摘 3 件（UIR-20〜22）。ライト / ダーク・幅 360〜1440・画面 3 枚は**存在する**。
ただし **3 画面のうち 2 枚が骨格・主役の軸を一切読まない**（UIR-20）ので、
「同じ設定が 3 枚とも成立するか」は色と間隔についてしか確かめられない。
狭い幅での破綻は `#frame{overflow:hidden}` に切り取られて見えない（UIR-21）。

#### 4. 一貫性

指摘 7 件（UIR-23〜29）。**同じ種類が画面によって別の色になる**（UIR-23）のが最大。
preset は名前と値がおおむね噛み合っているが、「見渡す」の行間 1.38（UIR-25）と、
「暦から」「見渡す」が選んだ瞬間に自分の骨格軸を無効化する点（UIR-29）が残る。
prompt 出力と state の不一致が 4 件（UIR-15, 26, 27, 28）。

#### 5. UI の落とし穴

指摘 7 件（UIR-30〜36）。**色だけの区別は「しるし」トグルを ON にしても解消しない**（UIR-30）。
桁揃えは時刻には入っているが所要時間に入っていない（UIR-31）。
空・エラー・読み込み中が 1 つも無い（UIR-32）。最長・最多ケースが無い（UIR-33）。
日本語の行間は preset とスライダ下限が 1.5 を割る（UIR-25）。
フォーカスの可視はソースにも doc にも一度も現れない（UIR-35）。
「触れる対象の重なり」は **該当なし**（`.row` 24px 床 + `.rw-card` の margin で、
密度最小でも隣接対象は 24px 離れる）。

---

### 指摘一覧

#### UIR-1 — 「地の深さ」の可動域が 20 ポイントしかない（既知の失敗 (b) がほぼそのまま） 【重大度: 高】
- 観点: 1
- 根拠: 230 行 `const s0=dark?4+state.depth*0.20:100-state.depth*0.20;`。
  スライダは 0〜100（135 行）だが、ダークで地の明度は **4% → 24%**、ライトで **100% → 80%**。
  前版の失敗として報告された「4%〜20%」から**上端が 4 ポイント広がっただけ**。
  計算値: ダーク depth=0 で `rgb(11,10,10)`、depth=100 で `rgb(65,61,58)`。
  両端どうしのコントラスト比は **1.84:1**（ライトは 1.60:1）。
  1 目盛あたり明度 0.2 ポイント＝ RGB で約 0.5。10 目盛動かして RGB が 5 変わる。
- 再現: 「表面 — 色」を開き、地の深さを 0 と 100 に振る。ダークの背景は
  `rgb(11,10,10)` と `rgb(65,61,58)` の間しか動かない。
- 何が起きるか: 「沈み具合」を決める軸として置いたのに、選んでも選ばなくても結果が
  ほぼ同じになり、**この軸の決定が空振りしたまま prompt に「地の深さは 30」とだけ出る**。
  前版と同じ失敗を再度通すことになる。
- 直し方の案: 端の値を「意味のある 2 つの状態」から逆算する（例: ダーク側 3%〜38%、
  ライト側 100%〜88% の別カーブ）。あるいは 0〜100 の連続値をやめて
  「漆黒 / 標準 / 浅い」の 3 択にし、可動域が狭いこと自体を設計として明示する。

#### UIR-2 — 「面の段数」の 4 段目が存在しない（`--s3` が出力されていない） 【重大度: 高】
- 観点: 1
- 根拠: `pal()` は `s3:L(3)` を返す（237 行）が、`render()` が `f.setAttribute('style',…)` で
  出力するのは `--s0` `--s1` `--s2` だけ（314〜315 行）。`--s3` は CSS 側にも 1 箇所も無い
  （`grep -- "--s3"` の結果は `pal()` の戻り値のみ）。
  さらに `L=i=>s0+step*Math.min(i,state.surfaces-1)` により、
  **段数 2 では `s2 === s1`**（実測 dark/既定: s0=6.8, s1=12.6, s2=12.6）、
  **段数 1 では `s0 === s1 === s2`**（実測: 全部 6.8）。
  結局この軸が作る「実際に異なる面の数」は 1, 2, 3, 3。
- 再現: 面の段数を 3 → 4 に動かす。増える面は無く、変わるのは `step`（=3.2+段数*1.3）の
  大きさとアクセント色の本数（UIR-23）だけ。
- 何が起きるか: 「面を何段で作るか」という決定が、選択肢 4 のうち 2 つでしか区別できない。
  段数 1 を選ぶと `.rw-card` `.ph i` `.expand` `.danger` の枠がすべて背景と同一色になり、
  カード・写真枠・展開ブロック・削除ボタンの枠が消える。
- 直し方の案: `--s3` を出力して `.expand` か `.ph i` のどちらかを s3 に載せる。
  段数 1 を選べる仕様を残すなら、「段数 1 のときは境界線トグルが必須」と
  軸の依存を画面に書く（UIR-3 と同じ処置で足りる）。

#### UIR-3 — 「境界線で区切る」トグルが既定状態では見えない 【重大度: 高】
- 観点: 1
- 根拠: 319 行 `--rule:${state.rule?`1px solid hsl(${p.h} ${p.s*0.3}% ${p.s2}%)`:"0"}`。
  線の色は `--s2` と同じ明度。UIR-2 のとおり段数 2 では `s2 === s1` なので、
  - `.rw-card`（背景 `--s1`）に引かれる線は **1.00:1（完全に同色）**
  - `.rw-tbl:nth-child(even)`（背景 `--s1`）の下線も **1.00:1**
  - `.rw-list`（背景 `--s0`）の下線で **1.16:1**（既定 dark）／ライトで 1.14:1
  段数 3 でも最大 1.55:1、段数 4・深さ 100 でも 1.95:1 が上限。
- 再現: preset「見渡す」（`rule:true, surfaces:2, layout:"table"`）を押す。
  表の罫線は偶数行で 1.00:1、奇数行で 1.14:1（ライト）。「区切る」を切っても見た目は変わらない。
- 何が起きるか: 「線で区切るか、面の差で区切るか」という設計上の分岐が、
  どちらを選んでも同じ絵になる。**動くが変わらないスライダの型がここにもう 1 つある。**
- 直し方の案: 罫線の色を面の系列から外し、`fg` と `s0` の間から別に引く
  （例: `dim` を 8〜12% の不透明度で重ねる、または `s2` ではなく `L(surfaces)` を使う）。

#### UIR-4 — 「並べ方 = カード」が既定では「リスト」とほぼ同じ絵になる 【重大度: 中】
- 観点: 1, 4
- 根拠: `.rw-card{background:var(--s1)}`（62 行）。既定（段数 2・深さ 14・ダーク）で
  `--s1` と `--s0` のコントラストは **1.16:1**。段数を最大 4・深さ 100 まで振っても **1.40:1** が上限。
  既定は `rule:false` なので枠線も無い（204 行 DEFAULTS）。
- 再現: 並べ方をリスト ⇄ カードで切り替える。変わるのは padding（`sp*0.55/0.7`）と
  margin（`sp*0.42`）だけで、面の差は 1.16:1。
- 何が起きるか: 骨格の軸「1 件ずつ囲うか / 1 行ずつか」の違いが余白の差にしか出ず、
  この軸を選んだ判断が「囲い方」ではなく「余白量」の判断にすり替わる。
- 直し方の案: カード面の明度差を段数から独立させる（最低 1.5:1 程度の床を持たせる）か、
  カードを選んだときだけ `--rule` を自動で立てて依存関係を明示する。

#### UIR-5 — 「並べ方 = 表」を選ぶと「詳細の降り方」が完全に無効になる 【重大度: 中】
- 観点: 1, 4
- 根拠: `rowHTML` の表分岐（256〜257 行）は `<div class="rw-tbl">…</div>` を返して
  **その場で return する**。`ex`（`.expand`）と `chev`（`›`）を組み立てるのは
  264〜265 行、つまり return より後。したがって `state.drill` は表レイアウトでは一切読まれない。
- 再現: 並べ方を「表」にして、詳細の降り方を「その場で開く」⇄「画面を移る」で切り替える。
  DOM は 1 文字も変わらない。preset「見渡す」がまさにこの組み合わせ（`layout:"table", drill:"push"`）。
- 何が起きるか: 骨格 4 軸のうち 1 つが、別の 1 軸の選び方で消える。
  組み合わせの一方（表 × 降り方）が未検証のまま「決めた」ことになる。
- 直し方の案: 表分岐にも chevron 列と展開行（`grid-column:1/-1`）を足す。
  あるいは「表では詳細は必ず画面遷移」と決め、その依存を画面に書いて `drill` を無効表示にする。

#### UIR-6 — 「主表現 = カレンダー」を選ぶと骨格・主役の軸が 5 つ死ぬ 【重大度: 中】
- 観点: 1, 4
- 根拠: `screenMain` のカレンダー分岐（273〜277 行）は `.cal` の 35 セルを描くだけで、
  `state.layout` `state.hero` `state.icon` `state.drill` を参照しない。
  `state.unit` は末尾の `.more` の文言（"生の記録" / "滞在" / "その日"）にのみ現れる。
- 再現: preset「暦から」（`main:"calendar", layout:"table", unit:"day", hero:"mood", drill:"push"`）を押し、
  並べ方や主役を動かす。画面は変化しない。
- 何が起きるか: preset「暦から」が主張する性格（表・日単位・気分が主役）を、
  その preset を選んだ状態では**一度も見られない**。
- 直し方の案: カレンダーのセル内に主役（気分の数値 / 写真の枚数 / 滞在時間）を出し、
  日を選んだときの下段リストを `layout` と `drill` に従って描く。

#### UIR-7 — 「主表現 = 一覧（長さや回数で並べ替える）」が滞在以外では並べ替えない 【重大度: 中】
- 観点: 1
- 根拠: 286 行 `[...list].sort((a,b)=>(b.mn||0)-(a.mn||0))`。
  ソート鍵 `mn` を持つのは `STAYS` だけ。`DAYS` は `mn:0` 固定（181〜183 行）、
  `items()` の raw 分岐も `mn:0` を詰めている（245〜246 行）。
- 再現: 1 行の単位を「日」または「生の記録」にしたまま、主表現を「時系列」⇄「一覧」で切り替える。
  行の並びは完全に同一で、見出しだけ "長くいた順" に変わる。
- 何が起きるか: 見出しが「長くいた順」と言っているのに順序が時刻順のまま、という
  **表示と内容が食い違う状態**を、そうと分からずに選べる。
- 直し方の案: `DAYS` に滞在合計分、raw に継続秒を持たせる。
  持たせないなら、単位が「日 / 生の記録」のとき「一覧」を選べないようにする。

#### UIR-8 — 幅スライダの値と実際の描画幅がずれる（表示は要求値のまま） 【重大度: 中】
- 観点: 1, 3
- 根拠: 313 行 `width:${Math.min(ctx.width,1440)}px` と同じ行の `max-width:100%`。
  `#frame` の実効幅は `.stage` の内側に頭打ちされる。
  `.wrap{grid-template-columns:352px 1fr}`（9 行）＋ `.stage{padding:22px}`（50 行）なので、
  1440px を実際に描くにはブラウザ幅 **1836px 以上**が要る。
  一方 `v-width` は `ctx.width+"px"` をそのまま表示（399 行）。
- 再現: 1440px 幅のノート PC で幅スライダを 1440 にする。表示は「1440px」だが
  実際の枠は約 1044px。使う側にはクランプされたことが伝わらない。
- 何が起きるか: 「PC 幅で成立するか」を確かめたつもりで、確かめていない幅の絵を見る。
- 直し方の案: `getBoundingClientRect().width` を測って要求値と違うときは
  `v-width` に「(実効 1044px)」を併記する。

#### UIR-9 — プレビューに幅依存の規則が 1 つも無い 【重大度: 中】
- 観点: 3
- 根拠: `@media` はファイル全体で 1 か所（10 行 `@media(max-width:960px){.wrap{…}}`）だけで、
  これは playground の枠組み（左パネルの折り返し）用。`@container` は 0 件。
  `.pv` 配下の指定はすべて `--body` `--sp` からの相対で、幅を参照しない。
- 再現: 幅を 360 と 1440 に振る。行の中身は同じで、`flex:1` の名前欄が伸びるだけ。
  表の固定列（`calc(var(--body)*3.4)` など、65 行）は幅に関係なく同じ px。
- 何が起きるか: 「狭い幅でゆったり」が「広い幅でスカスカ」になるかを見る、という
  文脈の目的が果たせない。1440px では 1 行が 1300px 超の行長になるが、
  それが妥当かを判断する材料（列を増やす / 最大行長を切る）が画面に無い。
- 直し方の案: 主表現に「広い幅では 2 列 / 最大行長 60〜75 字で止める」といった
  幅の振る舞いを 1 つだけ入れて、幅の文脈が実際に何かを変える状態にする。

#### UIR-10 — 「本文の大きさ」の表示値と実寸がずれ、prompt にもずれた値が出る 【重大度: 低】
- 観点: 1, 4
- 根拠: 243 行 `typo(){const dm=0.88+state.density*0.0026, b=state.base*dm; …}`。
  一方 `v-base` は `state.base+"px"`（397 行）、prompt も
  `q("base",\`本文は ${state.base}px\`)`（342 行）。
  実測: base=15・密度 50 → 15.2px、base=15・密度 0 → 13.2px、base=16・密度 74（preset「のんびり」）→ **17.2px**。
- 再現: 本文 16px・密度 74 で prompt をコピーすると「本文は 16px」と書かれるが、描かれているのは 17.2px。
- 何が起きるか: 本文サイズは要件の下限を意図的に落とした軸（doc 97〜98 行）なので、
  ここで出た数値がそのまま実装の唯一の根拠になる。1〜2px ずれた値が正典として渡る。
- 直し方の案: 表示と prompt を `typo().body` の実寸に変える（「本文は 17px（基準 16 × 密度 74）」）。

#### UIR-11 — 彩度スライダの上 3 分の 1 で文字色が変化しない 【重大度: 低】
- 観点: 1
- 根拠: 232 行 `const ts=Math.min(s*0.42,15);`。`s*0.42 = 15` となるのは s ≈ 35.7。
  スライダ上限は 55（134 行）なので、**彩度 36〜55 の範囲で `--fg` `--dim` の彩度は 15% で固定**。
- 再現: 彩度を 36 → 55 に振る。地とアクセントは色付くが、本文・二次テキストの色味は止まったまま。
- 何が起きるか: 実害は小さいが、「文字も含めて色を決めている」という説明
  （「地・文字・アクセント・写真枠まで、すべてこの 1 つから導く」133 行）と挙動が合わない。
- 直し方の案: 15% という上限を NFR パネルの運用値の欄に書き足す（UIR-18 と同じ処置）。

#### UIR-12 — 色相スライダのトラック自体が state を反映しない固定色 【重大度: 低】
- 観点: 1
- 根拠: 28〜30 行 `#hue{background:linear-gradient(90deg,hsl(0 45% 55%),…)}`。
  彩度 45% 固定で、実際に使う面の彩度（`s*0.3` = 最大 16.5%）とも
  アクセント（最大 55%）とも一致しない。
- 何が起きるか: 選ぶ前に見える色見本が実際より派手で、NFR-21（原色を避ける）の
  判断が見本の側で歪む。
- 直し方の案: トラックの彩度を `state.sat` に連動させる（`all()` で背景を差し替える）。

---

#### UIR-13 — 気分チップの文字が NFR-18（4.5:1）を既定状態で割っている 【重大度: 高】
- 観点: 2
- 根拠: 251 行 `<span class="mchip" style="background:hsl(… ${p.s*0.7}% ${p.card+(ctx.theme==="dark"?9:-9)}%)">`。
  `.mchip` の文字色は `color:var(--fg)`（74〜75 行）。ところが `--fg` は
  **card に対して 5.5:1 になるよう探索された値**（234 行 `seek(…,card,dir,5.5)`）であって、
  `card±9%` のチップ地に対しては計算されていない。実測（Node で再計算）:
  | 状態 | fg / チップ地 |
  |---|---|
  | 既定・ダーク | **3.99:1** |
  | preset「のんびり」・ダーク | **3.86:1** |
  | preset「地図から」・ダーク | **3.55:1** |
  | preset「見渡す」・ダーク | **4.16:1** |
  | preset「暦から」・ダーク | **4.25:1** |
  | 最悪（彩度 55・深さ 100・段数 4・ダーク） | **2.82:1** |
  色相 5 種 × 彩度 18 段 × 深さ 11 段 × 段数 4 × 明暗 2 の全 1584 通りのうち
  **1171 通り（74%）が 4.5:1 未満**。ライトはおおむね 4.4〜4.8 で境界上。
- 再現: 何も触らずに主表現を開く。「+1」「+2」「-1」のチップの文字が 3.99:1。
- 何が起きるか: **playground の既定状態そのものが要件を割っている。**
  しかも `#cr` は「5.5:1 で成立」と表示するので、割れていることが画面から分からない。
  気分の数値（FR-36〜43）は主観入力の中心で、色だけでなく数字で読ませる唯一の場所。
- 直し方の案: チップの文字色を専用に探索する（`seek(h, ts, chipBg, chipL, dir, 4.5)`）か、
  チップ地を `card` と同じにして枠線だけで区別する。

#### UIR-14 — 「その場で開く」展開ブロックの文字が段数 3 以上で 4.5:1 を割る 【重大度: 高】
- 観点: 2
- 根拠: 92 行 `.expand{background:var(--s2); … color:var(--dim)}`。
  `--dim` は **card に対して 4.5:1** で探索された値（235 行）。`--s2` は card より
  さらに 1 段離れているため、その上では 4.5 を割る。実測:
  | 状態 | dim / --s2 |
  |---|---|
  | preset「のんびり」・ダーク（段数 3, drill:inline） | **3.54:1** |
  | preset「地図から」・ダーク（段数 3, drill:inline） | **3.42:1** |
  | 最悪（彩度 55・深さ 50・段数 4・ダーク） | **3.11:1** |
  段数 2 では `s2 === s1` なので 4.52:1 で通る（UIR-2 の副作用でたまたま通っている）。
  文字サイズは `var(--cap)`＝ 12〜13px で、NFR-18 の「大きい文字（3:1）」には該当しない。
- 再現: preset「のんびり」を押す。4 行目に出る「生の記録 412 件 ・ 写真 3 枚 …」が 3.54:1。
- 何が起きるか: 骨格の既定（`drill:"inline"`）で開く先の文字が要件を割る。
  「その場で開く」を選ぶこと自体が要件違反の選択になる。
- 直し方の案: `--dim` の探索基準を「その色が載りうる最も近い面」＝ `--s2` に変える
  （＝ `seek(…, s2, …, 4.5)`）。面の段数を増やすほど文字が明るくなる方向で連動する。

#### UIR-15 — playground が「4.5:1 で成立」と断言するが、測っているのは 1 組だけ。しかも prompt はテーマ名を固定文字で出す 【重大度: 高】
- 観点: 2, 4
- 根拠: 238 行 `ratio:cr(hsl2rgb(h,ts,fg), hsl2rgb(h,s*0.3,card))` — 測っているのは
  **本文色 × カード地の 1 組だけ**。`.mchip`（UIR-13）、`.expand`（UIR-14）、
  `.rw-tbl:nth-child(even)` 上の `.sm`、`.cal div` 上の `--dim` は測っていない。
  そのうえで 324 行 `document.getElementById('cr').textContent = p.ratio.toFixed(1)+":1 で成立（…）"`。
  さらに prompt テンプレート（355 行）は
  `本文のコントラスト 4.5:1 以上（NFR-18。いまダーク ${p.ratio.toFixed(1)}:1）` で、
  **"ダーク" が固定文字列**。`p` は `ctx.theme` で計算されるので、
  ライトを選んだ状態でコピーすると「いまダーク 5.6:1」というライトの値が出る。
- 再現: テーマをライトにして「指示をコピー」。文面は「いまダーク 5.6:1」。
- 何が起きるか: 要件を満たしている証拠として prompt に載る数字が、
  (1) 最悪値ではない (2) テーマ名が間違っている。この文面が doc の「決めた方向」に
  そのまま貼られる設計（doc 102 行）なので、**誤った適合の記録が正典に入る。**
- 直し方の案: 測る対象を「地 × 文字」「カード × 文字」「チップ × 文字」「展開 × 二次文字」の
  4 組に増やし、最小値を表示する。テーマ名は `ctx.theme` から生成する。

#### UIR-16 — 地図の pin が 24×24px を割る（NFR-19） 【重大度: 中】
- 観点: 2, 5
- 根拠: 280〜282 行 `width:${(12+s.mn/14).toFixed(0)}px;height:同`。
  `STAYS` の `mn`（滞在分）は 190/65/140/55/25/100/50/35/260 なので、pin の直径は
  **25.6 / 16.6 / 22.0 / 15.9 / 13.8 / 19.1 / 15.6 / 14.5 / 30.6px**。
  **9 個中 7 個が 24px 未満**、最小 13.8px（スーパーやまと・25 分）。
- 再現: preset「地図から」を押す（`main:"map"`）。短時間の滞在ほど小さくなる。
- 何が起きるか: 主表現に地図を選ぶと、**滞在が短いほど触れなくなる**。
  NFR-19 は SC 2.5.8 由来で、間隔による例外はあるが pin は互いに重なりうる配置
  （280 行 `left:18+i*8.5%` / `top:28+(i%4)*16%`）なので例外に乗らない。
- 直し方の案: 見た目の円と当たり判定を分ける（`::after` で 24×24 の透明領域）か、
  直径の下限を 24px にして滞在の長さは色の濃さや縁の太さで表す。

#### UIR-17 — カレンダーの全セルに 44px を課しており、要件が 44px を限定した理由と衝突する 【重大度: 中】
- 観点: 2
- 根拠: 86 行 `.cal div{… min-height:44px …}`。
  一方 `docs/requirements.md` の NFR-20 は
  「1 画面に 20 件前後の行が並ぶ想定で全行に 44px を課すと 1 画面に収まらなくなる」ため
  **取り返しの付かない操作にのみ** 44px を課す、と理由まで書いている。
  カレンダーのセルは削除でも停止でもない。
- 再現: 主表現をカレンダーにする。35 セル × 44px = 1540px（＋ gap）で、
  密度スライダを最小にしても縦は縮まない。密度の軸がカレンダーでだけ効かない。
- 何が起きるか: 要件が「課さない」と明示した場所に AAA の数値を持ち込んでいる。
  これは「要件に無い下限を playground が自前で立てた」型そのもの。
- 直し方の案: `min-height` を 24px にし、44px はそのままにする根拠が要るなら
  NFR パネルの運用値の欄に理由つきで書く。

#### UIR-18 — 開示されていない自前の閾値が 4 つある 【重大度: 中】
- 観点: 2
- 根拠: doc 60〜62 行と NFR パネル（107 行）は運用値を
  「彩度 55% 上限・角 4px 下限」の **2 つだけ**と書いている。実際にソースにある要件外の数値:
  | 値 | 場所 | 要件との関係 |
  |---|---|---|
  | 本文 **5.5:1** で探索 | 234 行 `seek(…,5.5)` | NFR-18 は 4.5:1。1 ポイント厳しい線を無断で引いている |
  | アクセント **3.0:1** で探索 | 236 行 `seek(…,3.0)` | 非テキストのコントラストは要件に無い（SC 1.4.11 は EXT に無い） |
  | 文字彩度 **15% 上限** | 232 行 `Math.min(s*0.42,15)` | 要件に無い（UIR-11） |
  | 「1 画面に収まる」= **800px** | 326 行 `h<=800?…` | 要件にも文脈（幅 360〜1440）にも無い（UIR-19） |
  加えて `.cal div` の 44px（UIR-17）。
- 何が起きるか: 「前版で要件に無い 7:1 / 24×24 を自前で立てた」という失敗と同じ型が、
  数値は穏当になったまま**開示されていない形で**残っている。
  特に 5.5:1 は、要件の線が 4.5:1 であることを画面から見えなくする
  （UIR-13/14 のように 4.5 を割る箇所があっても「5.5 で成立」と出る）。
- 直し方の案: NFR パネルの運用値の欄を表にして 5 つ全部を出所つきで並べる。
  出所が無いもの（800px）は消す。

#### UIR-19 — 「1 画面に収まる」判定が固定 800px、かつ部分データで測っている 【重大度: 低】
- 観点: 2, 3
- 根拠: 325〜326 行 `const h=f.getBoundingClientRect().height;`
  `… h<=800?\`1 画面に収まる · 約 ${…}px\`:\`はみ出す · …\``。
  幅（`ctx.width`）に関係なく 800px 固定。加えて測る対象は描画済みの行だけで、
  `RAW` は 12 件（`N_RAW_DAY=4120`）、`DAYS` は 14 件（`N_DAY_MONTH=30`）しか無い。
  一方 `#cnt` は「1 か月 30 件 — 1 画面に収まる」と表示する（327〜328 行）。
- 再現: 単位を「日」にする。14 行の高さで「1 画面に収まる」と出るが、
  主張している 30 件は描かれていない。
- 何が起きるか: 情報密度の軸を決める唯一の定量的な手がかりが、
  幅を無視し、件数を半分以下で測っている。
- 直し方の案: 閾値を `ctx.width` に応じた想定縦（例: 360px 幅→ 780、1440px 幅→ 900）にし、
  `DAYS` を 30 件に増やす。raw は 1 行の実高 × 4120 の推定値を併記する。

---

#### UIR-20 — 3 画面のうち 2 枚が骨格・主役の軸を一切読まない 【重大度: 高】
- 観点: 3
- 根拠: 各 state を読む画面を実際に数えた。
  | 軸 | 主表現 | 検索結果 | 稼働状況 |
  |---|---|---|---|
  | `main` | ○ | — | — |
  | `unit` | ○ | — | — |
  | `hero` | ○（`rowHTML` の `H`） | — | — |
  | `drill` | ○（表以外・UIR-5） | — | — |
  | `icon` | ○ | — | — |
  | `layout` | ○ 3 値 | △ **card / それ以外の 2 値**（293 行） | △ 同（302 行） |
  | 色・文字・間隔 | ○ | ○ | ○ |
  `screenSearch`（291〜299 行）と `screenHealth`（300〜308 行）は
  `state.layout==="card"?"rw-card":"rw-list"` しか見ておらず、
  **「表」を選んでもこの 2 画面はリストで描かれる**。行の組み立ても `rowHTML` を使わず
  独自の文字列で、`.big` `.sm` を直書きしている（主役の選択が反映されない）。
- 再現: 並べ方を「表」、主役を「写真」、しるしを ON にして画面を「検索結果」に切り替える。
  3 つとも反映されない。
- 何が起きるか: 「同じ設定のまま切り替えて 3 枚とも成立するか」という文脈の目的が、
  **骨格・主役の 5 軸について果たせない**。骨格は「変えると全画面を作り直すもの」と
  自称している（113 行）のに、全画面で確かめられない。
- 直し方の案: `screenSearch` / `screenHealth` も `rowHTML` を通す
  （`items()` を画面ごとに差し替える形にして、行の描画は 1 本にまとめる）。

#### UIR-21 — 狭い幅での破綻が `overflow:hidden` で切り取られて見えない 【重大度: 中】
- 観点: 3, 5
- 根拠: 51 行 `#frame{border:1px solid #2a2a32;overflow:hidden; …}`。
  `.rw-tbl` の列は `calc(var(--body)*3.4) 1fr calc(var(--body)*4) calc(var(--body)*2.6)`（65 行）で
  幅に依存しない固定 px。本文 20px・密度 100（`body=22.8px`）だと
  固定列 228px ＋ gap 45px ＋ padding 21px ＝ **296px**。幅 360px では名前欄が **64px** しか残らず、
  そこに主役「場所の名前」＝ `--title` = 34.2px の文字が入る。
  grid の `1fr` は min-content 未満に縮まないので行は枠外へ出るが、**その分は切り取られる**。
- 再現: 幅 360 / 並べ方「表」/ 本文 20px / 密度 100 / 主役「場所の名前」。
  「スーパーやまと」の右側が枠の外で消える（スクロールバーも出ない）。
- 何が起きるか: 幅の文脈は「はみ出しを見つけるため」に置いたのに、
  はみ出した瞬間に見えなくなる。壊れていないように見える。
- 直し方の案: `#frame{overflow:hidden}` を `overflow-x:auto` にする
  （横スクロールバーが出れば破綻が可視化される）。

#### UIR-22 — 幅の初期値 430 が step 20 のグリッドに乗っていない 【重大度: 低】
- 観点: 3
- 根拠: 153 行 `<input type="range" id="width" min="360" max="1440" step="20">`。
  `ctx.width=430`（217 行）を `sync()` が `.value` に代入する（381〜382 行）が、
  range の値サニタイズで 420 か 440 に丸められる。表示（`v-width`）と枠の幅は 430px のまま。
- 何が起きるか: つまみの位置と表示値が最初からずれる。実害は小さい。
- 直し方の案: 初期値を 420 にするか `step="10"` にする。

---

#### UIR-23 — 同じ「種類」が画面ごとに別の色になる（アクセント本数が面の段数に紐づいている） 【重大度: 高】
- 観点: 4
- 根拠: 色相の割り当ては `accHue(h,n,k)`（240 行）だが、呼び出しごとに `n` が違う。
  | 呼び出し元 | `n` |
  |---|---|
  | `rowHTML` の行アクセント（250 行） | `Math.max(state.surfaces,3)` ← **面の段数に依存** |
  | `rowHTML` の気分チップ（251 行） | 3 |
  | 検索結果（294 行） | 4 |
  | 地図の pin（282 行） | 4 |
  | カレンダー（275 行） | 3 |
  | 稼働状況（305 行） | 3 |
  実測（色相 28°）: k=3（みなと公園・みどり湯）は **n=3 で 338°、n=4 で 88°**。
  つまり同じ種類が主表現では赤紫、検索結果では緑になる。
  さらに面の段数を 3 → 4 に動かすだけで主表現側の色が 338° → 88° に飛ぶ。
- 再現: 主表現で「みなと公園」の縦棒の色を見てから、画面を「検索結果」に切り替えて
  同じ「みなと公園」の縦棒を見る。別の色。次に面の段数を 4 にすると主表現側の色が変わる。
- 何が起きるか: 「種類を色で分ける」という決定が画面をまたいで成立しない。
  かつ **色の意味が「面の段数」という無関係な軸に支配される**（軸の独立性の破れ）。
- 直し方の案: アクセントの本数を独立した定数（または軸）にして全画面で同じ `n` を使う。

#### UIR-24 — 種類の数（5〜6）よりアクセント色の本数（3〜4）が少なく、黙って衝突する 【重大度: 中】
- 観点: 4, 5
- 根拠: `STAYS` の `k` は 0〜4 の 5 種（自宅 / 飲食 / 図書館 / 屋外 / 買い物）。
  `accHue` は `k%n` で折り返すので、n=3 では **k0 と k3 が同じ 338°、k1 と k4 が同じ 28°**。
  n=4 でも k0 と k4 が同じ 328°。さらに n=3 の k1 は 28° ＝ **地と文字の基準色相と同一**。
  `screenHealth` は 6 ソース（`SOURCES`）を `accHue(p.h,3,i)`（305 行）に通すので
  位置 / ウィンドウ、アプリ利用 / ブラウザ履歴、写真 / 健康 がそれぞれ同色になる。
- 再現: 稼働状況を開く。6 行の色は 3 色の繰り返し。
- 何が起きるか: 「何を色で分けるか」を決める軸が、実際には分けられていない。
  UIR-30（色だけの区別）と重なると、種類の識別手段が実質ゼロになる。
- 直し方の案: 種類の数を先に決めて（滞在の種類は要件のどこから来るのかを含めて）、
  本数がそれを下回るときは色以外の手段に落ちる、と画面で明示する。

#### UIR-25 — 行間の下限 1.30、preset「見渡す」の 1.38 が日本語本文で 1.5 を割る 【重大度: 中】
- 観点: 4, 5
- 根拠: 144 行 `<input type="range" id="lh" min="130" max="210">` → 下限 1.30。
  `PRESETS["見渡す"].lh = 138`（210 行）。本文は仮名漢字混じり
  （「カフェ・ムジカ」「中央図書館」など `STAYS`、165 行〜）で、`.pv{line-height:var(--lh)}`（59 行）。
  加えて preset「見渡す」は `base:13` × `density:12` = **実寸 11.8px**、`cap` は 11.2px。
- 再現: preset「見渡す」を押す。11.8px の日本語が行間 1.38 で並ぶ。
- 何が起きるか: 欧文前提の行間で日本語が詰まる。しかも
  これは「性格」として提示された選択肢なので、そのまま選ばれうる。
- 直し方の案: 下限を 1.45〜1.50 に上げる。1.30 を残すなら
  「日本語本文では 1.5 を推奨」と `.n` に書き、preset の値を 1.5 以上にする。

#### UIR-26 — 既定のまま prompt を出すと色の指示が 1 行も出ない 【重大度: 中】
- 観点: 4
- 根拠: 335 行 `const q=(k,txt)=>{if(state[k]!==DEFAULTS[k])d.push(txt);};` で
  **既定と同じ値は出力しない**。色相 28°・彩度 18%・地の深さ 14・面 2 段・角 16px・
  本文 15px・行間 1.65 はすべて既定なので、何も触らなければ
  `表面 — 既定のまま。`（352 行）の 1 文だけになる。
  「既定」が何かは prompt のどこにも書かれていない。
- 再現: 骨格だけ選んでコピーする。表面について何も伝わらない文面が出る。
- 何が起きるか: この prompt は doc の「決めた方向」にそのまま貼る設計（doc 102 行）。
  **色を決めた記録が正典に残らない。** 次の工程（`/production-prep`）は色相を知る術がない。
- 直し方の案: 差分表示は画面の `#prompt` の可読性のためと割り切り、
  出力には全軸の値を必ず書く（差分は「既定から変えたもの」として別行に添える）。

#### UIR-27 — prompt の彩度が常に「（低く抑える）」と言う 【重大度: 低】
- 観点: 4
- 根拠: 339 行 `q("sat",\`彩度は ${state.sat}%（低く抑える）\`)` — 固定文。
  彩度 55（スライダ上限＝運用値の上限）でも「低く抑える」と出る。
- 何が起きるか: 上限いっぱいを選んだ状態が「抑えた」と記録される。NFR-21 の判断が濁る。
- 直し方の案: 他の軸（`radius` は 341 行で 3 段階に文言を分けている）と同じく、
  値に応じて文言を分ける。

#### UIR-28 — prompt の「地の深さ」が単位のない内部値のまま出る 【重大度: 低】
- 観点: 4
- 根拠: 340 行 `q("depth",\`地の深さは ${state.depth}\`)`。0〜100 のスライダ生値。
  同じ行の `surfaces` は「面は 3 段」と読める形（340 行）、`density` は
  「間隔 17px」と実寸を併記している（338 行）のに、depth だけ生値。
- 何が起きるか: 受け取った側が「地の深さは 30」を実装値に翻訳できない。
- 直し方の案: `density` と同じく実寸を併記する（「地の深さは 30（ダークで背景 L=10%）」）。

#### UIR-29 — preset「暦から」「見渡す」は、選んだ瞬間に自分の骨格軸が確かめられなくなる 【重大度: 低】
- 観点: 4
- 根拠: UIR-5（表 × 降り方）と UIR-6（カレンダー × 並べ方・主役・しるし・降り方）の帰結。
  `PRESETS["見渡す"]={layout:"table", drill:"push", …}`、
  `PRESETS["暦から"]={main:"calendar", layout:"table", unit:"day", hero:"mood", drill:"push"}`。
- 何が起きるか: preset は「性格を一度に見る」ためのものなのに、
  5 つのうち 2 つは自分が主張する性格の半分を描かない。
- 直し方の案: UIR-5 / UIR-6 を直す。直さないなら preset の側で
  「カレンダーでは並べ方は効かない」と `.n` に出す。

---

#### UIR-30 — 「色だけに頼らない」しるしトグルが、色の四角を 1 つ足すだけ 【重大度: 高】
- 観点: 5
- 根拠: トグルのラベルは「種類にしるしを付ける（色だけに頼らない）」（146 行）。
  実装は 254 行 `const ico=state.icon?\`<span class="ic" style="background:${ac}"></span>\`:""` で、
  `.ic` は 77〜78 行 `{width:calc(var(--body)*1.1);height:同;border-radius:calc(var(--r)*.4)}` の
  **無地の四角**。色は `ac` ＝ すぐ左の `.bar3`（幅 3px の縦棒、76 行）と**同じ色**。
  形もラベルも文字も無いので、モノクロ・色覚特性下では ON と OFF の情報量が同じ。
- 再現: しるしを ON にする。色付き四角が 1 つ増えるだけで、種類は依然として色でしか区別できない。
  さらに `.ic` は `rowHTML` の中だけなので、検索結果・稼働状況では ON にしても何も出ない（UIR-20）。
- 何が起きるか: **「色だけの区別を避ける」という決定を、避けられていない状態で「決めた」ことになる。**
  UIR-24（5 種類 → 3 色に衝突）と重なると、種類の識別手段が実質ゼロ。
- 直し方の案: `.ic` を種類ごとに違う形（●▲■◆＋）か 1 文字のラベル（家・食・本・外・買）にする。
  少なくとも「形が違う」ことがトグルの中身になっていないと、この軸は判断できない。

#### UIR-31 — 桁揃え（tabular-nums）が時刻にしか入っていない 【重大度: 中】
- 観点: 5
- 根拠: `.num{font-variant-numeric:tabular-nums}`（73 行）が付いているのは
  時刻（`<span class="sm num">${it.t}</span>`、260 行 / 295 行）、
  表レイアウトの所要時間（257 行）、稼働状況の百分率（306 行）。
  付いていないのは:
  - **リスト / カードの所要時間** — 262 行 `<div class="${H[2]}">${it.d}</div>`。
    `H[2]` は主役がどれでも `"sm"` で、`num` を含まない。既定の並べ方はリストなので
    「3時間10分 / 1時間05分 / 55分」が既定で桁揃えされない。
  - **検索結果の副題** — 296 行 `<div class="sm">${it.s} ・ ${it.d}</div>`（「滞在 55 分」）。
  - **単位「日」の件数** — `DAYS` の `d:\`滞在 ${…} 件\``（181〜183 行）が上と同じ経路。
  - **単位「生の記録」の主見出し** — 緯度経度（`"35.6821, 139.7620 ±8m"`）が
    `H[0]`＝主役が場所なら `.big`（69 行、`num` 無し）で並ぶ。
- 再現: 既定（リスト・主役=場所）で右下の所要時間の列を見る。桁が揃わない。
  1 行の単位を「生の記録」にすると、揃うべき座標が見出しサイズで不揃いに並ぶ。
- 何が起きるか: 時系列で縦に数値が並ぶ画面（この製品の中心）で、
  最も比較したい量（滞在の長さ・件数・座標）が揃わない。
- 直し方の案: `.sm` `.big` `.mid` ではなく「数値を含む要素」に `num` を付ける規則にする
  （または `.pv{font-variant-numeric:tabular-nums}` を既定にして、必要な所だけ解除する）。

#### UIR-32 — 空・エラー・読み込み中がどこにも無い 【重大度: 中】
- 観点: 5
- 根拠: 3 画面とも常にデータがある前提。`STAYS`（9 件）、`SEARCH`（8 件）、
  `SOURCES`（6 件）はすべて固定配列で、0 件の経路が無い。
  唯一の欠測表現は `SOURCES` の `["健康",0.00]` と
  307 行の `目標 95%（NFR-13）— 健康データは未実装` の 1 行だけ。
  `docs/ui-direction.md` の「決めなかったこと」（130〜137 行）にも
  空・エラー状態は挙がっていない ＝ **意識的に外したのではなく、抜けている。**
- 何が起きるか: この製品は収集が止まりうる（FR-53 の停止、FR-55 の稼働状況、PERM 系の権限拒否）ので、
  「記録が 1 件も無い日」「位置が取れなかった時間帯」は例外ではなく常態。
  方向を決める段で骨格（主表現・1 行の単位）を選んだのに、
  その骨格が 0 件でどう見えるかが分からない。特に単位「日」＋カレンダーは
  空セルの見え方が主表現そのもの。
- 直し方の案: 画面の文脈に「データ: あり / 空 / 取得失敗」の 3 値を足す。
  ここで決めないなら、doc の「決めなかったこと」に理由つきで書いて Story に送る。

#### UIR-33 — 最長・最多ケースが入っていない 【重大度: 中】
- 観点: 5
- 根拠:
  | 項目 | playground の値 | doc / 要件の値 |
  |---|---|---|
  | 1 日の滞在 | `N_STAY_DAY=9`（163 行）固定 | doc 68 行「1 日 5〜15 件」 |
  | 日の行数 | `DAYS` 14 件（181 行） | `N_DAY_MONTH=30` |
  | 生の記録 | `RAW` 12 件（175 行） | `N_RAW_DAY=4120` |
  | 場所名の最長 | 「スーパーやまと」7 文字 | 上限の記述なし |
  | 写真の枚数 | 最大 `ph:3` | NFR-6 の年 2,000 枚＝ 1 日約 5.5 枚（doc 68 行） |
- 何が起きるか: 密度を判断するための件数を doc がわざわざ 1 節割いて出している（doc 64〜74 行）のに、
  playground はその**中央値**しか描かない。「最多の日」で 1 画面に収まるかは確かめられない。
  長い場所名（「東京都立中央図書館 多摩分館」のような実在しうる名前）での折り返しも見えない。
- 直し方の案: `STAYS` に 15 件版を持ち、文脈に「件数: 標準 / 最多」を足す。
  場所名に 1 件だけ 20 文字級を混ぜる。写真は 6 枚のケースを 1 行入れる。

#### UIR-34 — 二次テキストが 7px まで縮む（要件が下限を持たない軸なので、方向として決める必要がある） 【重大度: 中】
- 観点: 5, 2
- 根拠: 243 行 `typo(){… const b=state.base*dm; return{body:b, cap:b/r, …}}`。
  **比（情報階層）は見出しを大きくすると同時に脚注を小さくする**（`cap = body/ratio`）。
  本文 12px・密度 0・比 1.50 で `body=10.56px`、**`cap=7.04px`**。
  `--cap` を使うのは `.hd`（60 行）`.sm`（72 行）`.mchip`（74 行）`.more`（81 行）
  `.danger`（82〜83 行）`.cal div`（86 行）`.expand`（92 行）で、
  **時刻・所要時間・種類名・件数・削除ボタンの文字が全部 7px になる。**
- 再現: 本文 12px・密度 0・情報階層 1.50。
- 何が起きるか: 要件は本文の下限を意図的に持たない（doc 97〜98 行が「AI の仮置きだったので戻さなかった」）。
  つまり **ここで下限を決めないと、下限が無いまま実装に降りる。**
  NFR-18 は満たす（`--dim` は 4.5:1 で探索される）が、可読性の下限は誰も持っていない。
- 直し方の案: この工程の運用値として `cap` の下限（例 11px）を置き、
  NFR パネルの運用値欄に「要件ではない」と明記して出す（彩度 55% と同じ扱い）。
  または比を `cap` に効かせず、見出し側だけを動かす。

#### UIR-35 — フォーカスの可視がソースにも doc にも一度も出てこない 【重大度: 低】
- 観点: 5
- 根拠: ファイル全体に `:focus` / `:focus-visible` / `outline` の記述が 0 件
  （`grep -n "outline\|focus"` → 該当なし）。
  `docs/ui-direction.md` の「決めなかったこと」（130〜137 行）にも無い。
  `outline:none` を書いていないのでブラウザ既定のリングは残る（＝壊してはいない）が、
  **方向として何も決めていない。**
- 何が起きるか: 本人専用ツールとはいえ、キーボードで一覧を辿るのは主要な操作になりうる。
  行が「選択されている」状態の見え方は主表現の一部で、Story ごとに決めると画面ごとにばらつく。
- 直し方の案: 「フォーカスは決めない（Story 側）」と doc に理由つきで書くか、
  行の選択・フォーカスの見え方を軸に 1 つ足す。

#### UIR-36 — playground 自身の chrome が、それが掲げている下限を割っている 【重大度: 低】
- 観点: 5
- 根拠（製品ではなくツール側なので参考値。ただし選択の可視は判断の質に直結する）:
  | 箇所 | 実測 |
  |---|---|
  | `.tg input{width:34px;height:19px}`（44 行） | **19px** < 24px（NFR-19 の数値） |
  | `.n`（軸の説明文）`#71717b` on `#121216`、10.2px（26 行） | **3.87:1** |
  | `.gh` / `details>summary` `#6d6d78` on `#121216`、10.4px（18, 19 行） | **3.66:1** |
  | `.cards button i`（選択肢の補足）`#7c7c87` on `#1d1d25`、9.8px（36 行） | **4.06:1** |
  | 選択中 `#2f3f5c` と未選択 `#1d1d25` の地の差（37〜38 行） | **1.58:1**、区別は色のみ |
- 何が起きるか: 「どれを選んだか」を示すのが選択中の背景色（1.58:1）と枠線色だけで、
  骨格 4 群 × 選択肢を見比べる作業がやりにくい。軸の説明（`.n`）が 3.87:1 の 10.2px なので、
  「何を選んでいるのか」の説明が読みにくい。
- 直し方の案: `aria-pressed=true` に色以外のしるし（左の縦棒・チェック）を足し、
  `.n` と `.gh` の文字色を上げる。

#### UIR-37 — サンプルデータの日付・曜日が実在の暦と合わない 【重大度: 低】
- 観点: 5
- 根拠: `screenMain` の見出しは `2026年9月7日 日曜日`（289 行）だが、
  2026-09-07 は**月曜**。`DAYS` の日付は 181 行
  `t:\`9/${(7-i+30)%30||30}\`` で i=7 のとき 0 → `||30` により **9/30**。
  降順に 9/1 の次が 9/30 として並ぶ（本来は 8/31）。
- 何が起きるか: 単位「日」を選んだときに並ぶ日付列が暦として破綻している。
  桁揃え・折り返しの判断材料としては使えるが、日付の見せ方（月をまたぐ表示）を
  この画面で判断すると誤る。
- 直し方の案: `Date` から生成する。

---

## 見ていないもの（第 1〜3 回に共通）

- **レンダリング結果そのもの。** HTML を描画できないため、色や余白の「印象」は判断していない。
  本レビューの数値はすべて playground 内の `pal()` `typo()` `accHue()` `cr()` を
  Node で再実行して得たもので、CSS のカスケード・実際のフォントメトリクス
  （日本語フォントの実効行高、`min-content` の実測幅）は反映していない。
  UIR-21 の「切り取られる」は grid の仕様からの推論で、実測ではない。
- **クリップボード API・`details` の開閉など、ブラウザ挙動に依存する部分。**
- **`docs/stories/ST*.md` との突き合わせ**（画面一覧の網羅性）。今回の観点 1〜5 の外。
- **文章の質・誤字・doc の構成。** 指示により対象外。
- **第 2 回で追加した検証の限界**: `pal()` / `typo()` / `accHue()` / `SHAPE` を Node で再実装して
  12,672 通りを計算したが、`getBoundingClientRect()` に依存する `#fit` の判定、
  `border-radius` の形が小さい `.ic`（本文の 1.1 倍 ＝ 11〜25px）で実際に見分けられるか、
  `.err` の枠（`--dim` 1px）の見え方は、描画しないと確かめられない。
- **第 3 回で確かめられなかったもの**: `column-count:2` の実際の分割位置（`.rw-tbl` が
  段の切れ目で割れるかは multicol の断片化規則からの推論で、実測ではない）、
  `border-radius:undefined` がブラウザで無効宣言として捨てられること（CSS の構文規則からの推論）、
  `.ic` の 5 種類の形が本文の 1.1 倍（11〜25px）で見分けられるか、
  `opacity:.35` の骨組みの見え方。

