# ST23 への申し送り

ST23 は、この申し送りを書いた時点（2026-09-14）では上流が始まっていない（`tasks.md` が無い）。
**差し戻しではない** —— ST16 が作った滞在（派生）の側の事実を、ST23 の上流が深掘りの前に読めるように置いておく。

## st16-stay-derivation R38 — 位置の本文を消去するとき、滞在と滞在の前の版にも座標が残っている

- 何を: 位置の記録（`c01-location`）の本文を消去する（FR-51）とき、**その時間と重なる滞在（`logical_source = 's01-stay'`、`origin = 'derived'`）の行と、
  その滞在の `core.event_version` の前の版も消去の対象に含めるか**を、ST23 の深掘りで決める。
  滞在の `raw` / `payload` は重心の緯度経度（小数 6 桁）を持ち、作り直しで吸収された滞在も行を残す（design D3）。
  いま居る滞在は作り直しのたびに前の版を積む（design Risks: 1 日 288 回）ので、座標を持つ前の版は多い（code-verify の実測: テスト 1 周で 1,443 行）
- なぜ今入れないか: 消去の操作は ST23 まで存在しない。ST16 が消去の範囲を先に決めると、ST23 の判断を先取りする
- 手がかり: 消去の後に `stay_store::rebuild_day` を呼ぶと、消去した位置（`payload = '{}'`）は判定に使われず、その位置から作った滞在は吸収される。
  **ただし吸収は行を消さず、前の版も残る**ので、作り直しだけでは座標は消えない。消去には台帳の行が要る（ST03 の門）
- 根拠: openspec/changes/st16-stay-derivation/review/code.md R38

## st19-personal-attributes Q1 — 個人属性の主張の本文も消去できる。消去の後に値を当てられない形で入っている

- 何を: 個人属性の主張（`logical_source = 's01-attribute'`）の本文の消去（FR-51）は、**ST03 と同じ消去の形**（`raw = ''` / `payload = '{}'` / `event_time` と `content_hash` は不変）かつ
  **同じまとまりに、その主張の `core.erasure_ledger` の行（`event_id = NEW.id` かつ `scope = 'event'`）**があるときだけ通る（ST19 の `core.require_claim_erasure_ledger()`。design D2）。
  台帳の行があっても消去の形でない書き換えは拒む。主張には前の版（`core.event_version`）は無い
- 主張の原文には**他の列へ写さない乱数（`nonce`）**が入っている（design D4）ので、消去の後に残る `id` / `event_time` / `content_hash` と値の候補から鍵を作り直せない。
  **位置など他のソースの記録には同じ守りが無い**（鍵は SHA-256(ソース, 出来事の時刻, 原文) で塩が無い）—— ST23 の深掘りで、消去で鍵も消すか・他のソースにも同じ守りを掛けるかを問うなら、ここが手がかり
- **ST19 の読み出し（`GET /attributes`）は、本文を消去した主張（削除の印が無く `raw = ''`）を出さず、その主張による取り消しも効かせない**（design D6 の手順 1。spec「本文を消去した主張は出ず、その取り消しも効かない」）。
  消した主張が取り消していた主張は積んだ主張に戻る（削除の印と同じ。`docs/handoff/ST22.md` の ST19 の項）
- 根拠: openspec/changes/st19-personal-attributes/deep.md Q1 / C12、review/deep.md R3、review/spec.md R7 / R18、design.md D2 / D4 / D6

## st22-record-deletion R1 — 個人属性の主張を消す操作は ST23 が S-6 に足す

- 何を: 個人属性の主張（`logical_source = 's01-attribute'`、`origin = 'authored'`）を**消したことにする**操作（FR-50）を、
  マスタ管理の画面（S-6）と口に足す。ST19 は錠を `deleted_at` / `deleted_by` / `sensitivity` の書き換えだけ通す形で**開けて待っている**
  （`docs/handoff/ST22.md` の st19 Q1。FR-44 ★ 2026-09-15 は**本人の決定**）。
  読み出しの側は ST19 が済ませている（消した主張は出さず、その主張による取り消しも効かせない）
- なぜ ST22 で作らないか: ST22 の消す口は**滞在だけ**に狭めた（深掘り C3。本人に C の一覧として見せ、異論は無かった）。
  消した後に要る処理がソースごとに違い（滞在は R14 / R44、位置は R37）、広い口は副作用を今ソースごとに決めてしまう。
  `docs/stories/INDEX.md`:162 は「消す操作は ST22」と書いていたが、**ST22 の訂正（2026-09-19）で ST23 へ移した**
- 気をつけること: ST23 は同じ主張について**本文の消去**（FR-51。ST19 が `core.require_claim_erasure_ledger` で待っている）も作る。
  論理削除（ST22 の `core.deletion_ledger`）と消去（ST03 の `core.erasure_ledger`）は**別の台帳**で、
  主張の消去は「先に消したことにしてから消去する」のか「直接消去してよいのか」を ST23 の深掘りで本人に問う
- 根拠: openspec/changes/st22-record-deletion/review/spec.md R1 / deep.md C3 / docs/stories/INDEX.md の ST22 の訂正

## st22-record-deletion R20 — `core.event` への直の INSERT を DB で締めるか（ST19 からの持ち越し）

- 何を: `docs/handoff/ST22.md` の st19 R20（「ST22 / ST23 は錠を触る Story なので、そのとき `record-envelope` 全体の話として
  DB の側で INSERT まで締めるかを決められる」）。**ST22 は決めなかった** —— ST22 は削除の 2 列にしか触らず（もともと素通し）、
  錠そのものを動かしていないため
- 気をつけること: 締めるなら、検査用の抜け道（ロールか設定）を同時に決めないと `tools/check-immutable.sh` の台本が書けなくなる
- 根拠: openspec/changes/st22-record-deletion/design.md の Non-Goals / docs/handoff/ST22.md の st19 R20

## st22-record-deletion R4 — ブラウザで消えた訪問を ashiato でも消したことにするか（ST08 からの持ち越し）

- 何を: `docs/handoff/ST22.md` の st08 R4。ST08 は「消えた事実を記録として残す（**ashiato からは消さない**）」と本人が決めた（ST08 の深掘り Q2）。
  その記録を使って ashiato 側でも削除の印を付けるかは、**まだ誰も決めていない**
- なぜ ST22 で決めないか: ST08 の下流が未着手で、「消えた」の記録が 1 件も無い。ST22 は自動で消す経路を一切作っていない
  （**扉を開けたままにする既定** —— 消さない側はいつでも消せる）。ST22 の口は滞在だけを受ける（C3）ので、この経路は物理的に存在しない
- 気をつけること: 「消えた」の経路は本人の削除だけではない（90 日の期限切れ・同期を切ったときの一括削除・表の作り直し）。
  **同期を切っただけでスマホの訪問 90 日ぶんが「消した」に化けない**ように、判定を作る Story が経路を読み分ける
- 根拠: openspec/changes/st08-browser-history/deep.md Q2 / docs/handoff/ST22.md の st08 R4 / openspec/changes/st22-record-deletion/design.md の Non-Goals
