---
block_a: done
block_b: done
block_c: done
---

# 製造準備 — あしあと。

`/production-prep`。Story 36 本が出揃い（`docs/stories/INDEX.md`）、UI の方向が決まった
（`docs/ui-direction.md`）後、**1 本目の深掘りに入る前**に 1 度だけ行う。

判定基準は 1 行 —— **全 Story に共通で効き、後から入れると既に書いたコードを全部直すことになるもの。**
片方しか満たさないものは Story 側へ送っている。

---

## A 決める（契約）

### A-1. 実装の土台

| 項目 | 決定 | 記録先 | 可逆性 |
|---|---|---|---|
| 実装言語（S-01 サーバ） | **Rust**（本人の指定） | 本表 | 低 — 全実装が乗る |
| 実装言語（C-01 Android 収集） | **Kotlin** | 本表 | 低 — Health Connect / アプリ利用 / 写真の位置はいずれも native API（FR-11 / FR-2 / FR-3）で、これらを plugin 越しにすると 3 つとも他人の実装に依存する |
| 実装言語（C-02 Windows 収集） | **Rust**（本人の指定） | 本表 | 低 |
| 実装言語（V-01 画面） | **TypeScript + React（ブラウザ）** | 本表 | 中 |
| 画面をブラウザにする理由 | V-01 は PC とスマホの両方で見る（本人「pc, スマホ、バックエンド（pc）」）うえ、外からは Tailscale 網内で届く（PERM-7）。**1 実装で両方に載るのはブラウザだけ**。ネイティブにすると収集アプリと別にもう 1 本 Android アプリを抱える | 本表 | — |
| API 層の方式（読み） | **PostgREST でスキーマから自動生成**。テーブルを足せば読み取り口が出る | 本表 / EXT-J | 中 |
| API 層の方式（書き） | **自前の取り込み口 1 本**（`logical_source` + `external_id` + `payload` + `raw`）。冪等の判定（FR-22）・原文の保存（FR-18）・稼働記録の更新（FR-33）を必ずここに通す | 本表 | 低 |
| 読みと書きを分ける理由 | FR-61（登録簿に 1 行足すだけで API を変えずにソースを増やす）は**読み側だけ仕組みで守れる**。書きは不変条件を 1 か所に集める必要があり、DB のトリガに押し込むと見えなくなる | 本表 | — |
| **収集側の送信契約** | **Kotlin と Rust の 2 実装に分かれるので、契約を 1 つの文書に固定する。** 冪等キーの作り方・原文の詰め方・バッファから捨てるときの記録の残し方（FR-9）を含む。**両方が同じ 1 件を送ったら同じ結果になること**を B の縦串で確かめる | 本表 + `docs/collector-contract.md`（B で作る） | 低 |
| **本体のライセンス** | **AGPL-3.0 で公開する**（2026-09-08、本人が決定）。改造版を隠したまま配られるのを防ぐ。プラグインが別プロセス（FR-77）なので追加コストがゼロ | 要件 §5 | **不可逆** — 公開後は貢献者全員の同意が要る |
| **CLA（貢献の受け入れ条件）** | **許諾型の CLA を用意し、これ無しに貢献を受け入れない。** 貢献者は著作権を持ったまま、こちらに別条件でのライセンス許諾を与える形 | `CONTRIBUTING.md` + CLA 雛形（B で作る） | **不可逆** — 1 人でも CLA 無しに受け入れると、その部分はデュアルライセンスできなくなる |
| **プラグイン / 衛星の境界** | **別プロセスで HTTP API 越しのみ**（FR-77）。同一プロセスに読み込むと AGPL-3.0 の派生物になり、有料で配れなくなる | FR-77 / ST29 | **不可逆** — 後から締めると既存プラグインが全部作り直し |
| 契約仕様のフォーマット | **OpenAPI 3.1 をコードから生成してコミットし、CI が差分を検出する。手書きしない** | CI | 中 |
| フロントの構成 | 決めた主表現（**時系列 × リスト × 滞在 × その場で開く**）を実装する。ビルドは Vite。状態は最小限に保ち、SSR は持たない | `docs/ui-direction.md` | 中 |

### A-2. 動かす前に決めておくもの

| 項目 | 決定 | 記録先 | 可逆性 |
|---|---|---|---|
| secrets / 設定 | リポジトリに入れない。`.env` はローカルのみで、**雛形（`.env.example`）だけコミット**。CI は GitHub の secrets を使う | `.gitignore` / `.env.example` | 高 |
| DB マイグレーション | **前進のみ。各版に戻し手順を必ず書く。適用の直前に自動でバックアップを取る。** 破壊的変更は 3 段階に割る（列を足す → 移す → 落とすを別の版に）。**名前は作成時刻 `YYYYMMDDHHMM_<slug>.sql`、連番にしない**（★ 2026-09-12 追加。並走する Story が番号を取り合う —— ST02 が 0007 を取り、ST03 が 0008 へずらす PR を出した。適用の順は `crates/server/src/lib.rs` の `MIGRATIONS` 配列が持つので、名前は衝突しないことだけが要る。`tools/check-migrations.sh` が形を見る） | `migrations/` | 低 — 個人のデータは復旧できない |
| 依存更新 | **月 1 回まとめて上げ、CI が緑ならそのまま通す。** セキュリティ勧告は都度 | 本表 | 高 |
| 依存ライセンス | **AGPL-3.0 と両立しない許諾の依存を CI で落とす。** 本体を公開する = 配布が発生するので、ライセンスが実際に効く。Rust の crate は 1 本で数百の依存を引くため、**後から洗い直すのは現実的でない** | C ブロック | 低 |
| バージョンの単一情報源 | **build 設定から機械取得する。手書きの定数を作らない** | 本表 | 高 |
| i18n / l10n | **日本語のみ。ただし文言をコードから分離し、後から言語を足せる形にする** | 本表 | 中 |
| i18n の根拠 | 本人の逐語「日本語で、ただ将来言語設定で分けられるようにできたりしますか?」 | `reference/discussions/20260826_データ追加のタイミングと検討方法.md` | — |
| ログ | **私的データを出さない側が既定。** 位置・本文・写真のパス・主観の記述・人物名はログに出さない。出すのは件数・ソース名・所要時間・エラーの種別だけ | 本表 | 低 — 一度出たログは消せない |
| 時刻とタイムゾーン | 保存は `timestamptz`。**出来事の時刻と取り込み時刻の 2 本**（FR-19）に加え、UTC からのずれと tz 識別子の両方を持つ（FR-20）。**日の境界は記録時点の `tz_id` で切る** | FR-19 / FR-20 / 扉 #6 | 低 |

> **移行の改名（2026-09-12）。** 連番から作成時刻へ改名した。2026-09-12 より前の文書（ST01 の archive、
> ST02 の deep / tasks / review など）にある「移行 0005」のような**番号だけの言及**は、次の表で読み替える。
> ファイル名で言及している箇所は改名と同時に書き換え済み。
>
> | 旧 | 新 |
> |---|---|
> | 0001_envelope | 202609081618_envelope |
> | 0002_immutable_collected | 202609082001_immutable_collected |
> | 0003_raw_text | 202609092315_raw_text |
> | 0004_immutable_origin | 202609100000_immutable_origin |
> | 0005_coverage_rebuild | 202609111111_coverage_rebuild |
> | 0006_immutable_heartbeat | 202609111112_immutable_heartbeat |
> | 0007_source_lifecycle | 202609112113_source_lifecycle |

### A-3. 実データを入れる前でないと手遅れなもの

| 項目 | 決定 | 記録先 | 可逆性 |
|---|---|---|---|
| バックアップと復旧の初期形 | **取れることではなく戻せることを 1 回確かめる。** B の縦串に「バックアップ → 別の場所へ復元 → 同じ 1 件が読める」を含める | B の縦串 | 低 |
| 鍵・資格情報の保管 | **Windows の資格情報ストアに置き、復旧用の写しを紙で 1 部物理保管する**（扉 #24、2026-09-08 に本人が決定）。鍵管理サービスを使わないのは NFR-9（運用コスト 月 100 円以内）のため。**紙の写しが本体** | 要件 §5 扉 #24 | 低 |
| 破壊的操作のガード | 物理削除（FR-51）と収集停止（FR-53）は**対象件数を提示して確認を求める**（FR-52）。**CLI からも同じ関門を通す** —— 画面にだけ置くと、開発中に素通りする経路ができる | FR-51 / FR-52 / NFR-20 | 低 |
| 削除・除外の効かせ方 | **論理削除はビュー越しでしか読めない形にし、素のテーブルを直接引かせない。** 後から入れると全クエリを書き直すことになる | FR-50 | 低 |

### A-4. 開発を回すために要るもの

| 項目 | 決定 | 記録先 | 可逆性 |
|---|---|---|---|
| 開発用データ | **偽データ生成器を 1 本作り、全 Story がそれを使う。** 通常（滞在 9 件）・要件上限（15 件）・空・取得エラーの 4 状態を再現できること。Story ごとに fixture を作らせない | B で作る | 高 |
| 再現手順 | `README` に 0 から動かす手順を書き、**実際に踏んで確かめる**。踏んでいない手順は書いたことにしない | B の判定 | 高 |
| 貢献の受け入れ | `CONTRIBUTING.md` と CLA の雛形を**公開の初日に置く**。貢献者が 0 人のいまなら 10 分で終わる | B で作る | **不可逆** |

### A-5. 骨に残っている未決のうち、期限が「製造開始前」のもの

**扉の正典は要件 §5 に一本化している。** 本フローは骨（skeleton）の工程を持たず、
`reference/skeleton/doors.md` は旧プロジェクトの読み取り専用資料なので拾わない
（旧プロジェクトは要件 20 項 / 骨 24 項で食い違っていた）。

#### 閉じた扉

- **#24 バックアップ暗号鍵の保管方式** — 期限は要件上「初回バックアップの実行前」（ST31 / layer 5）
  だったが、A-3 が「失えば全データが消えるものは入れ始める前に決める」と要求しているので**前倒しで閉じた**。
  決定内容は A-3 の表のとおり。

#### 期限を引き直した扉（理由つき）

- **#25 画面キャプチャの保持期間と文字抽出エンジン** — 期限は「画面キャプチャの収集を開始する前」の
  まま**据え置く**。画面キャプチャは Could（FR-74 / ST35 / layer 4）で、製造準備の時点では
  実装対象ですらない。**いま決めると、実測なしの値が正典になる**。
  ただし版を記録する列（FR-32）は day one で持つので、後から選り分ける手段は失わない。

#### 期限が「製造開始前」の扉

**該当なし。** 未決の扉は #24 と #25 の 2 つだけで、#24 は前倒しで閉じ、#25 は期限が後ろにある。

### やらないと決めたもの（記録）

- **i18n の実装** —— 分離はするが、日本語以外の文言は持たない
- **SSR / メタフレームワーク** —— 本体の画面は 4 枚で、検索エンジンにも出さない
- **サーバ側のキャッシュ層** —— 「共通で効くが後から入れられる」ので Story 側へ送る
- **課金・販路・複数利用者・サポート体制** —— 商用化はするが、これらは後から足せる（要件 §5）
- **行レベルセキュリティ（RLS）** —— 商用化の形が「本体を無料公開し、衛星とプラグインで稼ぐ」に
  決まったため、利用者は各自の PC で 1 人のまま。**守る対象が無いので入れない。**
  `user_id` を全テーブルに持つ決定（FR-29 / 扉 #9）は残るので、必要になったときに効かせられる

> ★ 2026-09-08: 従来ここには「商用化に向けたライセンス適合の点検 —— 要件 §5 で範囲外」と
> 書いていたが、本人が商用化すると決めたため**撤回**した。点検は C ブロックの機械検査に入る。

---

## B 作る（工具）

**判定は「設定した」ではなく「走らせた」。** 下記はすべて 2026-09-08 に実際に実行した記録。

| 項目 | 実行したコマンド | 終了コード |
|---|---|---|
| 処理系が動く（Rust） | `cargo build --workspace` | **0** |
| 処理系が動く（Node / TypeScript） | `npm install` → `npx tsc -b` | **0 / 0** |
| 処理系が動く（PostgreSQL） | `docker compose up -d --wait db` → `Container ashiato2-db-1 Healthy` | **0** |
| 処理系が動く（Android / Kotlin） | `. ./tools/android-env.sh` → `cd collector-android && ./gradlew :app:assembleDebug` | **0**（2026-09-08） |
| workspace の構成 | Cargo workspace（`crates/server`・`crates/collector-windows`）＋ `web/`＋`migrations/` | — |
| build（Rust） | `cargo build --workspace` | **0** |
| test（Rust） | `cargo test --workspace` → 2 passed | **0** |
| lint（Rust） | `cargo clippy --workspace --all-targets -- -D warnings` | **0** |
| 整形（Rust） | `cargo fmt --all --check` | **0**（初回は 1 で落ち、整形して再検査） |
| build（Web） | `npm run build` → `dist/` 生成 | **0** |
| 型検査（Web） | `npx tsc -b` | **0** |
| lint（Web） | `npm run lint`（`--max-warnings 0`） | **0** |
| CI の骨格 | `.github/workflows/ci.yml`（rust / web / smoke の 3 job）。**ローカルで同じコマンドが 0 で通ることを確認済み。GitHub 上での実行は push 後** | — |
| 起動スクリプト | `./tools/dev.sh` → API が `ok` を返し、画面が HTTP 200 | **0** |
| **最短の縦串** | `./tools/smoke.sh`（9 手順） | **0** |
| 開発用データ（A-4 の決定） | `./tools/seed.sh normal` → 9 件 / `./tools/seed.sh max` → 15 件 | **0 / 0** |
| API の契約をコードから生成（A-1 の決定） | `cargo run -p ashiato-server --bin openapi > docs/openapi.json` → `./tools/check-openapi.sh` | **0 / 0** |
| **再現手順を実際に踏む**（A-4 の決定） | 別の場所へ `git clone --no-hardlinks` → `cp .env.example .env` → `./tools/smoke.sh` | **0** |
| **画面の e2e の器**（2026-09-18 に追加） | `cd web && npm run test:e2e`（`tools/stack.sh up` が DB → サーバ → 偽データ → 画面を立て、本物の Chromium が S-1 を開く）→ `1 passed` | **0** |
| 縦串の起動を 1 つにする（同上） | `tools/stack.sh` を切り出し、確認バッチの `dist/verify-<tag>/run.sh` と playwright の `webServer` が**同じものを呼ぶ** | — |

### 画面の e2e（2026-09-18 に足した）

**器だけを作り、テストは Story ごとの change が足す。** 1 本のダミー
（`web/e2e/stack.spec.ts`）が「縦串が立って S-1 が本物のブラウザで開く」まで通る。

- **起動を分けない。** `tools/stack.sh up` を人間（`run.sh`）と e2e（playwright の `webServer`）の
  両方が呼ぶ。分けると「e2e は緑なのに人間が見る画面は違う」が起きる
- **アサートするのは数値と経路**（`boundingBox` / `scrollWidth` / `activeElement` / 可視 / URL 遷移）。
  **視覚回帰（`toHaveScreenshot`）は入れない** —— 差分の是非を毎回人間が判断することになり、
  機械に移したはずの判断が人間へ戻る
- CI は `STACK_RESET=1` で DB を作り直す。**そこで `seed.sh` が落ちたら止める**
  （実測 2026-09-18: 溜まった DB では ST19 の主張の読み直しが合わず `seed.sh` が rc=1 を返していて、
  `run.sh` はそれを `warn` で流していた。人間も e2e も**何が入っているか分からない画面**を見ることになる）

### 縦串が実際に確かめていること

`tools/smoke.sh` は機能ゼロのまま、**A で決めたもの同士が噛み合うか**を 8 手順で通す。

1. DB を起動 → 2. サーバ起動時にマイグレーションを当てる → 3. **登録簿へ 1 行だけ INSERT**（FR-61）
→ 4. ダミーを 1 件送る → 5. **同じものをもう一度送り、`duplicate: true` で行が増えないことを確認**（FR-22）
→ 6. **論理削除を効かせたビュー越しに 1 件取り出す**（FR-50 / A-3）
→ 7. **登録簿に無いソースが 400 で拒否されることを確認**（FR-61 の関門）
→ 8. **バックアップを取り、別のデータベースへ戻して同じ 1 件が読めることを確認**（A-3「取れることではなく戻せること」）
→ 9. **資格情報の無い要求が 401 で拒否されることを確認**（PERM-10）

### ブロック B に対する自動セキュリティレビューの指摘（2026-09-08）

コミット時の自動レビューが 4 件を指摘した。**2 件は実際のバグだったので直した。**

| 指摘 | 対応 |
|---|---|
| 認証が無い（authentication-bypass） | **直した。** 合言葉（`API_TOKEN`、16 文字以上を起動時に強制）を全経路に要求。**loopback に閉じているだけでは足りない** —— 第三者製プラグインは同じ PC で動くので、素通しだと PERM-8（プラグインの既定を最も厳しい側に置く）が空文になる。縦串の手順 9 が毎回確かめる |
| エラー本文の漏れ（information-disclosure） | **直した。** データベースのエラーをそのまま応答に載せていた（スキーマ名・接続先・値が漏れる）。応答は `internal error` に固定し、詳細はログにだけ出す |
| 呼び出し元ごとの権限が無い（missing-authorization） | **直さない（この段階では）。** 合言葉は「持ち主かどうか」しか判定しない。**感度による出し分け（PERM-2〜6 / FR-60）とプラグインの書き込み範囲の制限（FR-64）は ST24 / ST27 / ST29 の担当**で、骨格の時点で作ると Story の設計を先取りしてしまう。**いまは持ち主だけが呼べる状態**なので穴は開いていない |
| 呼び出し元が `user_id` を自由に名乗れる | **直さない（同上）。** PERM-10 を要件に足したので、呼び出し元ごとの資格情報を決めるのは ST29 の深掘りになる |

**この指摘から要件の抜けが 1 件見つかり、PERM-10 として要件へ戻した。**
PERM-7（外部からの到達を Tailscale 網内に限る）は網の外を止めるだけで、
**同じ PC の中から呼ぶものを何も止めていなかった。**

### C-01（Android / Kotlin）を閉じた経緯（2026-09-08）

当初は JDK も Android SDK も無く `sudo` も使えないため未達だった。
**ビルドは Claude か CI に回す方針**なので、Android Studio（手作業）ではなく
**WSL に揃える**ことにした —— CI（ubuntu ランナー）でも同じヘッドレス設定が要るので作業が重複しない。
「Android Studio を推す」という当初の助言は、この進め方には当てはまらないので撤回した。

- Temurin JDK 21.0.12.1 と Android command-line tools を **`sudo` 無しで `~/.local/opt`** へ。
  環境変数は `tools/android-env.sh`。消すときは `rm -rf ~/.local/opt/{jdk21,android-sdk}` だけ
- Gradle wrapper をリポジトリに同梱したので `gradle` の別途導入は要らない（jar の出所と sha256 を記録）
- **ライセンスへの同意は本人が行った。** 代わりに押していない —— Google の条項に同意する行為なので

**最初のビルドは落ちた。2 件とも骨格側の不備だった。**

1. **`gradle.properties` を置き忘れていた** —— `androidx.*` に依存しているのに
   `android.useAndroidX=true` が無く、`checkDebugAarMetadata` で落ちた
2. `kotlinOptions { jvmTarget }` が Kotlin 2.x で非推奨 —— `kotlin { compilerOptions { … } }` へ移した

修正後 **`BUILD SUCCESSFUL`（35 タスク / rc=0）**。CI にも `android` job を足し、
**ローカルと同じ wrapper を使う**ようにした。

## C 仕掛ける（検査）

**B と同じゲートで判定しない。** B は壊れていれば即分かるが、**C は壊れていても分からない**。
だから「作った」ではなく「**壊したときに落ちるか**」を 1 回ずつ確かめている。

| 項目 | 適用済み（具体化 1 行）/ 該当なし（理由） | 壊して落ちることを確かめたか |
|---|---|---|
| 未捕捉エラーハンドラ ＋ ローカルのエラーログ | `main.rs` に panic hook。既定では生えない自己診断の経路（`ASHIATO_SELFTEST_PANIC=1` のときだけ）を持つ | **確かめた** — `./tools/check-panic-log.sh` がわざと落として `kind="panic"` がログに出ることを確認（rc=0） |
| lint を既定より一段厳格化 | `[workspace.lints]` に `unsafe_code = forbid` / `unwrap_used = deny` / `todo = deny` / `dbg_macro = deny` / `print_stdout = deny`。Web 側は `eqeqeq` / `no-console` / `no-explicit-any` / `explicit-module-boundary-types` を追加 | 既定に追加ルールを足した状態で `cargo clippy -- -D warnings` と `npm run lint --max-warnings 0` が **rc=0** |
| docstring の方針 | **公開する項目には「何を保証するか」を 1 行書く。「何をするか」はコードが語るので書かない** | — |
| プラットフォーム既定値の棚卸し | PostgreSQL を `log_statement=none` / `log_min_duration_statement=-1` / `log_error_verbosity=terse` で起動（**私的データが SQL ごとログに落ちるのを止める**）。**DB の待ち受けを `127.0.0.1` に固定**（既定では全インタフェースに出ていた） | 設定変更後に `./tools/smoke.sh` が **rc=0** |
| アーキ境界の検査 | `./tools/check-boundaries.sh` —— **本体にコードを読み込む経路（FR-77 違反）** / 収集側がサーバ内部に依存 / 画面がサーバ実装を直接見る、の 3 つを機械で見る | **確かめた** — `libloading` を 1 行足したら `NG 本体にコードを読み込む経路がある（FR-77 違反）` で **rc=1**。戻したら rc=0 |
| 依存ライセンスの検査 | `./tools/check-licenses.sh` —— Rust 237 件 / Node 170 件の許諾を SPDX 式で評価し、AGPL-3.0 での配布と両立しないものを落とす | **確かめた** — 許可一覧から MIT を外したら **67 件を検出して rc=1**。通常時は不許可 0 件で rc=0 |
| マイグレーションの安全性検査 | `./tools/check-migrations.sh` —— 前進側の破壊的変更（`DROP TABLE/COLUMN/SCHEMA`）と、戻し手順の欠落を落とす | **確かめた** — `ALTER TABLE ... DROP COLUMN` を入れたら rc=1、戻し手順の無い版を足したら rc=1。どちらも後始末後は rc=0 |
| API の契約とコードのずれ | `./tools/check-openapi.sh` —— `docs/openapi.json` はコードから生成する。**手書きしない**（A-1） | **確かめた** — `info.version` を書き換えたら差分を出して rc=1 |
| CI の費用（2026-09-19 に追加） | `changes` job が差分を見て重い job を出し分ける。**1 PR 約 52 分**（Windows は 2 倍課金）で、月 2,000 分では 38 回しか回せない。docs だけの PR で 9 job 全部を走らせていた | **実測で使い切った**。上流 PR は 52 分 → 0.5 分になる |
| CI の構造衛生 | 版を **SHA で固定**（`actions/checkout@fbc6f39…` ほか。タグは動く）/ `concurrency` で二重トリガを畳む / `permissions: contents: read` に最小化 / **CI が走らせるのはローカルと同じ `./tools/*.sh`** | ローカルで同じコマンドがすべて rc=0 |
| **人間へ落とすものに名前を要求**（2026-09-18 に追加） | `scripts/check_scenarios.py` —— `tasks.md`「人間の確認待ち」の `- Scenario:` に `> 物理: <lock\|battery\|gps\|time\|realdata\|device>` を要求する。名付けられないものは落とす（`review_triage.py` の `loss` と同じ型）。**画面の実寸・フォーカスは `web/e2e` が測る** | **確かめた** — 名前の無い `- Scenario: 履歴が一覧で読める` を st08 の tasks に置いたら `人間の確認待ちの理由が名付けられていない` で **rc=1**。戻したら名無し 0 件 |
| 停止の検知 | **該当なし。** 製造準備の時点で継続的に動き続けるものがまだ無い（収集は ST01 以降）。**仕組みは要件側にある** —— 稼働記録（FR-33）と、想定間隔の 3 倍で通知する条件（FR-35）。実装は ST14 の担当で、そこで「わざと止めて通知が出るか」を確かめる | — |

### C で新しく見つかったもの

- **DB の待ち受けが全インタフェースに出ていた。** `ports: ["55432:5432"]` は既定で `0.0.0.0` に出る。
  Tailscale 網に参加している端末から、認証なしで PostgreSQL に届く状態だった。`127.0.0.1` に固定した
- **PostgreSQL のログ設定が既定のままだった。** 遅いクエリのログが有効になると、
  位置や本文が SQL ごとログに落ちる。明示的に切った（A-2 の「私的データは出さない側が既定」と揃えた）

### 商用化の決定にともなって C に足す項目（2026-09-08）

- **依存ライセンスの機械検査** —— AGPL-3.0 と両立しない許諾が入ったら CI が落ちる
- **マイグレーションの安全性検査** —— 破壊的変更（列や表の削除）が 1 つの版に単独で入ったら落ちる。
  本体を配布すると、自分が見たことのない他人のデータに当たる
- **プラグイン境界の検査** —— 本体のプロセスにプラグインのコードを読み込む経路が生えたら落ちる（FR-77）。
  アーキ境界の検査と同じ仕掛けに乗せる

### C に足した項目（2026-09-18）

**機械が判定できることが、静かに人間の手順へ流れていた。**

実測: 確認バッチ 2 回ぶん（`docs/verify/20260913-2255.md` / `20260916-1442.md`）の **15 問のうち 6 問が
画面の Scenario**。「キーボードで移るとフォーカスの位置が見える」「1 年ぶんが一目で読める」は、
本物のブラウザなら機械が判定できる。**jsdom が測れないことは、機械が測れないことではない。**

- B に **画面の e2e の器**（`web/e2e` + `tools/stack.sh`）
- C に **人間へ落とすものに名前を要求する検査**（`> 物理:`）。名付けられない確認待ちは落ちる
- `docs/testing.md` §1 の表と §4 / §4.5 / §7 を同時に直した ——
  **規範を直さないと実装側は jsdom のままになる**
