# ST03 深掘り 第 2 回の独立レビュー

対象: `openspec/changes/st03-idempotent-ingest/deep-questions-r2.json`（Q8 / Q9）。
`openspec/schemas/ashiato/schema.yaml` の `deep` 手順 1〜5 を、**第 1 回の 7 つの答えを所与として**
独立にやり直し、そのうえで Q8 / Q9 と突き合わせた差分だけを書く。
**問いの JSON は 1 文字も触っていない。** 採番は R14 から（R1〜R13 は第 1 回で使用済み）。

実施日: 2026-09-11。ブランチ `docs/st03-upstream`（clean, `e685f7c`）。

## 実測の方法

`migrations/0001`〜`0004` を使い捨ての `postgres:17-alpine`（別ポート **55440**。ST02 の DB には触っていない）
に当て、**第 1 回の答えを実装したときに撃つことになる SQL** を手で流した。以下「実験 A〜F」はこれ。

| | 流したもの | 結果 |
|---|---|---|
| 実験 A | Q1 の「既存行を書き換える」を `collected` の行に対して（`raw` / `payload` / `content_hash`） | `ERROR: 収集した記録の原文・…・冪等キー・格納の時刻は書き換えられない（FR-30）` |
| 実験 B | 同じ `external_id`（動画 id を想定）で別の記録を 2 件目 | `ERROR: duplicate key … "event_dedup_ext"` |
| 実験 C | Q6+Q2 の索引に作り替え（`(user_id, logical_source, content_hash) WHERE external_id IS NULL` と `(user_id, logical_source, external_id) WHERE external_id IS NOT NULL`）→ 同じ内容・違う識別子 | **2 行入る**（Q6 の狙いどおり） |
| 実験 D | 同上の索引で、`external_id` の無い記録を 3 回 | **1 行のまま**（Story の核は保たれる）。ただし `ON CONFLICT` は **`WHERE` 句ごと書かないと当たらない**（部分索引） |
| 実験 E | `deleted_at` を立てた行に、同じ `external_id` で `ON CONFLICT … DO UPDATE` | **当たる。** `deleted_at` を見ないので、削除済みの行が更新され、1 行足せば復活する |
| 実験 F | `user_id` だけを UPDATE / `user_id` と `content_hash` を UPDATE | 前者は**通る**、後者は**トリガが `RAISE`** |

## 手順ごとの記録

- **手順 1（要件どうしの衝突）**: 第 1 回の答えを入れたうえで FR-22 / FR-23 と
  FR-10 / FR-18 / FR-21 / FR-29 / FR-30 / FR-50 / FR-51 / FR-61 / PERM-2 を突き合わせた。
  **FR-30 の DB 強制を開ける判断（R14）**、**FR-51 と履歴表（R16）**、
  **Q1 と Q5 が同じ到着に逆を指す（R18）** の 3 件が Q8 / Q9 に無い。
- **手順 2（扉の幅）**: 扉 #7 は「原文を残す」としか決めていないが、0002 / 0004 は
  「`collected` の行は UPDATE 自体を拒む」まで踏み込んでいる。Q1 の答えはこの幅の内側で
  解釈が割れる（R14）。扉 #12 は「外部識別子を持つ」としか決めておらず、
  **その識別子が「記録ごと」か「対象ごと」かは決めていない**（R17）。
- **手順 3（新たに立つ一方通行）**: 索引の形（実験 C / D）、本文の複製先（R16）、
  利用者識別子を鍵に混ぜるか索引に足すか（R19）、登録簿の印の既定値（R20）。
  **列の型・鍵の入力**の観点はここで 2 件（R19 / R20）出た。
- **手順 4（日常に影響する選択）**: 断られた記録の再送は第 1 回の R8 で既出。
  新しく出たのは **Q8 選択肢 2 の手作業の量**（R25）のみ。常時通知・電池・容量への新しい影響は無い
  （履歴の容量は Q1 選択肢 1 に書かれている）。
- **手順 5（既存コードが要件を満たしていない箇所）**: FR-22 / FR-23 について `lib.rs:184-233` と
  `ingest.rs:100-116` を読み直したが、**新しい未達は出なかった**（R11〜R13 が既出のまま）。
  1 点だけ記す: `lib.rs:224` は `event_count` に「新しく入った行だけ」を足すので、
  Q1 の更新で入った再取得は 0 件として数えられる。ST02 が `core.coverage` を作り直すので、
  数え方の決めは ST02 側と揃える必要がある（問いではなく tasks）。
- **不要な問い**: **該当なし。** ただし Q9 は実質的に Q4 の再問（R21 / R23）になっており、
  幅は本人が思うより狭い。
- **分類違い**: Q8 の `irreversible` が実態と合わない（R28）。Q9 の `irreversible` は正しい。

---

## R14. Q1 の「既存行を書き換える」は、0004 の凍結を開けないと 1 行も動かない —— 何をどこまで開けるかが問われていない

- 成果物: openspec/changes/st03-idempotent-ingest/deep-questions-r2.json
- 根拠: 実験 A（`UPDATE core.event SET raw=…, content_hash=…` が `RAISE` で落ちる）/ migrations/202609100000_immutable_origin.sql:30-40（`raw` `payload` `event_time` `content_hash` `ingest_time` と `origin` を凍結）/ migrations/202609082001_immutable_collected.sql:4-7（DB 側に置いた理由は「第三者製プラグイン（PERM-8）や psql を直に叩く運用が素通りする」）/ docs/requirements.md:142（FR-30）/ 同 :646-649（扉 #7）
- kind: conflict
- 処置: escalated — Q10。凍結をどの幅で開けるかを 1 問にした（履歴を書いたときだけ許す / 取り込み口だけ許す / やめる）。0002 が塞いだ 3 手の迂回を why に書いた
- 提案: 1 問足す。「収集した記録の書き換えを DB が拒む仕掛け（0002 / 0004）を、どの幅で開けるか」——(a) 取り込み経路だけに許す（`SECURITY DEFINER` の関数など、psql からの直接 UPDATE は拒んだまま）/ (b) 同じトランザクションで履歴行が書かれたときだけ許す / (c) 凍結をやめる。Q1 の `why` は「いまのコードで更新しようとすると例外で落ちます」と**現象**は書いたが、**その例外を何のために置いたか**（3 手の迂回を塞ぐため）と、どこまで開けるかは問うていない。(c) を選ぶと 0004 の前の状態に戻り、原文の不変が再び 3 手で迂回できる。

## R15. 削除済みの行に外部サービスの更新が届いたときが、どの問いにも無い（Q1 × Q3 × Q6 の交点）

- 成果物: openspec/changes/st03-idempotent-ingest/deep-questions-r2.json
- 根拠: 実験 E（`ON CONFLICT (user_id, logical_source, external_id) … DO UPDATE` は `deleted_at` を見ない。削除済みの行に当たり、`deleted_at = NULL` を 1 行足せば復活する）/ docs/stories/ST23.md:35（壊してはいけないもの:「ST03 の重複判定（**消したものが再流入しないこと**）」）/ docs/requirements.md:264-265（FR-50）/ Q3 の答え「戻さない」は **`DO NOTHING` だった頃の振る舞い**に対して選ばれている
- kind: irreversible
- 処置: escalated — Q11。削除済みの行に外部からの更新が届いたときを 1 問にした。Q3 が `DO NOTHING` に対する答えだったことを why に明記した
- 提案: 1 問足す。「本人が消した記録と同じ外部識別子で、内容の違う版が外部サービスから届いたら」——(a) 更新せず受理として返す（削除が勝つ）/ (b) 更新するが削除の印は残す（消えたままだが中身は書き換わり、履歴に版が積まれる）/ (c) 復活させる。Q3 は「同じ内容の再送」だけを扱っており、Q1 で更新経路ができた瞬間に **「戻さない」は自動では成り立たなくなる**。放置すると実装が黙って (b) か (c) を選ぶ。

## R16. Q1 の履歴は本文の複製を 2 か所目に作る —— 物理削除（FR-51 / ST23）と感度が履歴に及ぶかが問われていない

- 成果物: openspec/changes/st03-idempotent-ingest/deep-questions-r2.json
- 根拠: docs/requirements.md:266-267（FR-51「対象の本文を消去し」）/ docs/stories/ST23.md:4,35（ST23 が FR-51 を持ち、壊してはいけないものに ST03 を挙げている）/ migrations/202609081618_envelope.sql:29-31（`sensitivity` は `core.event` の列。履歴表を作れば同じ列が要る）/ 同 :14（FR-29 は全テーブルに `user_id`）/ Q1 選択肢 1 の detail は代償として**容量しか挙げていない**
- kind: irreversible
- 処置: escalated — Q12。履歴の本文を物理削除（FR-51）・論理削除（FR-50）・感度（PERM-2）の対象に含めるかを 1 問にした。Q1 の代償が容量だけではないことを why に書いた
- 提案: Q1 の答えの帰結として 1 問足すか、少なくとも ST03 の specs に書く。「履歴表に残した前の版の本文は、物理削除（FR-51）・論理削除（FR-50）・感度（PERM-2）の対象に含めるか」。含めないまま積むと、**本人が「本文ごと消した」記録の原文が履歴に残り続ける**。ST23 は ST03 より後なので、ここで決めておかないと ST23 は履歴表の存在を知らないまま作られる。

## R17. 外部識別子が「記録ごと」か「対象ごと」かを誰も保証していない —— Q9 の `why` は危険な例を安全な例として挙げている

- 成果物: openspec/changes/st03-idempotent-ingest/deep-questions-r2.json
- 根拠: 実験 B（同じ `external_id` の 2 件目は `event_dedup_ext` で落ちる。Q1 の答えの下では**落ちずに 1 件目を上書きする**）/ migrations/202609081618_envelope.sql:37-38（一意索引は `(logical_source, external_id)`。粒度の検査はどこにも無い）/ crates/server/src/ingest.rs:71-87（`validate` は `external_id` を一切見ない）/ Q9 の `why`:「YouTube の視聴履歴には**動画の識別子**があります」
- kind: irreversible
- 処置: escalated — Q13。外部識別子の粒度を誰が保証するかを 1 問にした。Q9 の why から動画 id の例を外し、Q13 の why で「対象の識別子であって記録の識別子ではない」危険な例として使い直した
- 提案: Q9 の `why` が「識別子がある側」の例に挙げた動画 id は、**記録（視聴 1 回）ではなく対象（動画）の識別子**で、同じ動画を 2 回見た記録は 1 行に畳まれる（Q1 の答えでは古い版が上書きされる）。逆に、取り込みのたびに振り直される識別子（書庫内の行番号など）だと ST12 の「同じ書庫を 2 回置いても増えない」が崩れる。**どちらも黙って起きる。** 問いを 1 つ足すか、Q9 の例を差し替えたうえで「ソースを登録するとき、その識別子が記録ごとに一意であることを誰が保証するか」を論点に立てる。

## R18. Q1（更新する）と Q5（400 で断る）が、同じ到着に対して逆を指す

- 成果物: openspec/changes/st03-idempotent-ingest/deep-questions-r2.json
- 根拠: Q1 の答え「既存行を書き換えたうえで履歴を残す」/ Q5 の答え「断る（400）」/ crates/server/src/lib.rs:184-194（`id` は主キーで、いまは PK 衝突が 500 になる）/ docs/requirements.md:129-130（FR-21「識別子の生成方式は実装時に決める」＝ **決まっていない**）
- kind: conflict
- 処置: escalated — Q14。取り込み側の識別子を毎回振るか外部識別子から導くかを 1 問にした。導く側を選ばれた場合は Q1 と Q5 の優先順位を追加で問う旨を選択肢に書いた。FR-21 の「生成方式は可逆」が成り立っていないことも context に入れた
- 提案: 外部サービスの記録を取り込み直したとき、取り込み側が `id` を**決定的に導く**（外部識別子から作る）なら、更新された記録は「同じ `id`・違う内容」として届く —— Q1 は「更新する」、Q5 は「400 で断る」。どちらが先に判定されるかで振る舞いが変わる。1 問足す（「取り込み側の `id` は記録ごとに振り直すのか、外部識別子から決定的に導くのか」）か、Q5 の答えに「`external_id` が一致する経路は Q1 が優先する」と但し書きを足す。いまはどちらも書かれていない。

## R19. Q2 の「利用者識別子を足す」が、鍵に混ぜるのか索引に足すのかで、誤った値を後から直せるかどうかが変わる

- 成果物: openspec/changes/st03-idempotent-ingest/deep-questions-r2.json
- 根拠: 実験 F（`user_id` 単独の UPDATE は**通る**。`content_hash` を伴うと 0004 が `RAISE`）/ crates/server/src/ingest.rs:100-116（鍵の入力は `logical_source` + `event_time` + `raw`）/ Q2 の question は「判定に**混ぜます**か」、選択肢 1 の label は「**索引 2 本**と…にも同じく足す」——2 通りに読める / collector-android/.../Config.kt:22（`userId` は `BuildConfig.USER_ID`＝ビルド時の設定）/ crates/server/src/lib.rs:49-61（認証は共有トークン 1 本。`user_id` は**申告値で、検証も外部キーも無い**。`core.user` 表は存在しない）
- kind: irreversible
- 処置: escalated — Q15。利用者識別子を鍵の中身に混ぜるか索引に足すだけかを 1 問にした。実験 F（単独の UPDATE は通る / 冪等キーを伴うと落ちる）を選択肢の不可逆に反映した
- 提案: 第 2 回で 1 行確かめる。**索引にだけ足す**なら、誤った利用者識別子は後から UPDATE で直せる（実験 F）。**`content_hash` の入力に混ぜる**なら、直すには凍結を外して全行を再計算する移行が要る（R14 と同じ扉）。ビルドのたびに手で入れる値なので取り違えは起こりうる。いまの文面はどちらとも読め、選択肢 1 の「単一利用者の運用では振る舞いが変わりません」は**値が変わった瞬間に偽**になる（全記録が別物として入り直す）。

## R20. 登録簿の「印」は列も既定値も無い。付け忘れたときにどちらへ倒れるかが決まっていない

- 成果物: openspec/changes/st03-idempotent-ingest/deep-questions-r2.json
- 根拠: migrations/202609081618_envelope.sql:6-11（`core.source` は 4 列。印に当たる列は無い）/ openspec/changes/st02-collection-coverage/design.md:111-112（ST02 が `user_id` と `collection_started_on` を足す。印は無い）/ ソースの登録は**手書きの INSERT が 5 か所**（tools/seed.sh:24 / tools/smoke.sh:30,108 / tools/check-immutable.sh:31 / collector-android/README.md:44）/ docs/requirements.md:313-314（FR-61）/ 同 :677-680（扉 #15「既定は厳しい側に寄せる。緩く始めて締めると、締める前に外へ出た分が戻らない」）
- kind: irreversible
- 処置: escalated — Q16。印の既定値を 1 問にした。あわせて Q9 の why から「印そのものは確実です」を外し、登録が手書きの 1 行で 5 か所に散っていることを Q16 の why に書いた
- 提案: Q9 の `why` は「印そのものは確実です。付け忘れ以外で外れることはありません」と本人の懸念を退けているが、**その付け忘れが唯一の失敗経路で、登録は手書きの 1 行が 5 か所にある**。既定を「印なし（通す）」に倒すと、付け忘れたソースの記録は識別子なしで入り、**後から足す手段が無い**（Q4 選択肢 2 の irreversible と同じ）。既定を「印あり（拒む）」に倒すと、忘れた瞬間に全部断られて気付く。この向きを 1 問にするか、少なくとも Q9 の `why` から「確実です」を外す。

## R21. Q9 の選択肢 1 は Q4 の答え（拒む）を実質的に取り消すのに、context が「その方針のまま」と書いている

- 成果物: openspec/changes/st03-idempotent-ingest/deep-questions-r2.json
- 根拠: Q9 context:「Q4 で『印のあるソースからの記録は識別子が無ければ拒む（400）』を選ばれました。この問いは、**その方針のまま**『混ざった場合だけ』をどう扱うか」/ Q9 選択肢 1:「識別子が無いものは内容の鍵で代用し…記録が 1 件も失われません」——**印のあるソースでも 400 が一度も出なくなる**/ Q4 選択肢 2（「拒まないが跡を残す」）は本人が選ばなかったもので、選択肢 1 はこれとほぼ同じ帰結になる
- kind: premise
- 処置: escalated — Q9 を書き直し、選択肢 3（内容の鍵で代用）に「Q4 で選ばれた強制は事実上なくなる」と明記した。context の「その方針のまま」を外し、強制を残す選択肢（論理ソースを分ける / 印を 2 段階に）を新たに 2 つ足した
- 提案: 選択肢 1 は「混ざった場合だけ」の話ではなく、**Q4 で選ばれた強制そのものを外す**選択肢である。context の「その方針のまま」を外すか、選択肢 1 を「**記録の種類ごと**に（例: Takeout のロケーション履歴だけ）代用を許す」に限定して、Q4 の答えとの差を本人に見える形にする。いまの文面だと、本人は「Q4 は生きたまま」と読んで推奨を押すことになる。

## R22. Q9 選択肢 2 の代償「何件あるか判らない・跡が残らない」は誤り。1 文で数えられる

- 成果物: openspec/changes/st03-idempotent-ingest/deep-questions-r2.json
- 根拠: 実測 —— `SELECT logical_source, count(*) FILTER (WHERE external_id IS NULL), count(*) FROM core.event GROUP BY 1;` が動く（出力: `geo.android | 1 | 1`）/ migrations/202609081618_envelope.sql:17（`external_id` は nullable な列で、無いこと自体が跡）/ 同 :15（`logical_source` が行に載っているのでソース別に数えられる）
- kind: premise
- 処置: escalated — Q9 選択肢 2 の「何件あるか判らない・跡が残らない」を削り、「件数はどちらを選んでも数えられる（前のターンの記述は誤り）」と本人へ訂正を書いた
- 提案: 選択肢 2 の detail「**「任意」にしたソースの中では、識別子の無い記録が何件あるのか判らない**」と irreversible「後から数え直せない（跡が残らないため）」を削るか、正しく書き直す。**選択肢 1 の利点として挙げた「画面で数えられます」は、選択肢 2 でも同じように成り立つ**。いまの文面は、実際には無い差を代償として並べて推奨側に寄せている。

## R23. Q8 の context が「識別子があればそれで、無ければ内容で」を決定済みと書き、Q9 がそれ自体を選ばせている（同じ 1 枚の中で食い違う）

- 成果物: openspec/changes/st03-idempotent-ingest/deep-questions-r2.json
- 根拠: Q8 context:「重複の判定は『識別子があればそれで、無ければ内容で』の 2 段になり」（**機械的な帰結**として提示）/ Q9 選択肢 1:「識別子が無いものは内容の鍵で**代用し**」（同じことを**選ばせている**）/ Q9 選択肢 3:「Q4 のまま拒む」（＝内容での代用をしない）
- kind: premise
- 処置: escalated — Q8 の context から 2 段判定の記述を外し、Q9 に一本化した。Q8 は「畳む仕組みを ST03 が持つか」だけを問う形にした
- 提案: 同じ 1 枚を上から読むと、Q8 で「決まっている」と言われたことを Q9 で選ばされる。どちらかに寄せる —— Q8 の context から 2 段の記述を外して Q9 に一本化するか、Q9 の選択肢 1 を「Q8 で述べた 2 段の判定を、印のあるソースにも適用する」と明示して重複を認める。いまのままだと、本人は Q9 の選択肢 3 を選んだときに Q8 の前提が崩れることに気付けない。

## R24. Q5 の補足への「事実」回答のうち、ST08 の部分が誤り（Android の部分は正しい）

- 成果物: openspec/changes/st03-idempotent-ingest/review/deep-r2.md（本人へ返した事実の訂正）
- 根拠: **正しい側** —— collector-android/.../FixCollector.kt:36（`newId()` は取得契機ごとに 1 回）/ LocationService.kt:59（`UUID.randomUUID()`）/ Outbox.kt:19-25 と OutboxStore.kt:123（採番済みの 1 件が**そのまま端末のファイルに直列化**され、再送は同じ `id` と同じ内容）→「Android では起きない」は正しい。**誤っている側** —— docs/stories/ST08.md:1-7（ST08 は PC のローカル収集で、`satisfies` は FR-13 のみ。**外部サービス由来ではない**ので Q4 の印の対象にならない）/ Chrome の識別子を使うとしたら入る先は `external_id`（FR-23）であって主キーの `id`（FR-21）ではなく、その場合の経路は **Q5 ではなく Q1**（同じ外部識別子・違う内容）
- kind: premise
- 処置: escalated — 本人へ訂正を返した。ST08 は PC のローカル収集で外部サービス由来ではなく、Chrome の識別子を使うなら入る先は external_id（＝Q1 の経路）。頻度は出典が無いので渡さない。Android の側（起きない）は正しいので残した
- 提案: 本人へ返す事実を直す。Q5 の経路（同じ**収集側の** `id`・違う内容）が確実に起きるのは、**取り込み側が `id` を外部識別子から決定的に導いたとき**で、それは FR-21 が「実装時に決める」と書いたまま未決（R18）。「ST08 のブラウザ履歴のように」は例として成り立たない。頻度についても、リポジトリ内に根拠は無い（EXT 表にブラウザ履歴の識別子に関する行は無く、EXT-E は 90 日の話だけ）—— **出典の無い頻度は本人に渡さない**。

## R25. Q8 選択肢 2 の「本人が手でやる」の量が見積もられていない（手順 4）

- 成果物: openspec/changes/st03-idempotent-ingest/deep-questions-r2.json
- 根拠: Q8 選択肢 2「重複の解消は本人が手でやる」に件数も頻度も無い / docs/stories/ST12.md:38 と docs/requirements.md:605（EXT-B: Takeout の予約エクスポートは **2 か月ごと・1 年で停止**）——重複が出るときは 1 件ずつではなく**書庫 1 本ぶんがまとまって**出る / Q6 の答えにより、識別子が振り直された書庫は全件が新しい行になる
- kind: daily
- 処置: escalated — Q8 選択肢 2 の detail に「重複は 1 件ずつではなく取り込み 1 回ぶんがまとまって出る（書庫の予約エクスポートは 2 か月ごと）」を足した
- 提案: 選択肢 2 の detail に「重複は 1 件ずつではなく、取り込み 1 回ぶんがまとまって出うる（2 か月ごとの書庫）」を足す。手作業の頻度と量が見えないまま「仕組みが要りません」だけが書かれていると、日常の代償が過小に見える。手順 4 で新しく出た項目はこれ 1 件。

## R26. Q8 の選択肢 1（読む側で畳む）は ST03 の範囲にも要件にも受け皿が無い

- 成果物: openspec/changes/st03-idempotent-ingest/deep-questions-r2.json
- 根拠: docs/stories/ST03.md:3（`satisfies: [FR-22, FR-23]`）/ 同 完了の判定は行数と更新の 2 項目だけで、表示に触れていない / docs/requirements.md に「重複を畳んで見せる」に当たる FR は無い（FR-17 は取り込み時の重複検出＝ST12）/ Q8 選択肢 1 自身が「閲覧・検索・AI・書き出しの 4 か所で規則が要る」と書いている（いずれも ST03 ではない Story）
- kind: conflict
- 処置: escalated — Q8 選択肢 1 を「ST03 が作るのは規則の置き場まで。4 か所への適用は後続 Story の作業」と書き直した。ST03 で完結するようには読めなくなった
- 提案: この選択肢を選んだ場合、**ST03 では 1 行も実装できない**（畳む規則を受け取る Story がまだ特定されていない）。選択肢の detail に「ST03 で作るのは規則の置き場（ビューか関数）までで、4 か所への適用は後続 Story の作業になる」と書くか、決めた内容を要件へ戻す前提を明記する（R27）。いまの文面は ST03 で完結するように読める。

## R27. 第 1 回の答えのうち、要件へ戻さないと次に読む人が逆に実装するもの（抜けと過剰）

- 成果物: docs/requirements.md
- 根拠: **FR-22**（:131-132「同一の識別子または同一の内容ハッシュを持つ記録が再び届いたら既存を更新し、新しい行を作らない」）に、Q5（同じ識別子は 400 で断る）・Q6（同じ内容でも識別子が違えば**新しい行を作る**）・Q7（派生は対象外）・Q2（利用者ごと）の 4 つが**正面から当たる** / **FR-23**（:133）は「持たせる」だけで、Q4 の「持たない記録を拒む」も登録簿の印も無い / **FR-30**（:142）と扉 #7（:646-649）は Q1 の更新に当たる / **FR-50**（:264）に Q3 の「削除済みへの再送は取り込まない」が無い / **FR-61**（:313-314）は登録簿を「1 行足せば受け付ける」と言っており、印という**必須項目が増える**ことに触れていない / **FR-10**（:99「未送信の記録を送信する」）に、Q4・Q5 が要求する「恒久的に断られた記録を諦める」が無い（捨てる決定なので要件が要る）/ **FR-18 と FR-29**（:123,141）は履歴表にも掛かる（R16）
- kind: conflict
- 処置: escalated — deep.md の「要件へ戻すもの」に列挙して改訂する。戻す先は FR-22（必須）/ FR-23 / FR-30 と扉 #7 / FR-50 / FR-61 / FR-10、履歴表を作るので FR-18・FR-29 の適用範囲。**FR-21 は過剰という指摘を容れて外し**、代わりに FR-22 側へ「この識別子は冪等の判定に使わない」を書く。第 2 回の答えが揃ってから一度に反映する
- 提案: 戻す先は **FR-22（改訂が必須）/ FR-23 / FR-30 と扉 #7 / FR-50 / FR-61 / FR-10**、および履歴表を作るなら FR-18・FR-29 の適用範囲。ご指摘の候補のうち **FR-21 は過剰** —— 「収集側で生成した一意な識別子を持たせる」の文言は答えのどれとも矛盾せず、必要なのは「この識別子は冪等の判定に使わない」という一言だけで、それは FR-22 側に書くのが筋（FR-21 の「生成方式は実装時に決める（可逆）」は R18 が決まるまで**可逆ではなくなっている**ので、そこだけは注記が要る）。

## R28. Q8 の `kind: irreversible` が実態と合わない

- 成果物: openspec/changes/st03-idempotent-ingest/deep-questions-r2.json
- 根拠: Q8 推奨（選択肢 1）の irreversible 欄:「**この形では何も失われない**（畳み方の変更も、畳むのをやめることも、後からできる）」/ 選択肢 2 の irreversible は手で消したときの話で、選択の不可逆ではない / 不可逆なのは選択肢 3（畳む組を間違えると別々の記録が 1 件になる）だけ
- kind: technical
- 処置: fixed deep-questions-r2.json — Q8 の kind を irreversible から open に変えた。推奨選択肢自身が「何も失われない」と書いているのに不可逆と分類していたのは誤りで、**問いの重みを実態より重く見せると次の深掘りが読み飛ばされる**という指摘を容れた
- 提案: Q8 の実体は「**畳む仕組みを ST03 で持つか、ST12 と表示の Story に送るか**」という範囲の問い（Q7 と同型）で、`conflict`（FR-22 と表示の要件の不在）か `open` が合う。`irreversible` のままだと、本人は「いま決めないと取り返しがつかない」と読んで、実際には後から変えられる選択に時間を使う。**問いの重みを実態より重く見せる分類は、次の深掘りで「どうせ全部不可逆と書いてある」と読まれる方向に効く。**
