# ST03 深掘り 第 3 回の独立レビュー（収束の検査）

対象: `deep-questions.json`（Q1〜Q7）と `deep-questions-r2.json`（Q8〜Q16）の **16 問すべてに
答えが出た状態**。`openspec/schemas/ashiato/schema.yaml` の `deep` 手順 1〜5 を、
**16 の答えを所与として**独立にやり直し、「まだ誰も決めていない一方通行・衝突」が残っているかを見た。
**問いの JSON も `deep.md` も 1 文字も触っていない。** 採番は R29 から（R1〜R28 は使用済み）。

実施日: 2026-09-11。ブランチ `docs/st03-upstream`（clean, `b3dab23`）。

## 判定

**第 3 回が要る。** 人間に返すべき論点が **6 件**残っている（R29 / R30 / R31 / R32 / R33 / R34）。
いずれも 16 の答えを**全部入れて実際に動かした結果**出たもので、答えのどれも向きを決めていない。
残り 5 件（R35〜R39）は問いではなく、戻す先の増減・前提の訂正・design/tasks への固定。

## 実測の方法

使い捨ての DB（**同じコンテナ内に別データベース `st03probe` を作った。ST02 の `ashiato` には
接続していない**）に `migrations/0001`〜`0004` を当て、そのうえで **16 の答えを全部実装した
SQL**（登録簿の 2 列・利用者識別子を足した索引 2 本・履歴表・Q10 の遅延制約トリガ・
16 の答えを分岐にした `core.ingest_one()`）を書いて流した。以下「実験 G〜R」はこれ。

| | 流したもの | 結果 |
|---|---|---|
| 実験 G | Q10 を遅延制約トリガ（`AFTER UPDATE … DEFERRABLE INITIALLY DEFERRED` + `pg_current_xact_id()`）で実装し、履歴を書かずに UPDATE | **UPDATE 文は通り、COMMIT で `ERROR`。** 行は元のまま（＝実現手段は存在する） |
| 実験 H | 同上で、UPDATE の**あとに**履歴を INSERT | **通る**（順序に依存しない） |
| 実験 I | 同 txn で履歴を INSERT → DELETE | **COMMIT で `ERROR`**（同 txn の消しは効かない） |
| 実験 J | txn を分けて `UPDATE core.event_version` / `DELETE FROM core.event_version` | **どちらも通る**（→ R29） |
| 実験 K | **完了の判定 1**: 同じ記録を 3 回（外部ソース / 端末ソースの両方） | **どちらも 1 行のまま**（`accepted(duplicate)` / `accepted(duplicate-by-hash)`） |
| 実験 L | **完了の判定 2**: 外部で更新された記録を取り直す | **行は増えず、既存行が更新され、履歴が 1 件**（成立） |
| 実験 M | Q6: 同じ内容・違う外部識別子を、**0001 の索引のまま** | `ERROR: duplicate key … "event_dedup_hash"`。狭めた部分索引に作り替えると **2 行入る** |
| 実験 N | Q13: 「対象ごとの識別子」のソースを Q16 の既定（＝外部由来）で登録し、Q13 の答えどおり `external_id` を空にして送る | **`400 missing_external_id`。1 件も入らない**（→ R30） |
| 実験 O | Q3 / Q11 / Q5 | すべて答えどおりに動く（`duplicate-by-hash` で削除済みのまま / `deleted-wins` / `400 id_reused`） |
| 実験 P | 削除済みの行と**同じ本文**を、**違う外部識別子**で送る | **新しい行として入り、`event_live` に見える**（→ R31） |
| 実験 Q | 新しい版を入れたあとに**古い版**を送る（書庫の取り込み・並行取得） | **古い内容が上書きし、行数は 1 のまま、履歴だけ増える。**交互に来ると履歴が無限に積む（→ R32） |
| 実験 R | 感度を 1 → 3 に締める / 親を論理削除する | **履歴の感度は 1 のまま、履歴の削除印も付かない**（→ R33） |

Q10 の実現手段については、**存在することを実測で確認した**（実験 G〜I）。
制約トリガは COMMIT 時に走るが、その時点でも `pg_current_xact_id()` は同じ値を返すので、
「この txn が履歴を書いたか」を commit の瞬間に判定できる。**これ自体は問いにしない**（→ R38）。

## 手順ごとの記録

- **手順 1（要件どうしの衝突）**: FR-22 / FR-23 と FR-10 / FR-17 / FR-18 / FR-21 / FR-29 /
  FR-30 / FR-31 / FR-50 / FR-51 / FR-61 / PERM-2〜3 を、16 の答えを入れた状態で突き合わせた。
  新しく出たのは **Q6 × Q3 / ST23（R31）**、**Q4・Q16 × Q13（R30）**、**Q8 の受け皿の不在（R35）**。
  PERM-3（収集した記録の既定の感度＝外部 AI 可）と `0001:29` の `DEFAULT 1` は**一致している**
  （扉 #15 の「厳しい側」と矛盾して見えるが、PERM-3 が明示的に決めている）。
- **手順 2（扉の幅）**: 扉 #7 は Q10 の答えで「履歴が同 txn にあるときだけ開く」に狭まったが、
  **その履歴表自体には扉が立っていない**（R29）。扉 #15（感度は厳しい側へ）は、
  Q12 の答えの 2 通りの読みのうち片方でしか成り立たない（R33）。
  扉 #12 は Q13 で「記録ごと / 対象ごと」まで降りたが、**降りた先が Q4 の 400 と衝突する**（R30）。
- **手順 3（新たに立つ一方通行）**: 捨てるもの＝断られた記録（Q4 / Q5。決定済）、
  削除済みへの外部の訂正（Q11。決定済）。**鍵の計算に使う入力**の観点で新しく 1 件 ——
  `content_hash` は `logical_source` を含むので、**Q9 の「論理ソースを分ける」が鍵を変える**（R34）。
  **列の型**の観点で 1 件 —— 履歴表の `raw` の型が誰の成果物にも書かれていない（R39。
  `jsonb` で作ると 0003 でやり直した失敗がそのまま履歴側で再発する）。
- **手順 4（日常に影響する選択）**: 容量は Q1 に書かれているが、**R32 の往復更新を入れると
  上限が無い**。手作業の頻度は R25（第 2 回）が見積もった範囲を超えない。
  常時通知・電池への新しい影響は**該当なし**（ST03 はサーバ側だけを触り、収集側の変更は
  「断られた記録を諦める」＝送信回数が減る向き）。Q16 の「すぐ気付きます」は誤り（R36）。
- **手順 5（既存コードが要件を満たしていない箇所）**: FR-22 / FR-23 について
  `crates/server/src/lib.rs:179-233` と `crates/server/src/ingest.rs:93-116` を読み直した。
  **新しい未達は出なかった**（R11 / R12 / R13 が既出のまま）。1 点だけ追記 ——
  `tools/check-immutable.sh:44-80` は CI（`.github/workflows/ci.yml:58`）で
  **ST03 が開けることになる列そのもの**（`raw` / `payload` / `event_time` / `content_hash`）を
  「拒まれること」として検査している（R38）。
- **不要な問い**: **該当なし。** 16 問すべて、要件・扉・既存コードのどれも答えを決めていなかった
  （第 1 回・第 2 回のレビューの結論を、16 の答えを入れた状態から再確認した）。
- **分類違い**: **該当なし**（R28 で直した Q8 の `open` を含め、16 問の `kind` は実態と合っている）。

---

## R29. 履歴表そのものには凍結が無い。0004 が塞いだ筋が、履歴側で 2 手のまま開く

- 成果物: openspec/changes/st03-idempotent-ingest/deep-questions-r3.json（新規）
- 根拠: 実験 J（実測）—— `① {"t":"v1"}` → `BEGIN; UPDATE core.event SET raw='{"t":"tampered"}'; INSERT INTO core.event_version …; COMMIT;` → `DELETE FROM core.event_version WHERE event_id=…;` → **`② {"t":"tampered"}` / 残った原文の版 0**。
  同 txn 内の削除は Q10 のトリガが止める（実験 I）が、**次の txn の `UPDATE` / `DELETE` は素通り**する。
  migrations/0002_immutable_collected.sql:5-8 が DB 側に置いた理由として挙げているのは
  「同じ PC で動く第三者製プラグイン（PERM-8）や psql を直に叩く運用」で、
  migrations/0004_immutable_origin.sql:5-13 は**その相手が 3 手を打てた**ことを実測で記録している。
  Q10 の答えは本表の書き換えを履歴に縛ったが、**原文の最後の 1 本が履歴表に移った**ことで、
  同じ相手が 2 手で同じ結果（原文の消失）に到達する。deep.md にも 16 の答えにも履歴表の保護は無い
- kind: irreversible
- 処置: escalated — Q17。履歴表の凍結の幅を 1 問にした（追記のみ / 本表と同じ幅 / 掛けない）。Q12 が認めた削除の印・感度・本文の消去は通す必要があることを context に書いた
- 提案: 「履歴に残した前の版を、DB の側でどこまで凍結するか」を問う。
  選択肢は (a) 履歴表は追記のみ（`UPDATE` / `DELETE` を拒み、Q12 が認めた削除印と感度の更新だけ通す）、
  (b) 本表と同じ幅（本文だけ凍結）、(c) 掛けない。**(c) を選ぶと Q1 の「原文を失わない」が成り立たない。**

## R30. Q4・Q16 の「印」と Q13 の「何の識別子か」は同じことを決めており、対象ごとの識別子しか持たない外部ソースは 1 件も入らない

- 成果物: openspec/changes/st03-idempotent-ingest/deep-questions-r3.json（新規）
- 根拠: 実験 N（実測）—— `is_external=true`（Q16 の既定）と `external_id_kind='subject'`（Q13）で
  登録したソースへ、Q13 の答えどおり `external_id` を空にして送ると **`400 missing_external_id`**。
  Q4 の答えは「印のあるソースからの記録は識別子が無ければ拒む」、Q13 の答えは
  「記録ごとでないソースは外部識別子を使わない（内容の鍵で畳む）」で、**同じソースに対して逆を指す**。
  Q9 の答え（識別子を返さない種類は別のソースにし、印を付けない）は**混在**の話で、
  一様に「対象ごと」のソース（Q13 が挙げた動画）には当たらない。
  さらに **Q16 は 1 列目の既定だけを決めており、2 列目（`external_id_kind`）の既定は誰も決めていない** ——
  `'record'` を既定にすれば付け忘れは全件 400、`'none'` を既定にすれば強制が黙って消える。
  既存コードには印に当たる列が無いので（migrations/0001_envelope.sql:6-11）、どちらも新設時の判断
- kind: conflict
- 処置: escalated — Q18。400 の条件をどちらの列に載せるかを 1 問にし、**2 列を 1 列に畳む**選択肢を推奨に置いた。畳むなら FR-61 へ戻す列は 1 本になる（R35 と連動）
- 提案: 「400 を出す条件を、印（外部由来か）と識別子の種類のどちらに載せるか。2 列目の既定はどちらへ倒すか」
  を問う。**2 列は同じことを決めている**ので、1 列に畳む（`external_id_kind` だけにし、`'record'` のときだけ 400）
  という選択肢も並べる。畳むなら FR-61 へ戻す列は 2 本ではなく 1 本になる（→ R35）。

## R31. Q6 の索引の狭めが、Q3 の「二度と入りません」と ST23 の前提を破る —— 消した本文が別の外部識別子で戻る

- 成果物: openspec/changes/st03-idempotent-ingest/deep-questions-r3.json（新規）
- 根拠: 実験 P（実測）—— `external_id='N1'` の行を論理削除したうえで、**同じ本文**を
  `external_id='N9'` で送ると `accepted(stored)` で**新しい行として入り、`core.event_live` に見える**。
  Q6 の答え（識別子が違えば別の記録）を成立させるには内容の鍵の一意索引を
  `WHERE external_id IS NULL` に狭めるほかなく（実験 M。deep.md の Q6「効く先」も同じ結論）、
  狭めた瞬間に**外部識別子を持つ行には内容の鍵の網が掛からなくなる**。
  docs/stories/ST23.md:35 は壊してはいけないものに「**ST03 の重複判定（消したものが再流入しないこと）**」を挙げ、
  Q3 の選択肢 1 は「**本人が消した記録は、端末に残っていても二度と入りません**」と本人に伝えている。
  外部サービスが識別子を振り直す・同じ投稿が 2 件に分かれるのは Q6 の context が挙げた実在の経路
- kind: irreversible
- 処置: escalated — Q19。削除済みの本文が違う外部識別子で戻る件を 1 問にした。Q3 で本人に「二度と入りません」と伝えた文面が成り立たないことを why に明記した
- 提案: 「削除済みの本文が、違う外部識別子で戻ってきたときどうするか」を問う。
  選択肢は (a) 内容の鍵は削除済みの行にも当て続ける（＝ Q6 の「別の記録として入れる」を
  削除済みに限って外す）、(b) 入れる（Q6 を優先し、Q3 の約束を「その記録に限る」と読み直す）、
  (c) 入れるが削除済みとして入れる。**(b) を選ぶなら Q3 の文面と ST23 の前提を直す必要がある。**

## R32. 古い版があとから届いたときの順序が決まっていない —— 古い内容が新しい内容を上書きし、履歴だけが無限に積む

- 成果物: openspec/changes/st03-idempotent-ingest/deep-questions-r3.json（新規）
- 根拠: 実験 Q（実測）—— `v1` → `v2` と取り込んだあと `v1` を送ると `accepted(updated)` で
  **`v1` に戻り**、行数は 1 のまま、履歴は 2 件。`v2`・`v1` を交互に送ると履歴が 4 件へ増え続ける。
  Q1 の答え（既存行を書き換え、前の版を履歴へ）には**どちらが新しいかの判定が無い**。
  判定する材料も無い —— Q1 の context 自身が「いまの記録には『更新された時刻』を持つ欄がありません」と
  書いており、外部サービス側の版・更新時刻を持つ列は `core.event` に無い（migrations/0001_envelope.sql:14-37）。
  この経路は仮定ではなく docs/stories/ST12.md:38（書庫の取り込み。2 か月ごとの予約エクスポート）と
  FR-17（docs/requirements.md:117-118「過去のエクスポートを置いたときの重複検出」）そのもの。
  ST12 の「同じ書庫を 2 回置いても増えない」は**行数としては守られる**が、内容は過去へ巻き戻る
- kind: irreversible
- 処置: escalated — Q20。古い版があとから届いたときを 1 問にした。列を day one で持たないと順序が復元できないことを context に書き、ST12（2 か月ごとの書庫）がこの経路そのものであることを why に書いた
- 提案: 「古い版があとから届いたとき、上書きするか無視するか」を問う。
  選択肢は (a) 外部サービスの更新時刻を列で持ち、新しいほうだけを採る（**列は day one で持たないと、
  持つ前の更新の順序は復元できない**）、(b) 常に最後に届いたものを採る（いまの実装。履歴に上限が要る）、
  (c) 内容の鍵が既に履歴にある版と一致したら更新しない（往復は止まるが、正当な差し戻しも入らない）。

## R33. Q12 の「履歴も削除・感度の対象」は 2 通りに読め、片方では感度を締めても過去版が締まらない

- 成果物: openspec/changes/st03-idempotent-ingest/deep-questions-r3.json（新規）
- 根拠: 実験 R（実測）—— 履歴表が自分の `sensitivity` / `deleted_at` を持つ形（deep.md の Q12
  「効く先」が書いている形）で、親の感度を 1 → 3（AI に出さない）へ締めても
  **履歴の感度は 1 のまま**で、`SELECT … WHERE sensitivity <= 1` が前の版の本文を拾う。
  親を論理削除しても**履歴の削除印は付かない**（`本体は削除済み t / 履歴は削除済み f`）。
  docs/requirements.md:676（扉 #15）は「既定は厳しい側に寄せる。**緩く始めて締めると、
  締める前に外へ出た分が戻らない**」と決めており、ここで漏れるのは同じ性質のもの。
  FR-60（:308-309）は感度で許可された範囲だけを MCP に返すと定めている。
  Q12 の答えの文面（「含める」）は、(a) 履歴が自分の列を持ち、締め直しを伝播させる、
  (b) 履歴は常に親の感度・削除に従う（列を持たない）、のどちらにも読める
- kind: conflict
- 処置: escalated — Q21。履歴の感度と削除を親に従わせるか自分で持たせるかを 1 問にした。扉 #15 と FR-60 を why に引いた
- 提案: 「履歴の感度と削除を、親と一緒に動かすか、別に持つか」を問う。
  (b)（親に従う）なら伝播の実装が要らず漏れないが、**前の版だけを強く保護する**ことはできなくなる。
  (a) なら親の更新時に必ず伝播させる仕掛け（トリガ）が要り、書き忘れが即「外へ出る」に繋がる。

## R34. Q9 の「論理ソースを分ける」は冪等キーの入力を変える。分けたあとに同じ書庫を入れ直すと全件が新しい行になる

- 成果物: openspec/changes/st03-idempotent-ingest/deep-questions-r3.json（新規）
- 根拠: `crates/server/src/ingest.rs:112` が `field(req.logical_source.as_bytes())` で
  **論理ソース名を鍵の最初の入力にしている**（同 :200-206 の `field_boundaries_are_unambiguous` が
  ソース名の違いで鍵が変わることを固定している）。別実装で再現: 同じ原文・同じ時刻で
  `takeout` → `9c24f1f6d0d1c8bb…` / `takeout.location` → `8e58d7f31fa62e16…`。
  実測でも、分けたあとに同じ書庫を入れ直すと **2 行**になった。
  さらに `logical_source` は 0002 / 0004 のどちらのトリガも見ていないので
  **既存行のソース名だけを動かせる**（実測）が、動かしても `content_hash` は古い名前で計算されたまま残り、
  その行は以後どの再送とも鍵が一致しない（重複判定が黙って当たらない行ができる）。
  Q9 の答え（混ざるものは論理ソースを分けて登録する）と Q13 の答え（記録ごとでないソースは内容の鍵で畳む）は
  **分けることを通常の運用にする**が、選択肢の代償欄は「ソースの数が増えること」しか書いていない。
  Q15 で「鍵の中身は今のまま」と決めた以上、鍵から名前を外す逃げ道は閉じている
- kind: irreversible
- 処置: escalated — Q22。分けたあとの過去分をどうするかを 1 問にした。Q9 の選択肢が代償に「ソースの数が増える」しか書いていなかったことも why に書いた
- 提案: 「分ける判断を、いつまでに下すか」を問う（取り込む前に分けるなら無害、入れた後だと
  過去分が二重になる）。または Q9 / Q15 の代償欄にこの事実を足したうえで、
  **「分けたあとの過去分をどうするか（入れ直す / 古いソース名のまま残す / 移し替える）」**を問う。

## R35. Q8 の答えの受け皿になる要件が無く、deep.md の「要件へ戻すもの」にも Q8 の行が無い

- 成果物: openspec/changes/st03-idempotent-ingest/deep.md（「要件へ戻すもの」の表）
- 根拠: Q8 の答えは「ST03 は**内容の鍵が同じものを 1 件として読む置き場**を作る」で、
  deep.md:196-198 は「`specs` に読み出しの形」と書いている。しかし
  docs/requirements.md に「重複を畳んで見せる」に当たる FR は無い（第 2 回 R26 が同じことを実測済み。
  FR-17 は取り込み時の重複検出＝ST12、FR-22 は書き込み側）。deep.md:199-218 の表は
  FR-22 / FR-23 / FR-30 / 扉 #7 / FR-50 / FR-51 / FR-61 / FR-10 / FR-18 / FR-29 / PERM-2 の 11 行で、
  **Q8 を元にした行が 1 つも無い**。受け皿が無いまま specs に書くと、
  `scripts/check_chain.py`（要件 → Story の鎖）から見えない成果物が 1 つ生まれる
- kind: conflict
- 処置: escalated — deep.md の「要件へ戻すもの」に Q8 の行を足す（FR-22 に「内容の鍵が同じ複数行を 1 件として読む形を用意する。適用は閲覧・検索・AI・書き出しの各 Story」）。**kind=conflict は過大**（レビュー自身が「本人に返さなくてよい」と書いている）が、指摘を書いた側の分類は書き換えない —— 戻し先の文言は requirements.md に ★ 付きで現れるので、本人の目に触れる形で閉じる
- 提案: 「要件へ戻すもの」に 1 行足す —— **FR-22 に「内容の鍵が同じ複数行を 1 件として読む形を
  用意する。適用は閲覧・検索・AI・書き出しの各 Story」**を書くか、新しい FR を起こす。
  どちらにするかは本人に返さなくてよい（Q8 の答えが範囲を決めている）。

## R36. Q16 の「付け忘れた瞬間に全部断られるので、すぐ気付きます」は、いまの実装では成り立たない

- 成果物: openspec/changes/st03-idempotent-ingest/deep-questions-r2.json（Q16 選択肢 1 の detail）
- 根拠: 断りは収集側では `Log.w` の 1 行にしかならない
  （`collector-android/.../Sender.kt:87-90` が `rejected` を種別ごとに数えてログへ出すだけ。
  `MainActivity.kt` に画面へ出す経路は無く、`grep -rn "accepted\|rejected" collector-android/…/MainActivity.kt` は 0 件）。
  サーバ側も `tracing::info!(kind="ingest", …)`（`crates/server/src/lib.rs:279-284`）だけ。
  気付く唯一の自動経路は稼働記録の途絶通知だが、断られた記録は
  `lib.rs:160-177` が `core.coverage` を書く前に return するので「記録が無い日」として現れ、
  FR-35 の通知は**想定間隔の 3 倍**を超えてから（位置は `expected_gap_sec=21600` なので 18 時間後）。
  しかもその通知は ST02 の未実装分
- kind: premise
- 処置: escalated — deep.md の Q16「効く先」に事実を書き足し、本人にも伝える。気付く経路は端末のログ（画面に出ない）か ST02 の途絶通知（位置なら 18 時間後・未実装）だけで、「すぐ気付きます」は成り立たない。既存 5 か所へ「外部由来ではない」を明示する移行の優先度が上がる
- 提案: Q16 は本人が既に答えているので問い直さない。**deep.md の Q16「効く先」に
  「気付く経路は端末のログか、ST02 の途絶通知（位置なら 18 時間後）だけ」**と事実を書き足す。
  移行で既存 5 か所に「外部由来ではない」を明示する作業の優先度が、この事実で上がる。

## R37. deep.md の Q2「ST02 は未 merge」は古い。ST02 側の索引は実装ブランチで既に直っている

- 成果物: openspec/changes/st03-idempotent-ingest/deep.md（Q2 の「効く先」）
- 根拠: deep.md:57-59 は「ST02 の `core.heartbeat` の索引にも同じ変更が要る（ST02 は未 merge なので、
  いま直せば片方だけ直る状態を避けられる）」と書いている。実際には
  `git merge-base --is-ancestor ac97574 main` が真（ST02 の上流は main に入っている）で、
  実装ブランチ `feat/st02-collection-coverage` の `migrations/0005_coverage_rebuild.sql:83-84` は
  既に `CREATE UNIQUE INDEX heartbeat_dedup ON core.heartbeat (user_id, logical_source, content_hash)` になっている
  （ST02 自身の code review R13 が独立に見つけた）。
  残っているのは `openspec/changes/st02-collection-coverage/design.md:85` の文面だけ（`user_id` が無い）。
  併せて、**ST02 が `0005` と `0006` を使っているので ST03 の移行は `0007` から**
- kind: premise
- 処置: escalated — deep.md の Q2「効く先」を事実に直し、本人にも伝える。ST02 側の索引は実装ブランチの 0005 で既に user_id を含んでおり、残るのは design.md:85 の文面のみ。ST03 の移行は 0007 から
- 提案: deep.md の Q2「効く先」を事実に合わせる（「ST02 側は実装ブランチの 0005 で済んでいる。
  残るのは design.md:85 の文面のみ」）。ST03 の tasks が同じ索引をもう一度作らないよう、
  移行の番号（0007〜）と併せて design へ書く。

## R38. Q10 の実現手段は存在する（実測）。ただし本人に伝えていない性質が 3 つあり、うち 1 つは CI の検査を無効化する

- 成果物: openspec/changes/st03-idempotent-ingest/design.md（未作成）
- 根拠: (a) 実験 G〜I —— `CREATE CONSTRAINT TRIGGER … AFTER UPDATE … DEFERRABLE INITIALLY DEFERRED` と
  履歴行の `txid xid8 DEFAULT pg_current_xact_id()` の組で、Q10 の答えはそのまま実装できる。
  **落ちるのは COMMIT 時**なので、まとめ送りを 1 トランザクションにすると 1 件の失敗が全件を巻き戻す
  （design D9 / D20 の「1 件の恒久的な失敗が後続を永久に止めない」に当たる。1 件 1 txn が要る）。
  (b) 実測 —— `content_hash` を 0004 のまま凍結しておくと Q1 の更新は
  `ERROR: 冪等キーは書き換えられない` で 1 行も動かない。開ける列に `content_hash` が入る
  （Q10 の選択肢の文面は「どの列を開けるか」を書いていない）。
  (c) `tools/check-immutable.sh:44-80` は `raw` / `payload` / `event_time` / `content_hash` の
  UPDATE が**拒まれること**を CI（`.github/workflows/ci.yml:58`）で検査している。
  ST03 がこの 4 列を開けるので、検査を消さないと CI が落ち、**消すと守りが黙って消える**
- kind: technical
- 処置: fixed deep.md — 「上流で見つかった既存実装の欠陥」に申し送りとして追記。Q10 は遅延制約トリガで実装できるが (a) 落ちるのが COMMIT 時なので 1 件 1 トランザクションが要る (b) 開ける列に content_hash が入る (c) tools/check-immutable.sh が CI でその 4 列の UPDATE が拒まれることを検査しているので作り替えが要る。design と tasks へ落とす
- 提案: 問いにはしない。design に (a)(b) を書き、tasks に
  「`check-immutable.sh` を『履歴を書かない UPDATE は拒まれる / 履歴を書けば通る』の 2 本に作り替え、
  R29 の答え（履歴表の凍結）も同じ台本で検査する」を入れる。

## R39. 履歴表の `raw` の列の型が、どの成果物にも書かれていない

- 成果物: openspec/changes/st03-idempotent-ingest/design.md（未作成）
- 根拠: 手順 3 の「列の型・保存形式」の観点。migrations/0003_raw_text.sql:4-14 は
  **`jsonb` が原文を保たない**ことを実測で記録している（キー順が変わり、重複キーが消え、
  数値の表記が展開される）。Q1 の答えで前の版の原文が履歴表へ移るのに、
  16 の答え・deep.md・第 1 回/第 2 回のレビューのいずれも履歴表の列の型に触れていない。
  `jsonb` で作ると 0003 でやり直したのと同じ失敗が履歴側で再発し、
  **今度は前の版しか原文が無いので取り返せない**。ST02 は同じ轍を踏まない形にしてある
  （`feat/st02-collection-coverage` の `0005_coverage_rebuild.sql:64` に
  `raw text NOT NULL -- 原文の素通し。**text**（0003 と同じ理由）`）
- kind: technical
- 処置: fixed deep.md — 同上。履歴表の raw は text（理由は 0003 と同じ。jsonb はキー順・重複キー・数値表記を保たない）。design に明記し、tasks に information_schema での型の検査を入れる
- 提案: 問いにはしない。design に「履歴表の `raw` は `text`。理由は 0003 と同じ」を明記し、
  tasks に型を確かめる検査（`information_schema.columns` で `text` であること）を入れる。
