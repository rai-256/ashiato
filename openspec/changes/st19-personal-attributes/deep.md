# ST19 の深掘り — 個人属性を上書きせず履歴で残す

> 状態: **問いを渡した。答え待ち（2026-09-15）。**
> 問いは `deep-questions.json`（4 件。A 2 件 / B 2 件）、渡した形は `docs/briefs/ST19-deep.html`。
> 画面の問い（Q2）が埋め込む playground は `proto.html`（この change に置く ——
> `docs/briefs/` は `.gitignore` で消えるが、この proto は script で再生成できない）。
> 独立レビューは `review/deep.md`（9 件。問いの文面を直した 3 / 聞かないで決める既定に足した・直した 6。要件へ戻すもの 2 件を足した）。

## 問うた項目

<!-- 本人の答えが戻ったら、1 項目ずつ「本人の答え」と「効く先」を埋める -->

### Q1. 書いた主張を消せるか（A / conflict / loss: discarded）

- **なぜ人間の判断か**: 要件の矛盾。FR-44（書き換えず追記する）と FR-50 / FR-51（記録を消したことにする／本文を消す）が、
  個人属性の主張について逆を指す。消せる側を選び、本人が本文を消すと値は戻らない
- **当初の案**: なし（A）
- **出所**: 手順 1（要件どうしの衝突）と手順 5（既存コード）。いまの DB は「本人が書いた」記録の書き換えも行の削除も止めていない
  （`migrations/202609120944_gates.sql:40` / `:82` / `:241-248`。`tools/check-immutable.sh:147-159` がそれを OK として確かめている）
- **本人の答え**: （待ち）
- **効く先**: （待ち）

### Q2. 個人属性の画面の構造（A / visual）

- **なぜ人間の判断か**: 画面の構造は文字の選択肢で決められない。ST19 は S-6 マスタ管理の最初の Story で、骨格を ST20 / ST21 が引き継ぐ
- **当初の案**: proto の初期値（種類ごとのカード・履歴は開いたときだけ / 「いつから」の新しい順 / 「いつから」を主役に書いた日を小さく /
  取り消された主張は取り消し線で残す / カードごとに「変わった」「間違いを直す」/ 精度を先に選ぶ）
- **実測**（proto を headless で描いて測った。幅 360 px・導入直後 21 件 → 10 年後 32 件）:
  カード・畳む 924 → 902 px / カード・全部 2,622 → 3,488 px / 1 本の時系列 2,480 → 3,863 px /
  年表の帯 640 px（1 画面）だが文字が入りきらない区間 14 → 23
- **本人の答え**: （待ち。proto の出力を逐語で貼る）
- **効く先**: （待ち）

### Q3. 個人属性の既定の感度（B / open。推奨: ローカル AI まで）

- **なぜ仮でよいか**: 外へ出る経路が ST19 の時点で無い（外部 AI は ST27、感度の操作は ST24）。既定は後から既存の主張ごと締め直せる
- **当初の案（推奨）**: ローカル AI まで（PERM-4 の主観・感情と揃える）
- **本人の答え**: （待ち）
- **効く先**: `docs/requirements.md` の PERM に 1 行（個人属性の既定を定める行が無い）

### Q4. 同時に 2 つの値を持つ属性（B / open。推奨: 1 種類 1 値。R7 で daily → open）

- **なぜ仮でよいか**: 「いまの値」を導く規則で、主張そのものは変わらない
- **当初の案（推奨）**: 1 種類 1 値。同時に持つ値は種類を分けて書く
- **本人の答え**: （待ち）
- **効く先**: （待ち）

## 聞かないで決めるもの（C —— 扉を開けたままにする既定）

design の D 番号に残す。**異論があれば、答えを貼り戻すときに番号で書いてください。**

- **C1. 主張の行に DB の錠を足す。** 書き換え（値・いつから・種類・訂正先・主張した日時）と行の削除と表の切り詰めを DB が拒む
  （既定は厳しい側。FR-30 が凍結をアプリ層に置かなかった理由と同じ —— 同じ PC の psql やプラグインが素通りする）。
  通すのは感度の列と、Q1 の答えに応じた削除の印・台帳つきの消去だけ。
  `tools/check-immutable.sh` の「本人が書いた記録は書き換えられる」は、**主張以外の**本人が書いた記録（ST17 の主観など）について残す
- **C2. 書くたびに新しい主張として積み、内容で畳まない。** 同じ値・同じ「いつから」をもう一度書いても 1 件増える
  （「その日にまだそうだと確かめた」が残る。捨てるより入れる）。二重送信は**主張ごとに振る識別子**（FR-21）を、
  滞在（ST16 の design D2）と同じく外部識別子の欄に置いて 1 件にする。**同じ識別子で中身が違えば 400 で断り、FR-22 の更新の経路に乗せない**
  （いまの `/ingest` は外部識別子つきで中身の違う記録を由来を見ずに書き換える。`crates/server/src/lib.rs:566-569`。**R9**）。
  実測の根拠（**R6 で直した**）: 内容の鍵には出来事の時刻が入るので、A → B → A と戻った 3 件目は「いつから」が違えば畳まれない。
  畳まれるのは**同じ値・同じ「いつから」を書き直したとき**（`INSERT 0 0` で黙って消える）。
  **これは FR-22（同一の判定は内容の鍵。FR-21 の識別子は判定に使わない）と FR-50（削除済みと同じ内容は取り込まない）の文面を主張について覆す**ので、
  要件へ戻す（**R1 / R2**）
- **C3. 「いつから」は精度ごと持つ**（年 / 年月 / 年月日 / 分からない）。2019 年とだけ分かっている値を 2019-01-01 に丸めない
  （細かい粒度で持つ・丸めない）。「分からない」も値として受け付ける
- **C4. 「主張した日時」は画面が「積む」を押した時刻（出来事の時刻。タイムゾーンつき）とし、本人が入力する欄を置かない。** FR-45 の 2 軸のうち
  「いつそう書いたか」を本人が過去にずらせると、2 軸を分けた意味（後から直したことが分かる）が消える。D-01 に入った時刻（FR-19）も別に残る（**R5 で直した**。当初は「D-01 が受け取った時刻」）
- **C5. 訂正は「どの主張を取り消すか」を指して積む。** 取り消された主張も行は残り、印は訂正した側が持つ（追記のみ）。
  指さずに積むと、「いつから」を直す訂正（2019 年 4 月 → 10 月）で古い開始が導出に残り続ける
- **C6. 未来の「いつから」を受け付ける**（来月から新しい住所）。今日を過ぎるまで「いまの値」にはしない
- **C7. 種類は本人が足せる。** 種類は識別子で持ち、名前を変えても識別子は変えない（扉 #17 と同じ型）。最初に「住所」「職業」を置く
- **C8. 値は本人が打った文字列をそのまま持つ**（NFC。FR-27）。住所を場所（ST21）に結ぶ・構造にするのは後から足す
- **C9. 主張に任意の補足（文）を持たせる。** 「なぜ変わったか」は後から書き足せても、その時点の言葉は戻らない
- **C10. 「なし」（その属性が終わった）も主張として積める。** 置き換える値が無い終わり方（副業をやめた）を表すため
- **C11. 主張も受け取った要求の原文を文字列のまま残す**（FR-18 を主張にも当てる。列を持つ既定）。「いつから」を 1 つの欄から読み取る場合に、
  読み違えても打ったままの文字列から直せる（**R4**）
- **C12. 本文を消したあとに残る列から値を当てられないようにする。** 内容の鍵の入力に主張ごとの識別子を混ぜる
  （いまの鍵は塩の無い SHA-256(ソース, 出来事の時刻, 原文) で、消去は鍵を残す。住所は候補が少ないので総当たりで確かめられる。**R3**）
- **C13. 主張は「本人が書いた」記録として、記録の約束（FR-19 / FR-20 / FR-24〜FR-29 / PERM-2）に乗る。** 出来事の時刻には主張した日時（C4）を入れ、
  **「いつから」は精度つきの別の欄に置く**（出来事の時刻に入れると「分からない」を置く値が要り、その値が鍵に入る）。
  旧設計（`reference/skeleton/entities.md:30`）の「2 軸を event_time / ingest_time と混ぜない」とも揃う。消せるかどうか（FR-50 / FR-51）は Q1 が決める（**R5**）

## 確かめたが問わなかったこと

- **手順 2（扉 #3 の幅）**: 「上書きせず追記」「2 時間軸」は FR-44 / FR-45 と一致し、解釈の幅は Q1（消せるか）と C3（精度）と C5（訂正）に出た。それ以外は無い
- **手順 4（日常）**: 通知・電池・容量に新しい判断は立たない。端末側のコードは変わらない。主張は 1 人で一生に数十〜百件（proto の 10 年後で 32 件）
- **主張をどの表に置くか**: 表の名前は移行できる（扉ではないもの:「テーブル名・正規化の度合い」）。どの要件が主張を縛るかは C13 に置いた（R5）
- **利用者識別子を呼び出し元が名乗れる**（`docs/production-prep.md:167`）: ST29 の担当。ST19 は触らない
- **走っている Story との重なり**: ST04（`device-collection` / `collection-coverage`）と ST16（`derived-records` / `browsing-views`、merge 済み・archive 待ち）は
  capability が重ならない。ST20 / ST21 は同じ `personal-entities` だが上流が始まっていない

## 要件へ戻すもの

- **FR-22** —— 個人属性の主張は内容で畳まず、主張ごとの識別子で同一を判定する（C2。R1）
- **FR-50** —— 削除済みと「同じ内容」の判定を、主張については主張ごとの識別子で行う（C2。R2）。Q1 の答え次第で文面が変わる
- **FR-44 / FR-51** —— Q1 の答えに従う
- **PERM（個人属性の既定の感度）** —— Q3 の答えで 1 行足す

## 当初案を覆したもの

（答え待ち）
