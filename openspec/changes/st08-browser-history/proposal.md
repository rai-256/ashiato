## Why

**Chrome 系のブラウザは、90 日を過ぎた閲覧履歴を手元の DB から削除する**（Chromium の
`history_backend.h`: `kExpireDaysThreshold = 90`「is considered "old" and is deleted」。2026-09-15 確認）。
放置した日数ぶん、閲覧した URL は永久に遡れなくなる。

ST07 のウィンドウの記録は「その瞬間どれを前景に置いていたか」を残すが、**「いつ訪問したか」は残さない** ——
背景のタブで開いたページ・訪問の正確な時刻・遷移の種類（入力した / リンク / 再読み込み）・どこから来たかは、
ブラウザの履歴 DB にしか無い。成功条件 2 の QS-7 / QS-10（いちばん時間を使ったサイト / 関心の移り変わり）が
直接これに乗る。

いま作れる理由: 先行の ST01 と、同じ capability を作った ST07 がどちらも archive 済みで、
`desktop-collection` の正典（`openspec/specs/desktop-collection/spec.md`）に `MODIFIED` を書ける。

## What Changes

深掘りは **1 巡・3 問（すべて A）**。本人が 3 問すべてを選択肢から明示的に選んだ（未回答 0 件・推奨のまま 0 件）。
渡す前の独立レビュー（`deep-review`）は **10 件** —— 問いを 2 問 → 3 問にした（Q3 を新設、Q2 を絞った）。
**レビューが前提の誤りを 1 件見つけた**: 「訪問の番号は再利用されない」は事実と違い、全期間の削除で表が作り直され
番号が 1 から振り直される（R2）。これで「読み直したときの見分け方」を design から問いへ上げた。

- **C-02 が 24 時間ごとに、PC で見つかったブラウザ（Chromium 系 5 つと Firefox）の全プロファイルから履歴を取る**（FR-13）
- **訪問 1 件ごとに 1 件**。訪問時刻は**マイクロ秒**で持ち、ブラウザ・プロファイル・遷移の種類・どこから来たか・滞在時間・
  同期で入った訪問の発生元の印を載せる
- **訪問ごとの識別子で同じ訪問を見分ける**。題名や滞在時間が後から書き換わっていたら**更新して前の版を残す**（FR-22 の経路に乗る）。
  そのため**登録簿の `c02-browser-history` を「記録ごとの識別子がある」に変える**（移行 1 本。このソースの行が 0 件のときだけ当てる）
- **一度取った訪問が次の取得で消えていたら、「消えた」事実を記録として残す**（ashiato からは消さない）。経路の手がかりを載せ、判定はしない
- **除外（FR-83）を履歴にも効かせる**。登録の形に**URL の部分一致**と**ブラウザ + プロファイル**を足す（URL の部分一致はウィンドウの記録にも効く）
- **履歴のソースにも生存信号を送る**（想定間隔 24 時間）。見つかった対象の 1 つでも読めなければ「取得できない」

**BREAKING は無い。** 取り込み口・エンベロープ・受け口の形は変えない。
C-02 が送る本文に `external_id` と `source_updated_at` が載るのは**履歴のソースだけ**で、ウィンドウの記録の形は 1 文字も変えない。

## 深掘りで本人が決めたこと（2026-09-15。1 巡・3 問）

全文は `deep.md`。**下流はこれを勝手に変えない。**

| # | 決めたこと | 段 |
|---|---|---|
| Q1 | **見つかったブラウザの全プロファイル**（Chromium 系 5 つ = Chrome / Edge / Brave / Vivaldi / Opera と Firefox）。取得できる状態かは見つかった対象の 1 つでも読めなければ「できない」（R8）。除外は履歴にも効かせる（R9） | A |
| Q2 | 一度取った訪問が消えていたら**「消えた」事実を記録として残す**（ashiato からは消さない。消したことにするかは ST22） | A |
| Q3 | **訪問ごとに識別子を持たせ、変わっていたら更新して前の版を残す**。識別子はブラウザ・プロファイル・番号・訪問時刻・URL の組（R2）、時刻はマイクロ秒（R5）、ゾーンは取得時の PC のもの + 印（R6）、同期の訪問は発生元の印（R7） | A |

## Capabilities

### New Capabilities

無し。

### Modified Capabilities

- **`desktop-collection`** —— ブラウザ履歴の取得・見分け・消えた事実・同期の印の Requirement を足し、
  除外・生存信号・感度・保持・ログの 5 本を履歴にも及ぶ形に `MODIFIED` する。
  `docs/stories/INDEX.md` の表どおり（ST07, ST08）で、**前倒しは無い**

## Impact

### 触るもの

- `crates/collector-windows/`（履歴の読み手・見分け・消えた事実・2 本目の生存信号・除外の登録の形）
- `migrations/<作成時刻>_browser_history_record_id.sql`（`c02-browser-history` を `record` に。**名前は作成時刻**）と
  `crates/server/src/lib.rs` の `MIGRATIONS` 配列の末尾
- `docs/collector-contract.md`（C-02 の履歴の `payload` の形と識別子の作り方）
- `crates/collector-windows/README.md`（除外の登録の形に 2 つ足す）
- `.github/workflows/ci.yml`（SQLite を読む依存が C のコンパイルを要する場合、windows 向けの型検査に要るもの）

### 走っている Story との重なり

| Story | 状態 | capability | 重なり |
|---|---|---|---|
| ST16 | 下流（`openspec/changes/st16-stay-derivation`） | `derived-records`, `browsing-views` | **無し** |

- `collection-coverage`（生存信号の受け口・NFR-13 の数え方）は**触らない**。受け口は既存の契約のまま送る。
  C-02 の 2 ソースの分母の数え方は `docs/handoff/ST02.md`（st07-active-window R10）で既に申し送られている
- **ST22**（記録を消したことにできる。上流未着手）へ、「ブラウザで消した訪問を ashiato でも消したことにするか」を
  `docs/handoff/ST22.md` に申し送った（review/deep.md R4）

### 要件の改訂

- **FR-13** を訂正（★ 2026-09-15）—— 対象の範囲・訪問ごとの識別子と版・消えた事実
- **EXT-E の注記** を訂正 —— 「削除を保証しない」→ Chromium は 90 日で手元から削除する

### 要件の穴（この change では埋めない）

- **C-02 の未送信に上限が無い**（ST07 から引き継ぐ。FR-8 を C-02 に及ぼすかは ST04）。
  初回の取得で Firefox が何年分も持っていると、未送信が一時的に大きくなる
