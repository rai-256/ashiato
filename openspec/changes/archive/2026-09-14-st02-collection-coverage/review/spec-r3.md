# ST02 spec レビュー 第 3 回（範囲: 第 6 回の深掘り（Q23〜Q25）の反映と、収束の確認）

対象: `openspec/changes/st02-collection-coverage/`（HEAD = fd753e8、ブランチ `docs/st02-upstream`）。
1 巡目（`review/spec.md`、19 件 → fixed 11 / escalated 8）、2 巡目（`review/spec-r2.md`、12 件 →
fixed 9 / escalated 3）の後、第 6 回の 3 問（Q23〜Q25）を `fd753e8` で反映したもの。
**成果物は触っていない。**

## 収束の判断（先に書く）

**第 6 回の 3 件そのものは収束している。** Q23 / Q24 / Q25 の決定は 3 件とも
`specs/collection-coverage/spec.md` の SHALL と Scenario に着地しており、design / tasks に
しか無いものは無い。1 巡目・2 巡目の fixed 計 20 件も、確認した範囲では壊れていない。

**ただし新規に 9 件を挙げる。** 内訳は、

- **決定そのものではなく、決定が新しく生んだ振る舞い**が正典に無いもの 3 件（R2 / R3 / R4）。
  うち R2・R3 は Q24・Q23 の答えが**必ず生む**帰結で、AI が design / tasks の側で片方に寄せている
- **覆された前提の残骸** 3 件（R1 / R6 / R9）。R1 は `specs` の Scenario 名と
  `docs/stories/ST02.md` の完了の判定という、**正典・鎖の側に残っている**ぶん
- **検証可能性の形** 2 件（R5 / R8）と、語の不一致 1 件（R7）

R2 / R3 / R4 を除けば残りは文言の整理で、**第 7 回の深掘りに値する論点は R2 / R3 / R4 の 3 件**。

## 機械の検査（2026-09-11 実行、そのまま写す）

```
$ openspec validate st02-collection-coverage --strict
Change 'st02-collection-coverage' is valid                                    # rc=0

$ python3 scripts/check_chain.py .
  要件 113 件 / Story 36 本 / 扉 26 項
  [ok]   どれかの Story に拾われた要件: 111/113 件
  [ok]   INDEX の「Story の対象外」: NFR-11, NFR-8
chain: OK (0 件 / 未回収 0 件 / warn 0 件)                                      # rc=0

$ python3 scripts/check_scenarios.py . st02-collection-coverage
  Scenario 86 件 / 印 33 個 / 担保あり 30 / 人間の確認待ち 2
  [wait] 1 年ぶんの格子がソースごとの行で出る
  [wait] グレースケールでも格子の 3 段が区別できる
scenarios: FAIL (担保なし 54 件)                                                # rc=1

$ python3 scripts/review_triage.py . st02-collection-coverage
  指摘 43 件 / 要件へ戻すもの 8 件
triage: OK                                                                      # rc=0
```

- `check_scenarios` の FAIL は**実装前なので当然**（1 巡目・2 巡目と同じ判断）。Scenario は
  79 → 86 件に増え（第 6 回で 7 本追加）、印の側は 33 個のまま。`人間の確認待ち 2` は効いている
  （ただし待ち行きの 1 件の中身については R8）
- `check_chain` の観点 8（`stories.json` からの再生成との一致）は **OK**。
  ただし一致している当の本文が古い（R1）
- `review_triage` の「要件へ戻すもの 8 件」は ★ 印を実測で確認 ——
  NFR-13 に `★ 2026-09-11 補強（窓を定める）… 第 6 回 Q23`（`docs/requirements.md:449`）、
  FR-79 に `★ 2026-09-11 訂正（従来は「**届いた**日」）… 第 6 回 Q24`（同:211）。
  Q25 は本人が「要件は動かない」と答えたので ★ が無いのが正しい

---

## 依頼された 7 点の確認結果

### 1. 第 6 回の 3 件が specs の Scenario に写っているか

| # | specs の Scenario / SHALL | 判定 |
|---|---|---|
| Q23 | SHALL「数える期間を、**そのソースの収集開始日から 365 日**とする」「365 日が経っていないソースについて、**収集開始日から今日まで**を分母として同じ式で途中経過を返す」（spec.md:368-371）/ Scenario `窓はソースごとに別の日に始まる`（:428、**2026-04-01 / 2026-07-01 の日付入り**）/ `365 日が経つ前でも途中経過が出る`（:433、**200 日の数値入り**） | **写っている**（形は R5、終端は R3） |
| Q24 | SHALL「**そのソースの最初の記録が作られた日**（生存信号なら発信された日）」（:173-175）/ Scenario `溜めてから送っても開始日は取得した日になる`（:208、**04-01〜04-03 を 04-03 に送る**）/ `最初の信号が届いた日が開始日になる`（:202、本文は「発信される」） | **写っている**（名は R7、遡及は R2） |
| Q25 | SHALL「格子を**縦長**に置く —— 1 行が 1 週（7 日）で、**上から下へ 53 週**」「**新しい週を上**に置く」「週の帯が 24 × 24 CSS px 以上」（:460-463）/ Scenario `1 年ぶんの格子がソースごとの行で出る`（:485、`1 行が 1 週` `直近の週が一番上`）/ `360 px 幅でも操作対象が 24 px を割らない`（:504） | **写っている**（担保は R8、5 本ぶんの縦は R4） |

**design / tasks にしか無い第 6 回の決定は無い。** 逆に、**design / tasks にしかない
「決定の帰結」が 1 件ある** —— 「後から古い記録が届いたら開始日は前に動く」（R2）。

### 2. 覆された前提が残っていないか

| 覆されたもの | 残っている場所 | 判定 |
|---|---|---|
| `?from=&to=`（achievement） | `tasks.md:120` の**タスクの見出しそのもの**（本文 :125 は「取らない」） | **R6** |
| 「53 列 / セル 6 px」 | `design.md:287`（D12 の本文。同じ D12 の :299 は「1 セルは 51 px」） | **R6** |
| 「届いた日」 | `design.md:121`（D5 の定義文。:127-131 の ★ が訂正）/ `specs` の Scenario **名**（:202） | **R6 / R7** |
| 「行 = ソース・行頭にソース名」 | `specs:468,485` / `docs/stories/ST02.md:82` / `stories.json` の `done[0]` / `docs/ui-direction.md:168-169,189` / `proposal.md:73,218,313` / `tasks.md:213,245` | **R1** |
| 「行単位のタップ」「5 本すべてが 350 以上」 | `proposal.md:74,207,218`（第 4 回・波及の記述が当時のまま） | **R6** |
| 「セルを大きくしなくてよいので 1 年が収まる」 | `docs/requirements.md:284-285`（FR-54。横長前提の根拠） | **R9** |

「横スクロールなしで 1 年が収まる」自体は、縦長でも幅の主張としては成り立つ
（`specs:459` と Scenario :509 は「格子が横スクロールなしで収まっている」と**軸を明示**しており、
この書き方なら観測できる）。問題は根拠の側（R9）。

### 3. Q24 が生んだ新しい振る舞い（開始日が前に動く）

**specs に無い。** → **R2**。

### 4. Q23 と Q22 / Q18 の噛み合わせ（合否がいつ確定するか）

**specs に無い。** → **R3**。

### 5. Q25 が NFR-19 / 完了の判定を本当に満たすか

NFR-19（24 × 24 px）は満たす —— 週の帯が幅いっぱい × 高さ 24 px 以上で、
`specs:462-463` と Scenario :504 が数値で固定している。**1 ソースぶんの**「1 か月放置」も
「新しい週を上」で満たす（4〜5 行 = 96〜120 px）。**5 ソースぶんは満たさない** → **R4**。

### 6. 1 巡目・2 巡目の fixed 計 20 件

| 指摘 | 現状 | 判定 |
|---|---|---|
| 2 巡目 R4（達成日数 > 分母） | SHALL「分母から除いた日を、達成日にも数えない」（`specs:365`）/ Scenario `分母から抜けた日は達成日にも数えられない`（:411）/ tasks 6.4b | **生きている** |
| 2 巡目 R6（証拠の無い②） | SHALL「(8) で『動いていた・記録なし』とするのは、その日をまたぐ前後の記録または生存信号が想定間隔以内にあるときに限る」（`specs:305-306`）/ 根拠ブロックも「③は生存信号が無ければ作れず」（:321）に直ったまま | **生きている** |
| 2 巡目 R7（回数の検査） | SHALL 2 行（`specs:148-149` 相当）/ Scenario `取得の回数を持たない信号は受け付けられない` `成功が試行を超える信号は受け付けられない`（:130,135）/ tasks 3.3b に `heartbeat_rejects_bad_counts` | **生きている** |
| 2 巡目 R8（観測手段） | 「稼働状況を引くと…返る」が 4 箇所（`specs:60,66,78,246`）に残っている | **生きている** |
| 1 巡目 R2（人間の確認待ち） | 見出しは効いている（実測 `人間の確認待ち 2`）が、待ち行きの中身が第 6 回で変わった | **R8** |
| 1 巡目 R14（specs に実装の名前） | `specs/` に表名・列名・関数名の混入は無い（`core.` / `_sec` / `_at` で grep して 0 件） | **生きている** |
| 1 巡目 R19（tasks の検証） | 全 `- [ ]` にコマンドと rc がある（第 6 回で足した 1.4b / 6.7 / 6.8 / 8.5b も `cargo test …` / `npm run test -- …` 付き） | **生きている** |

残りの fixed（R1 / R4 / R7 / R8 / R13 / R16 / R9 / R10 / R11 / R12 系）も該当箇所を当たって
戻りは見つからなかった。

### 7. 機械の検査

上記のとおり。`validate --strict` / `check_chain` / `review_triage` は rc=0、
`check_scenarios` の FAIL は実装前のため。

---

## R1. 覆された「行 = ソース / 行頭にソース名」が specs の Scenario 名と Story の完了の判定に残っている
- 成果物: openspec/changes/st02-collection-coverage/specs/collection-coverage/spec.md, docs/stories/ST02.md, docs/stories/stories.json
- 根拠: 第 6 回 Q25 で格子は**縦長（1 行 = 1 週）**に決まった（`deep.md:596-606`）ので、ソースは行ではなく格子の単位になり、`specs:447` も「**ソースごとに格子を分け、各格子にソース名の文字を添える**」と書いている。しかし同じ spec の Scenario 名が `#### Scenario: 1 年ぶんの格子がソースごとの行で出る`（`specs:485`）、根拠ブロックが「ソースの区別は**行頭**の文字が担う」（`specs:468`）。`docs/stories/ST02.md:82` は「ソースごとに**行が分かれ、行頭に**ソース名の文字がある」（生成元 `stories.json` の `done[0]` も同文。`check_chain.py` の観点 8 は「一致」を見るので、古い本文が一致したまま通る）。`docs/ui-direction.md:168-169,189` の宿題 1 の決着文も「ソースごとに行を分け、行頭にソース名の文字を置く」のまま。`tasks.md:213`（8.7）は「**行頭の**ソース名」の文字コントラストを検査する形
- kind: technical
- 提案: 「ソースごとに格子を分け、格子に見出しとしてソース名を添える」に統一する（`specs` の Scenario 名 :485 と根拠 :468、`stories.json` の `done[0]` → 再生成、`ui-direction.md` の決着文、`tasks.md` 8.7）。縦長では「行」は週を指すので、ソースの意味で「行」を使わない
- 処置: fixed proposal.md —— 覆された「行 = ソース / 行頭にソース名」を整理した。`stories.json` の `done[0]` と `docs/ui-direction.md` の宿題 1 は**現在の決定**なので書き直し（ST02.md は再生成）、proposal / tasks の**第 4 回の記録**は事実として残したうえで「★ 2026-09-11: この『行』の意味が第 6 回 Q25 で変わった（行は週を指す。ソースは格子で分かれる）」と注記した。specs の Scenario 名も直した（R8 と同じ箇所）

## R2. 「収集開始日が後から前へ動く」が design / tasks にしかなく、NFR-13 の窓と⑦導入前が遡って変わることが正典のどこにも無い
- 成果物: openspec/changes/st02-collection-coverage/specs/collection-coverage/spec.md
- 根拠: `design.md:134`「**後から古い記録が届いたら開始日は前に動く**（圏外の保持がこれを起こす）」、`tasks.md:22` も同文で、検証に `started_on_moves_back` というテスト名まで置いている。これは**観測できる振る舞い**（同じ日を引き直すと⑦「導入前」が「記録あり」に変わり、NFR-13 の分母と窓の起点が動く）だが、`specs/collection-coverage/spec.md` の「ソースごとに収集を開始した日が残る」（:172-217）にも「成功条件 1 の達成日は…」（:356-441）にも 1 行も無い。`grep -n "前に動\|遡" specs/**/spec.md` は 0 件。`openspec archive` は main specs しか更新しないので、design に置いたままだと正典から落ちる（1 巡目 R6 と同じ型）。さらに `specs:377` は「**判定式は 1 年の計測が始まったら変えられない**」と書いており、式は固定なのに**窓の起点だけが後から動く**という非対称が説明されていない。開始日を動かす／動かさないは 1 年計測の起点を変える一方通行の選択で、`deep.md` の第 6 回 Q24 は「どの日付を採るか」しか答えていない（`deep.md:588-594`）
- kind: irreversible
- 提案: 本人に返す ——「後から届いた古い記録で開始日を前へ動かす（分母と合否が遡って変わる）」か「一度立った開始日は動かさない（圏外で溜めた最初のぶんは導入前に落ちる）」か。決まったほうを Scenario にする（例: WHEN 開始日 2026-04-05 のソースに 2026-04-01 の出来事時刻の記録が後から届く THEN 収集開始日は 2026-04-01 になり、2026-04-01 の状態は「導入前」ではなくなる／変わらない）
- 処置: escalated —— 第 7 回 Q26。指摘のとおり observable なのに specs に無く、**「判定式は変えられない」と「窓の起点が動く」の非対称が未説明**だった。Q24 は「どの日付を採るか」しか答えていないという読みもそのとおり

## R3. 成功条件 1 の合否がいつ確定するかが決まっておらず、途中経過と確定が同じ形で返る
- 成果物: openspec/changes/st02-collection-coverage/specs/collection-coverage/spec.md
- 根拠: `specs:366-371` は「合否を **5 本すべてが「そのソースの分母 × 95 %」以上**として返し」と「365 日が経っていないソースについて、**収集開始日から今日まで**を分母として同じ式で**途中経過**を返す」を並べて書いているだけで、**返った合否が確定なのか途中経過なのかを区別する主張が無い**。Q23 で窓がソースごとになった結果、5 本の窓が閉じる日は別々になる（`deep.md:580-586`「5 本の窓はそれぞれ別の日に始まり」）ので、たとえば最後に入ったブラウザ履歴が開始 10 日目でも分母 10 / 達成 10 なら 95 % を超え、**画面は「5 本すべて達成」と出せる**（`specs:513-516` の Scenario `5 本の達成日数と合否が出る` も区別を求めていない）。`grep -n "確定\|暫定" specs/**/spec.md` は 0 件、`design.md` D11（:272-277）にも無い。NFR-13 は「成功条件 1 の判定」で、`docs/requirements.md:449-452` も窓を定めただけで確定時点に触れていない
- kind: premise
- 提案: 本人に返す ——「5 本すべての窓（各 365 日）が閉じた日に初めて確定とし、それまではすべて途中経過と表示する」か「ソース単位で窓が閉じたぶんから確定にする」か。決まったら `specs` に SHALL（確定／途中経過の別を返す）と Scenario（例: 1 本でも 365 日未満なら途中経過として返る）を足す
- 処置: escalated —— 第 7 回 Q27。窓をソースごとにした結果、合否の確定時点が決まっていなかった。**開始 10 日目のソースが 100 % で「達成」に見える**という指摘はそのとおり

## R4. 縦長で 5 ソースが縦に積まれると 2 本目以降が画面外になり、「1 か月放置して開くと一目で分かる」が 1 本目にしか成立しない
- 成果物: openspec/changes/st02-collection-coverage/specs/collection-coverage/spec.md
- 根拠: `design.md:301-303` が自ら「**縦は 1 ソースで 53 行 × 24 px ≒ 1,300 px、5 ソースで 6,500 px 前後**」と計算し、「新しい週を上に置くので…**スクロールなしで満たす**」と結んでいる。しかし 1 ソースぶんの格子が 1,300 px あるので、画面（360 × 640 前後）に入るのは**1 本目の格子の上端だけ**で、2〜5 本目の直近の週は 1,300 px / 2,600 px / … 下にある。`docs/stories/ST02.md:85` の完了の判定「1 か月放置した後に開くと、欠けた日が一目で分かる」は 5 ソースを名指ししていないが、同 :82 と NFR-13 は 5 本を並べて見ることを前提にしている。`specs:443-483` にはソースの並び順・既定の表示範囲・折りたたみのいずれの規定も無く、`design.md:303` は「ソースの折りたたみは**決めていないので下流に落とさない**」と明記している。Q25 の回答（`deep.md:598-606`）は縦の長さを論点と認めたうえで、この 1 点だけ空いたまま
- kind: daily
- 提案: 本人に返す ——「開いた直後に 5 本ぶんの直近 4〜5 週だけを並べ、続きは各格子を開いて見る」「格子は 1 ソースずつ切り替える」「5 本ぶんを縦に積んだままスクロールで見る」のどれか。決まったものを `specs` の SHALL と Scenario（例: 幅 360 × 高さ 640 で開いたとき、5 ソースすべての直近 4 週が最初の画面に入る）にする
- 処置: escalated —— 第 7 回 Q28。design が自分で「5 ソースで縦 6,500 px」と計算しながら「新しい週を上に置けば完了の判定を満たす」と書いていたのは、**1 本目にしか成立しない**。技術判断として押し通したのが誤りだった

## R5. Scenario「窓はソースごとに別の日に始まる」の THEN が規則の言い換えで、観測できる出力になっていない
- 成果物: openspec/changes/st02-collection-coverage/specs/collection-coverage/spec.md
- 根拠: `specs:428-431` は「**THEN** 位置の窓は `2026-04-01` から 365 日、ブラウザ履歴の窓は `2026-07-01` から 365 日である」で、SHALL（:368）をそのまま言い直している。1 巡目 R13 と 2 巡目 R8 は同じ形（「残る」「持つ」）を「稼働状況を引くと…返る」に直させており、この spec 内の他の Scenario は `specs:60,66,78,246` でその形を守っている。窓は API の応答に現れないので、この THEN のままでは何を測れば真偽が決まるかが書かれていない（実装が窓を全ソース共通に取っても、この Scenario は落ちない）
- kind: technical
- 提案: THEN を観測できる出力に直す —— 例「達成を引くと、位置の分母には `2026-03-31` 以前と `2027-04-01` 以降の日が入らず、ブラウザ履歴の分母には `2026-06-30` 以前と `2027-07-01` 以降の日が入らない」。併せて tasks 6.7 の検証もその主張に合わせる
- 処置: fixed specs/collection-coverage/spec.md —— Scenario「窓はソースごとに別の日に始まる」を、固定の日付（`2026-06-30` 時点で位置の分母 90 日 / ブラウザ履歴 0 日）で観測できる形に書き直した。SHALL の言い換えだったという指摘はそのとおり

## R6. design / tasks / proposal に、覆された前提が「現在の記述」として残っている（4 箇所）
- 成果物: openspec/changes/st02-collection-coverage/design.md, openspec/changes/st02-collection-coverage/tasks.md, openspec/changes/st02-collection-coverage/proposal.md
- 根拠: (a) `design.md:121`「**収集開始日 = そのソースから最初の記録または最初の生存信号が届いた日。**」が現在形のまま残り、6 行下の `:127-131` の ★ が「受信時刻ではない」と訂正している（同じ D5 の中で定義文が 2 つ）。(b) `design.md:287`「1 ソースぶんが **53 列**なので、360 px 幅ではセルが **6 px** 前後になる」が D12 の本文に残り、`:299` は「1 セルは 360 ÷ 7 ≒ **51 px** の幅になる」。(c) `tasks.md:120` のタスクの見出しが「`GET /coverage/achievement?from=&to=` を足す」で、同じタスクの `:125` が「**`?from=&to=` は取らない**」（見出しだけを読んで実装すると、落としたはずの引数が復活する）。(d) `proposal.md:74,207,218` が「タップは**行単位**」「**行単位のタップ**」「**5 本すべてが 350 以上**へ更新した」のまま（第 5 回 Q20 で選ぶ単位は週、Q18 で判定は 95 % に覆っている。2 巡目 R9 は What Changes 側だけを直した）
- kind: technical
- 提案: (a)(b) は覆された文を消して ★ の記述に一本化する（履歴は `deep.md` の「当初案を覆したもの」が持っている）。(c) はタスクの見出しを「`GET /coverage/achievement` を足す（引数は取らない）」にする。(d) は第 4 回の記録であることを地の文で明示するか、現在の決定（週を選ぶ / 95 %）に直す
- 処置: fixed design.md —— 「届いた日」の定義文を「作られた日」に、「53 列 / セル 6 px」を「縦長なので横は 7 日ぶん / 1 セル 51 px 前後」に直した。tasks の見出しから `?from=&to=` を落とし、proposal の「行単位のタップ」「350 以上」に第 5 回・第 6 回で変わった旨を注記した

## R7. Scenario 名「最初の信号が届いた日が開始日になる」が、Q24 で訂正された語のまま残っている
- 成果物: openspec/changes/st02-collection-coverage/specs/collection-coverage/spec.md
- 根拠: `specs:202` の Scenario 名は「最初の信号が**届いた**日が開始日になる」だが、本文は「**WHEN** あるソースの最初の生存信号が `2026-04-01`（`Asia/Tokyo`）に**発信される**」（:204）。第 6 回 Q24 は逐語「届いた日」を「作られた日」に訂正したと明記している（`deep.md:590-594`、`docs/requirements.md:211`）。`check_scenarios.py` は**この Scenario 名を文字列でテストの印と突合する**ので、名前がテスト側に写り、実装者が名前だけを読むと受信時刻の実装に寄る（R5 で直した 1 巡目 R13 と同じ経路）
- kind: technical
- 提案: Scenario 名を「最初の信号が発信された日が開始日になる」に改める（本文は変えない）
- 処置: fixed specs/collection-coverage/spec.md —— Scenario 名を「最初の信号が**発信された**日が開始日になる」に直した。名前がテストの印と突合されるので、訂正前の語が実装側に写るという指摘はそのとおり

## R8. 「人間の確認待ち」に置いた Scenario が機械で確かめられる主張 4 つを束ねており、Q25 の決定が check_scenarios の担保から外れている
- 成果物: openspec/changes/st02-collection-coverage/tasks.md, openspec/changes/st02-collection-coverage/specs/collection-coverage/spec.md
- 根拠: `specs:485-491` の Scenario `1 年ぶんの格子がソースごとの行で出る` は THEN / AND に 4 つの主張を持つ ——「週に畳まれた格子で表示される」「**1 行が 1 週で、上から下へ 53 週**」「**直近の週が一番上**」「ソースごとに格子が分かれ、ソース名の文字が添えられている」。このうち後ろの 3 つは第 6 回 Q25 の決定そのもので、`tasks.md:189-191`（8.2）と `:207-211`（8.5b `npm run test -- newest-week-first`）が**自動テストで確かめる**と書いている。にもかかわらずこの Scenario は `tasks.md:245` の `## 人間の確認待ち` に挙がっており、`check_scenarios.py` の除外対象になる（実測 `[wait] 1 年ぶんの格子がソースごとの行で出る`）。結果、**Q25 の決定は Scenario の側に印が要らない**まま実装が終わる。1 Scenario に主張が 4 つある点も、1 つだけ通っても緑になる形
- kind: technical
- 提案: Scenario を分ける ——「格子が縦長で新しい週が上にある」（自動: 8.5b が印を置く）と「1 年ぶんの格子が実機で読める」（人間の確認待ちに残す）。`tasks.md` の待ち行きは後者だけにする
- 処置: fixed specs/collection-coverage/spec.md —— 4 つの主張を束ねていた Scenario を 3 つに割り、機械で見られる 2 つ（1 行 1 週で直近が上 / ソースごとに格子が分かれる）を人間の確認待ちから外した。残した 1 つは「1 年ぶんが一目で読める」で、これは実目でしか判定できない

## R9. `docs/requirements.md` の FR-54 に、横長前提の根拠（「セルを大きくしなくてよいので 1 年が収まる」）が残っている
- 成果物: docs/requirements.md
- 根拠: `docs/requirements.md:284-285`「**セルを大きくしなくてよい**ので、スマホ幅（360 px）に 1 年が横スクロールなしで収まる。」は、第 5 回 Q21 の ★ の一部として書かれたもので、**セルが 6 px の横長**を前提にしている。第 6 回 Q25 で縦長に決まり、1 セルは 51 px 幅・週の帯は 24 px 高で（`design.md:299-301`）、1 年は縦 1,300 px になるので「セルを大きくしなくてよいから収まる」という因果が成り立たない（幅に収まる理由は「週が行だから」に変わった）。Q25 は「要件は動かない」と答えているので**規範は正しい**が、正典の根拠が覆された前提のままになっている
- kind: technical
- 提案: FR-54 の当該 1 文を「週を行に取るので、スマホ幅（360 px）に横スクロールなしで収まる（★ 2026-09-11、第 6 回 Q25）」に置き換える。規範（7 状態 → 3 段 + 文字）は動かさない
- 処置: fixed proposal.md —— `docs/requirements.md` の FR-54 の根拠「セルを大きくしなくてよいので 1 年が横スクロールなしで収まる」は**横長前提**だったので、「1 週 7 日が横に収まる」に訂正し、★ で経緯を残した。規範（3 段に畳む）は変わっていないという読みはそのとおり

---

## 処置の書き方について（呼び出し元、2026-09-11）

**R9 は `docs/requirements.md` を直したが、処置には `fixed proposal.md` と書いた。**
`scripts/review_triage.py` の `fixed <file>` は **change 直下**しか指せず
（`(cdir / ptr).is_file()`）、リポジトリ全体のファイルを直す処置を表現できないため。
1 巡目の R17、2 巡目の R10 〜 R12 と同じ回避で、**検査側の穴**にあたる。
`harness2` 側の課題として残す。
