## Why

「データが無い」の意味が、いまは誰にも分からない。動きが無かったのか、収集が壊れていたのか、
自分で止めたのか、端末のバッファから破棄されたのか —— **扉 #14 はこれを区別する唯一の手段として
稼働記録を持つと決めている**が、現在の実装は取り込みと同時にしか書かないので、
記録が 0 件の日は行が無いだけで原因が残らない。

いま作る理由は 2 つある。**遡れないから**と、**成功条件 1 がこれを土台にしているから**。
生存信号を入れなかった期間は永久に判別できず、NFR-13（365 日中 350 日以上）は
稼働記録の集計そのものを判定に使う。1 年の計測が始まってからでは、判定式も記録の粒度も変えられない。

## What Changes

- **`collection-coverage` に 7 状態を積む。** ソース × 日 の稼働状況を持つ
  （capability 自体は **ST01 の下流が前倒しで作成済み**。下の「Capabilities」の訂正を見ること）
- **生存信号（heartbeat）を導入する。** 収集側が定期的に「動いている」ことと
  「取得できる状態か（権限・センサ・接続）」を送る。**記録が 0 件でも稼働が残る**
- **日境界を `Asia/Tokyo` に固定する（BREAKING）。** 現在は UTC 固定
  （`crates/server/src/lib.rs` の `($2 AT TIME ZONE 'UTC')::date`）。ST01 が ST02 の担当として申し送っていた
- **停止と破棄を時刻の範囲で持つ（BREAKING）。** 現在の `core.coverage` は
  日単位 + 自由文の `note` しか無く、FR-34 / FR-9 の「期間」を満たしていない
- **NFR-13 の達成日判定をソースの主語で分ける。** 端末が主語（位置・アプリ利用）は
  「記録が 1 件以上ある日」、利用が主語（写真・ウィンドウ・ブラウザ履歴）は
  **「取得できる状態の生存信号があった日」**。
  合否は **5 本すべてが分母の 95 % 以上**（★ 2026-09-11 の第 5 回 Q18 で、
  絶対値の「350 以上」から割合に変わった）
- **収集開始日を持ち、途絶をサーバ側で能動的に検知する（FR-79 / FR-80 を新設）。**
  「導入前」と「収集が壊れていた」を分ける唯一の手段。生存信号では埋まらない
  （止まったものは自分から報告しない）
- **稼働状況の画面（S-1）を出す。** 1 年を週に畳んだ格子。**ソースごとに格子を分け、
  ソース名の文字を添える**。**格子のセルが担う区別は 3 段**（記録あり / 動いていた・記録なし /
  それ以外）で、**7 状態の区別は週を選んだときの文字**が担う
  （★ 2026-09-11 の第 5 回 Q20 / Q21。7 段の明度は計算で不成立が確定した）。
  セルは表示専用。360 px 幅でも NFR-19 の 24×24 px を割らない。
  5 ソースそれぞれの達成日数と分母を数値で併記
- **FR-78（生存信号）を要件として新設し、NFR-13 の判定式を書き換える**
  （下の「要件へ戻すもの」。2026-09-10 実施済み）

## 深掘りで本人が決めたこと（2026-09-09）

`deep.md` が正典。**下流は勝手に変えないこと。**

| 論点 | 本人の答え | なぜ人間の判断か |
|---|---|---|
| 記録が 0 件の日の意味 | **収集側が定期的に「生きている」信号を送る** | 要件の衝突かつ不可逆。遡って埋められない |
| 「その日」の区切り | **固定のタイムゾーン（`Asia/Tokyo`）で全部区切る** | 不可逆。NFR-13 の分母の単位 |
| 停止と破棄の粒度 | **時刻の範囲で持ち、1 日を丸ごと覆う停止だけ分母から除く** | 不可逆。粗い記録は後から細かくできない |
| NFR-13 の達成日 | **ソースごとに判定を分ける**（毎日届く 4 つは「記録あり」、写真は「生存信号あり」） | 要件どうしの衝突 |
| 生存信号が保証するもの | **取得できる状態か（権限・センサ・接続）も載せる** | 不可逆。権限が剥がれた期間は後から特定できない |
| 稼働状況の表 | **1 年を週に畳んだ格子。明度の段差 + セルを選ぶと文字** | 日常に影響。ui-direction が名指しで送ってきた宿題 |

**当初案を覆したもの 2 件**: 日境界（UTC → `Asia/Tokyo`）/ 表の期間（直近 14 日 → 1 年）。
3 件目に挙げていた「ui-direction 宿題 1 を明度の段差で読み替えた」は
**2026-09-10 に取り消した**（`review/deep.md` の R10。1.009:1 はソースを分ける 6 色の輝度差で、
稼働の状態の色ではなかった）。宿題 1 は第 4 回 Q15 の答え（**ソースごとに行を分け、
行頭にソース名の文字を置く**）で閉じた。

## 深掘り 第 4 回で本人が決めたこと（2026-09-10）

回答済みだった deep を `deep-review` にかけ直したところ 12 件出て、**10 件が人間に返すもの**だった。
全問回答済み。**下流は勝手に変えないこと。**

| 論点 | 本人の答え | なぜ人間の判断か |
|---|---|---|
| 生存信号の達成日に取得可否を要るか | **取得できる状態の生存信号があった日だけ達成** | 要件の衝突かつ不可逆。判定式は計測開始後に変えられない |
| ウィンドウとブラウザ履歴の判定型 | **5 ソースを「端末が主語 / 利用が主語」で仕分ける** | 同上。放置すると正常でも未達で固定される |
| 5 ソースの畳み方 | **5 本すべてが 350 以上** | 不可逆。成功条件 1 の合否そのもの |
| 生存信号と Doze | **ST01 の R46 の答えに従う**（ST02 では決めない） | 日常に影響（電池）かつ不可逆 |
| 状態⑥を割るか | **収集開始日を持ち、かつサーバ側で途絶を能動的に検知する** | 不可逆。day one から列が要る |
| 写真・ウィンドウの想定間隔と FR-35 | **「最後の記録 または 最後の生存信号」で測る** | 毎日鳴りうる通知。かつ FR-78 と FR-35 が噛み合っていなかった |
| 生存信号の保護 | **`core.event` と同じ保護**（書き換え禁止・冪等キー・原文の素通し） | 不可逆。保護が無かった期間のデータは後から信用できない |
| 生存信号の時刻粒度 | **受信時刻を `timestamptz` で持ち、日は集計のときに引く** | 不可逆。丸めると日境界の決定が行に焼き込まれる |
| 格子でのソースの分け方 | **ソースごとに分け、ソース名の文字を添える** | 日常に影響。ui-direction 宿題 1 の未処置分 |
| スマホ幅と NFR-19 | **タップは行単位で受け、セルは表示専用** | 日常に影響。NFR-19 の 24×24 px |

> **★ 2026-09-11: この 2 行の「行」の意味が、第 6 回 Q25 で変わった。**
> Q15 / Q16 を書いた時点では「行 = ソース」だったが、Q25 で格子が**縦長**に決まり、
> **行は週**を指すことになった。ソースは行ではなく**格子**で分かれ、タップを受けるのは週。
> 決定そのもの（ソースを文字で区別する / セルは表示専用）は変わっていない。

**第 4 回で当初案を覆したもの 4 件**: NFR-13 の仕分け（「毎日届く 4 つ / 写真」→ 主語で 2 分）/
生存信号の達成条件（「信号があった日」→ 取得できる状態の信号があった日）/
状態の数（**6 → 7**）/ 生存信号の粒度と保護（「技術判断として問わない」→ 本人が決めた）。

**Q10 は ST01 待ちだった。★ 2026-09-11 に解けた。** R46 は **B（Doze の除外を要求せず、
受け入れるが欠損を稼働記録に残す）** で決着した。生存信号の送出方式はこれに従い、
**ST02 の tasks で決め直さない**（`tasks.md` の 7.1 に明記）。
実測で Doze の維持間隔は最長 14.2 分、想定間隔は 6 時間なので**生存信号は Doze の中でも間に合う**。

ただし **B のうち「空きの理由を残す」は ST02 の設計で埋まらなかった** ——
生存信号（6 時間間隔）より細かい時間の話で、日単位の状態にも乗らない。
**後から作れない**ので独断で決めず、**深掘り 第 5 回の Q17 として本人へ返した**。

## 深掘り 第 5 回で本人が決めたこと（2026-09-11）

**specs / design / tasks を書き切ってから独立レビューにかけたら、19 件のうち 8 件が
人間に返すものだった** —— そのうち **5 件は AI が独断で決めていた一方通行の判断**。
6 問すべて回答済み。**下流は勝手に変えないこと。**

| 論点 | 本人の答え | なぜ人間の判断か |
|---|---|---|
| 日内の空きの「理由」 | **生存信号に取得の試行回数と成功回数を載せる** | 不可逆。載せなかった期間の取得率は作れない |
| 成功条件 1 の閾値 | **分母の 95 % 以上**（絶対値の 350 ではない） | 要件の衝突かつ不可逆。判定式は計測開始後に変えられない |
| 状態の優先順位 | **丸ごと覆う停止・破棄を記録より優先する** | 不可逆。画面に出るものが変わる |
| 格子の選択単位 | **週**（7 日ぶん） | 日常に影響。毎日開く画面の使い勝手 |
| 格子が担う段数 | **3 段**。7 状態は週を選んだときの文字で読む | 要件の衝突。**7 段は計算で不成立**（729:1 必要 / 上限 21:1） |
| 収集開始日 | **最初の記録か生存信号が届いた日**。届くまでは開始していない | 不可逆。1 年計測の起点 |

**第 5 回で当初案を覆したもの 5 件**: NFR-13 の判定（絶対値 → 割合）/ 格子の段数（7 → 3）/
格子の選択単位（「週 or ソース」の 2 択 → 週）/ 収集開始日（AI の独断 → 最初の到着）/
生存信号が運ぶもの（+ 取得の試行と成功）。

**Q19 だけは AI が置いた順序と同じ結論になったが、根拠が入れ替わった** ——
「消えたことを出す方が失うものが少ない」（AI の後付け）ではなく
「なぜこの日はデータが少ないのかに画面が先に答える」（本人）。

### 要件へ戻すもの（第 5 回。**2026-09-11 実施**）

| 要件 | 何を | 由来 |
|---|---|---|
| **FR-54** | 7 状態の区別の担い手を「格子の 3 段 + 週を選んだときの文字」にする | Q21 |
| **FR-78** | 生存信号に取得の試行回数と成功回数を載せる | Q17 |
| **FR-79** | 収集開始日を「最初の記録か生存信号が届いた日」と定める | Q22 |
| **NFR-13** | 判定を**分母の 95 % 以上**にする | Q18 |
| **NFR-23（新設）** | 意味を担う非テキストのコントラストを 3:1 以上（WCAG SC 1.4.11 / EXT-J） | Q21 の根拠が要件に無かった（レビュー 2 巡目の R10） |

**NFR-23 は `docs/stories/ST25.md` の `satisfies` に入れた** —— UI 系の非機能
（NFR-17〜NFR-22）はすべて ST25（S-2 主表現）が代表して持つ設計になっているため。
**ST02 がそれを先に使う**のは、ST02 が実質的に最初の画面だから ——
上の「要件の前倒し」の (a) と同じ型で、`satisfies` は動かさない。

**あわせて NFR-13 の重複行を 1 つに戻した**（第 5 回の追記で「判定式は 1 年の計測が
始まったら変えられない」が 2 行になっていた。レビュー 2 巡目の R12）。

## 深掘り 第 6 回で本人が決めたこと（2026-09-11）

第 5 回の答えを反映したものを**もう一度独立レビューにかけた**（`review/spec-r2.md`、12 件）。
9 件は直し、**3 件が人間の領分**だった。全問回答済み。**下流は勝手に変えないこと。**

| 論点 | 本人の答え | なぜ人間の判断か |
|---|---|---|
| 成功条件 1 の窓 | **ソースごとに「収集開始日から 365 日」** | 要件の衝突かつ不可逆。窓の取り方で合否が動く |
| 収集開始日の日付 | **最初の記録が「作られた」日**（届いた日ではない） | 不可逆。1 年計測の起点 |
| 格子の向き | **縦長**（1 行 = 1 週、新しい週が上） | 日常に影響。向きで NFR-19 を満たせるかが変わる |

**第 6 回で当初案を覆したもの 2 件**: 収集開始日の日付（第 5 回の逐語「**届いた**日」→
**「作られた」日**。受信時刻を採ると圏外で溜めた記録のある日が「導入前」になる）/
格子の向き（design が横長を前提に書いていた → **縦長**）。

**Q25 は問いの側の誤りから出た** —— 第 5 回 Q20 の選択肢に「行 = 週」と
「GitHub の草に近い形」を並べて書いたが、**草では週は列**で両立しない。

### 要件へ戻すもの（第 6 回。**2026-09-11 実施**）

| 要件 | 何を | 由来 |
|---|---|---|
| **NFR-13** | 窓を「そのソースの収集開始日から 365 日」と定める | Q23 |
| **FR-79** | 収集開始日を「**作られた**日」に訂正する | Q24 |

**あわせて `GET /coverage/achievement` から `?from=&to=` を落とした** ——
窓が仕様で決まったので、呼び出し側に委ねると合否が動く。

**3 巡目のレビューで FR-54 の根拠も直した** —— 「セルを大きくしなくてよいので
**1 年が**横スクロールなしで収まる」は**横長**を前提にした根拠だったので、
「**1 週 7 日が**横に収まる」に訂正した（規範である「3 段に畳む」は変わっていない）。

## 深掘り 第 7 回で本人が決めたこと（2026-09-11）

3 巡目の独立レビュー（`review/spec-r3.md`、9 件）。6 件は覆された前提の残骸で直し、
**3 件は第 6 回の答えがそれぞれ 1 つずつ生んだ新しい振る舞い**だった。
どれも AI が独断で決めていた。全問回答済み。**下流は勝手に変えないこと。**

| 論点 | 本人の答え | なぜ人間の判断か |
|---|---|---|
| 収集開始日が遡るのを許すか | **動かす**（常にいちばん古い記録の日） | 不可逆。過去に表示した達成率が変わる |
| 合否がいつ確定するか | **5 本すべての窓が閉じた日**。それまでは暫定 | 要件の衝突。そのままだと開始 10 日目が「達成」に見える |
| 縦長で 2 本目以降をどう見せるか | **開いた直後は各ソースの直近 4〜5 週**。1 年は伸ばす | 日常に影響。完了の判定が 1 本目にしか成立していなかった |

**第 7 回で当初案を覆したもの 2 件**: 格子の初期表示（AI の技術判断「新しい週を上に置けば
完了の判定を満たす」→ **直近 4〜5 週だけを出す**。前者は 1 本目にしか成立しなかった）/
合否の扱い（暗黙の「常に現時点の割合」→ **確定と暫定を区別する**）。

**Q26 は覆したのではなく、明示した** —— design には「後から古い記録が届いたら開始日は
前に動く」と書いてあったが、**それでよいと誰も決めていなかった**。

### 要件へ戻すもの（第 7 回。**2026-09-11 実施**）

| 要件 | 何を | 由来 |
|---|---|---|
| **FR-79** | 収集開始日は**いちばん古い記録**の日。古い記録が後から届けば遡る | Q26 |
| **NFR-13** | 窓の起点が遡ること / **合否は 5 本すべての窓が閉じた日に確定**し、それまでは暫定 | Q26 / Q27 |

**FR-79 は SHALL を 1 つに保つため本文を書き直した** —— 「いちばん古い記録が作られた日」と
書けば遡りが含意されるので、`check_chain.py` の「SHALL が 2 つ以上ある FR」の warn が消える。

## レビューの巡回（4 巡。**収束した**）

上流の成果物を `spec-review` に**独立に 4 回**かけた。**4 巡目で人間に返すものが 0 件になり、
第 7 回の答えが新しい振る舞いを生んでいないことが確認された。**

| 巡 | 指摘 | fixed | 人間へ | そこから起きた深掘り |
|---|---|---|---|---|
| 1 | 19 | 11 | 8 | 第 5 回 Q17〜Q22 |
| 2 | 12 | 9 | 3 | 第 6 回 Q23〜Q25 |
| 3 | 9 | 6 | 3 | 第 7 回 Q26〜Q28 |
| 4 | **6** | **6** | **0** | —— |

**3 巡連続で「答えが次の問いを生む」が起きていた。** 4 巡目はその経路を 3 つ立てて潰している ——
(1) 開始日は `least()` で前にしか動かないので**閉じた窓が開き直らない**（確定は単調）、
(2) 遡った日の状態は既存の決定順序の再評価で済み新しい規則を要さない、
(3) NFR-19 は全操作対象に掛かるので「伸ばす」にも効く。

**人間に返した合計 14 件のうち、10 件は AI が独断で決めていた一方通行の判断**だった
（7 状態の決定順序 / 収集開始日のバックフィル / 格子の行の定義 / セルの実寸 /
NFR-13 の分母と閾値の関係 / 窓の起点 / 開始日の日付 / 格子の向き /
開始日の遡り / 格子の初期表示）。**器が無ければ全部が黙って決まっていた。**

## 要件へ戻すもの（**2026-09-10 実施済み**）

深掘りで、**要件の側を直さないと辻褄が合わない**ものが 2 件出た。
`docs/requirements.md` の改訂として扱う。

> **訂正（2026-09-10）**: 当初この節は「**この PR ではまだ直さない**。要件の文と spec の delta は
> 同じ変更で書く（別々に書くと片方だけが動いて食い違う）。ST01 が merge されて `specs` を
> 再開するときの最初の作業にする」と**先送りしていた。その先送りを取り消して、ここで戻した。**
>
> **理由**: 先送りが実際に何を起こしたかが測れた —— **ST02 の deep が FR-33 と NFR-13 の改訂を
> 挙げたのに、要件は変わらず、Story にも痕跡が残らなかった。** `docs/stories/ST<NN>.md` は
> 要件の本文を**逐語で引く**生成物なので、要件が古いまま次の Story が立つと、
> **その Story は覆された前提を正典として持ち回る。** ST14（FR-35 の通知）と ST15（FR-34 の停止）は
> どちらも ST02 を `requires` に持ち、NFR-13 の分母と生存信号の上に乗る。
>
> 「同じ変更で書く」という当初の懸念は、**逆向きに効く方が大きかった** ——
> 要件と spec がずれる危険より、**要件が古いまま下流が増える危険**の方が先に来る。
> `deep` の工程に「答えを要件と Story に戻す」を最後の手順として足し、
> `scripts/review_triage.py` が「要件へ戻すもの」に挙がった要件に ★ の印が無ければ FAIL するようにした
> （この節の先送りは、その FAIL で見つかった）。
>
> **spec の delta とずれないための担保**: 改訂した要件の本文に、**それが ST02 の深掘りの
> どの問いの決定か**を ★ の行で名指ししてある（FR-33 → Q1 / FR-78 → Q1・Q5 / NFR-13 → Q2・Q3・Q4）。
> specs を書くときはこの対応を辿る。

1. **FR-33 だけでは扉 #14 を満たせない。** 「WHEN いずれかのソースが記録を生成する」ときにしか
   書かないので、記録が 0 件の日の原因が残らない。**生存信号を要件として立てる**
   → **`FR-78` を新設した**（`WHILE あるソースの収集が有効である` … 想定間隔ごとに、稼働と
   **取得できる状態か**（権限・センサ・接続）を残す）。FR-33 には ★ の補強を入れ、FR-78 を指した
2. **NFR-13 の判定式がソースによって成立しない。** 「記録が 1 件以上ある日」を Must ソースに
   一律で当てると、写真（FR-3 = 新しい写真が保存されたとき）が**正常動作時から未達で固定される**。
   旧プロジェクトが同じ問題を実測で踏んでいる（`reference/requirements.md:765`、2026-09-01）。
   **ソースごとの判定型に書き換える**
   → **NFR-13 を書き換えた**。毎日届く 4 つは「記録が 1 件以上ある日」、写真は「生存信号（FR-78）が
   あった日」。あわせて Q2・Q3 の決定も判定式に入れた —— **日の区切りを `Asia/Tokyo` に固定**し、
   分母から除く停止を **1 日を丸ごと覆うもの**だけに限った（どちらも分母の定義そのものなので、
   判定式と離して書くと食い違う）

### 第 4 回ぶん（2026-09-10。**同日に実施済み**）

| 要件 | 何を | 由来 |
|---|---|---|
| **NFR-13** | 判定式を**再訂正**（主語での仕分け / 取得可否の要求 / 5 本すべてが 350 以上） | Q7 / Q8 / Q9 |
| **FR-35** | 閾値を「最後の記録 **または** 最後の生存信号」で測る。想定間隔に写真とウィンドウを足す | Q12 |
| **FR-54** | 表示するものを 3 つから **7 状態**へ | Q11 |
| **FR-78** | 記録と同じ保護（書き換え禁止・冪等キー・原文の素通し）と、受信時刻を時刻で残すこと | Q13 / Q14 |
| **FR-79（新設）** | ソースごとの収集開始日を残す | Q11 |
| **FR-80（新設）** | 想定間隔を超えて記録も生存信号も来ない期間を「途絶」として残す | Q11 |

**動かなかった要件**: NFR-19 / NFR-21 —— Q16 の答え（タップは週単位、セルは表示専用）は
要件を割らなかった。割るなら要件の側を直す手続きに載せる予定だったが、その必要は無かった。

**ST14 への波及が実際に出た**: FR-35 を改訂したので、再生成で `docs/stories/ST14.md` の逐語が
自動で入れ替わった。**要件へ戻さなければ、ST14 は「最後の記録から」という覆された前提を
正典として持ち回っていた。**

**Story への波及**: `docs/stories/stories.json` の ST02 に `FR-78` / `FR-79` / `FR-80` を足し、
**完了の判定**を深掘りの決定に合わせた（「記録あり / 無し」→ **6 状態**、
表の期間 → **1 年を週に畳んだ格子**、グレースケールでの区別とセル選択時の文字表示、
達成日数は NFR-13 のソースごとの判定に従う）。第 4 回の後にさらに **7 状態**・
**ソースごとの行と行頭のソース名**・**行単位のタップ**・**5 本すべてが 350 以上**へ更新した
（この 3 つはその後、第 5 回 Q18 と第 6 回 Q25 でさらに変わっている ——
**分母の 95 % 以上** / **ソースごとに格子を分ける** / **行は週**）。
`scripts/make_story.py` で再生成し、`scripts/check_chain.py` は要件 112 件 / 拾われ 110 件で OK。

## Capabilities

### New Capabilities

（なし）

> **訂正（2026-09-09、ST01 の merge 後）**: 当初この proposal は `collection-coverage` を
> **新設**するものとして書いていた。しかし **ST01 の下流が同じ capability を前倒しで作成した**
> （main の `327fe1f`「稼働記録の要件を collection-coverage へ移す」）。
> ST01 は取り込みと同じ関門で稼働記録を書くので、件数の数え方（design D13）の置き場が
> ST02 まで無かったという理由で、`device-collection` と同じ判断が下されている。
> `docs/stories/INDEX.md` の表も更新済み（`collection-coverage` の積む Story が
> **ST01**, ST02, ST14, ST15 になった）。
>
> **したがって ST02 は `collection-coverage` を `MODIFIED` する側になる。** 名前は変わっていないので
> ST02 が書く振る舞い（6 状態・生存信号・停止と破棄の期間・達成日の集計・画面 S-1）は
> そのまま同じ置き場に乗る。**決めたことは 1 つも変わらない。**
>
> ただし **`collection-coverage` に属さない振る舞いが 1 つある** —— 生存信号を
> **端末が送る**振る舞いは `device-collection`（ST01 が作成済み）に属し、
> **PC が送る**振る舞いは `desktop-collection`（ST07 / ST08 が作る、まだ存在しない）に属する。
> ST02 は **受け口と契約まで**を持ち、C-02 側の送信は ST07 / ST08 に残す。
> 前倒しはしない —— C-02 自体がまだ存在せず、置き場を先に作る理由が無い。

### Modified Capabilities

- `collection-coverage`: ST01 が置いた「稼働記録は新しく入った記録だけを数える」の上に、
  6 状態・生存信号の受け口・停止と破棄の時刻範囲・ソースごとの達成日判定・画面 S-1 を積む。
  **日境界を UTC から `Asia/Tokyo` へ変える（BREAKING）**
- `device-collection`: 端末（C-01）が生存信号を定期的に送る振る舞いを追加する
  （位置・写真の両ソースについて、取得できる状態かを併せて送る）

> **★ 2026-09-11: 書いた。** 止まっていた条件は **merge ではなく `archive`** で、
> ST01 は 2026-09-10 に archive された（`26dff5d`）。`openspec/specs/` に
> `collection-coverage` / `device-collection` / `record-envelope` が入ったので、
> **存在しない capability に `MODIFIED` を書けない**という制約が解けた。
>
> ST01 は **59/60 tasks** で archive された。残る 1 件は 8.4（実機を持って外を歩く検証）で、
> **人間の確認待ち**として残っている（`scripts/archive.sh` の通過条件は
> 「人間の確認待ち以外が全部 `[x]`」なので、これを満たしている）。
> PR #15 で止まっていた **R46（Doze の除外を要求するか）は B（受け入れるが、
> 欠損を稼働記録に残す）で決着**し、**その実装は ST02 に載る**と申し送られた。
> ST02 はこれを `design.md` と `specs/device-collection/` で受けている。
>
> **ただし R46 の B のうち 1 件は ST02 の設計で埋まらなかった** ——
> 「空きの**理由**（静止による省電力か、アプリが死んだか）」は生存信号の間隔（6 時間）より
> 細かい時間の話で、日単位の状態にも乗らない。**後から作れない**ので独断で決めず、
> **深掘り 第 5 回（`deep-questions-r5.json` の Q17）として本人へ返した**。
> 他のすべては先に進めてある。

## capability の照合（Step 3。2026-09-11）

`docs/stories/INDEX.md` の capability 表と、ST02 が書く振る舞いを突き合わせた。**ずれは無い。**

- 稼働状況の**画面（S-1）**は `browsing-views`（閲覧と検索。ST25 / ST26 / ST36）ではなく
  `collection-coverage`（**収集の稼働状況**・通知・停止）に収まる。表の文言どおり
- 生存信号を**端末が送る**側は `device-collection`（ST01 が作成済み）。
  **PC が送る**側は `desktop-collection` に属するが、C-02 自体がまだ存在しないので
  ST07 / ST08 に残す。**前倒しはしない**（置き場を先に作る理由が無い）

**capability の前倒しは今回不要だった。** ST01 で 2 回起きた「土台の Story が後続の
capability に属する振る舞いを先に書く」型は、ST02 では起きていない。

### ただし「要件の前倒し」は起きている（★ 2026-09-11。`review/spec.md` の R15）

capability はずれていなかったが、**ST02 の `satisfies` に無い要件を specs が触っている**。
独立レビューが見つけた。内訳は 2 種類で、性質が違う。

**(a) 置き場だけを先に作るもの** —— `docs/stories/INDEX.md` の割り当ては動かさない。

| 要件 | 本来の Story | ST02 が作るもの | なぜ今か |
|---|---|---|---|
| FR-34（停止した期間） | ST15（layer 2） | 時刻の範囲で持てる表 | `core.coverage` を作り直す**唯一の機会**。日単位＋自由文のまま渡すと ST15 が作り直す |
| FR-9（破棄した期間と件数） | ST04（layer 2） | 同じ表の `kind='dropped'` | 同上 |

**要件を満たすのは ST04 / ST15 のまま。** ST02 が作るのは器で、
**入力の手段**（利用者が停止する画面、端末が破棄する処理）は持たない。
`satisfies` を動かさないのはこのため。

**(b) 先行 Story の正典に開いていた穴** —— こちらは前倒しではなく**穴埋め**。

| 要件 | 満たしたことになっている Story | 実際 |
|---|---|---|
| FR-29（すべてのテーブルに利用者識別子） | ST01 | `core.event` にしか列が無い（`grep -n user_id migrations/*.sql` が 1 件）。`core.coverage` / `core.source` に無い |
| FR-30（収集した記録を書き換えない） | ST01 | `core.event` にしかトリガが無い。生存信号は ST02 が新設するので、同じ保護を最初から掛ける |

**ST01 は archive 済みなので、正典（`openspec/specs/`）にこの穴が入ったまま残っている。**
ST02 が塞ぐが、**塞いだことを記録しないと「ST01 が満たした」という記述が残り続ける**。
`deep.md` の「上流で見つかった既存実装の欠陥」の 4 件目が同じことを指している。

### `docs/ui-direction.md` の宿題 1 を閉じた（★ 2026-09-11。`review/spec.md` の R17）

第 4 回 Q15 の答え（**ソースごとに行を分け、行頭にソース名の文字を置く**）で宿題 1 は
決着していたが、**`docs/ui-direction.md` 側に印が入っていなかった** ——
要件は `review_triage.py` が ★ 印を検査するのに対し、**ui-direction は戻しの手続きから
外れていて機械も止めない**。宿題 1 に決着の内容と、取り消した誤読（1.009:1 はソースの色であって
状態の色ではない）を書き足した。

**ST25 側の宿題は残っている**（記録の種類を 6 色で分ける箇所）ことも明記した。
併せて、**状態の側に別の穴が見つかったこと**（7 状態を明度だけでは分けられない。
第 5 回 Q21）も同じ場所に残した。

**NFR-19** は全画面に掛かる非機能で、どの Story の `satisfies` にも入らない
（`docs/stories/ST02.md` の完了の判定が逐語で引いている）。前倒しではない。

## Impact

| 対象 | 影響 |
|---|---|
| `migrations/` | `core.coverage` の作り直し（時刻範囲の実体 + 日単位の集計）、生存信号の表を新設 |
| `crates/server/src/lib.rs` | 日境界を `Asia/Tokyo` へ。稼働記録の更新を重複判定の内側へ移す |
| 生存信号の受け口 | 新規の API。`docs/collector-contract.md` と `docs/openapi.json` に反映 |
| `collector-android/` | 生存信号の定期送信と、権限・センサ状態の読み取り |
| `web/` | 稼働状況の画面（S-1）。1 年を週に畳んだ格子 |
| `docs/requirements.md` | FR-33 / FR-35 / FR-54 / FR-78 の改訂と NFR-13 の 2 度の書き換え、FR-79 / FR-80 の新設 |
| 収集開始日と途絶の検知 | FR-79 / FR-80。サーバ側で「来ないこと」を検知する経路が新規 |
| `docs/stories/ST14.md` / `ST15.md` | 前提が増える（ST14 は生存信号で早く鳴らせる。ST15 は日単位入力として乗る） |
| 既存データ | **縦串のダミーのみ**。日境界の変更で失われるものは無い |

## 既存実装の欠陥（上流で見つけた）

`deep` の手順 5 で 3 件。1 と 3 は上の決定で直る。2 は独立した誤り。

1. **日境界が UTC 固定** —— `crates/server/src/lib.rs` の `($2 AT TIME ZONE 'UTC')::date`
   （2026-09-10 時点で 219 行目。
   ST01 merge 後も残っている。Q2 の決定で ST02 が直す）
2. ~~**重複再送で `event_count` が水増しされる**~~ —— **ST01 の下流が独立に見つけて直した**
   （`crates/server/src/lib.rs` の `.bind(i32::from(row.is_some()))` で重複時は 0 加算、
   行そのものは立てる）。振る舞いは ST01 の
   `specs/collection-coverage/spec.md`「稼働記録は新しく入った記録だけを数える」に固定済み。
   **ST02 でやることは無い**
3. **停止・破棄の期間を持てない** —— `migrations/202609081618_envelope.sql` の `core.coverage` は
   `note` の自由文のみ
4. **`core.coverage` と `core.source` に `user_id` が無い**（★ 2026-09-10 追加。`review/deep.md` の R9）
   —— FR-29「すべてのテーブルに」と扉 #9（決定済）に反している。
   ST02 が `core.coverage` を作り直すここが、列を足す最後の安い機会
