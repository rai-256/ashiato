# ST06 の深掘り —— 携帯端末のアプリ利用を集める

対象: `docs/stories/ST06.md`（satisfies: FR-2 / requires: ST04 / capability: `device-collection`）。
面は無い（`docs/ui-direction.md` の「面を持たない Story（17 本）」に ST06 が入っている）ので proto は作らない。

**この Story に宛てられたものは無い** —— `docs/handoff/ST06.md` は存在せず、
他の change のレビューに `処置: deferred ST06` も無い（`grep -rn "ST06" openspec/ docs/` で確認）。

## 第 1 回（2026-09-18。8 問。止める 7 / 仮でよい 1）

問い: `deep-questions.json` / 渡した HTML: `docs/briefs/ST06-deep.html`
独立レビュー: `review/deep.md`（12 件）。**Q7 と Q8 はレビューが足させたもの**（R1 / R2）。

| # | 段 | 何を問うたか |
|---|---|---|
| Q1 | A（uncaptured） | 記録するイベントの種別をどこまでにするか |
| Q8 | A（uncaptured） | イベント 1 件から取る欄をどこまでにするか |
| Q2 | A（premise） | NFR-13 の「アプリ利用は端末が主語」の仕分けのままでよいか |
| Q3 | A（uncaptured） | 「利用状況へのアクセス」が無いときの振る舞い |
| Q7 | A（uncaptured） | 片方のソースが取れないとき、もう片方の収集をどうするか |
| Q4 | A（uncaptured） | 取りに行ったが OS に無かった期間を残すか |
| Q5 | A（uncaptured） | 導入より前の利用（OS の集計）を取り込むか |
| Q6 | B（conflict） | 取得の契機の間隔（推奨 30 分） |

### 本人の答え（2026-09-18。貼り戻された文字列の逐語）

```
=== ST06 の深掘り の回答 ===
Q1 [記録するイベントの種別] -> ふるいにかけず、OS が返すイベントを全部記録する
Q8 [1 件から取る欄] -> OS が返す欄を全部
Q2 [1 年の成功判定で、アプリ利用を何で数えるか] -> NFR-13 を訂正し、アプリ利用も「利用が主語」に移す
Q3 [「利用状況へのアクセス」が無いときの振る舞い] -> 初回起動で設定画面へ送る。拒んでも位置の収集は始める
Q7 [片方のソースが取れないとき、もう片方をどうするか] -> ソースごとに独立させる（欠けたソースだけ止め、他は取り続ける）
Q4 [取りに行ったが OS に無かった期間の残し方] -> 取りこぼした期間を、アプリ利用の記録 1 件として残す
Q5 [このアプリを入れるより前の利用を取り込むか] -> 初回に、OS が持っている全部の粒度（年・月・週・日）を取り込む
Q6 [取得の契機の間隔] -> 30 分（FR-2 を訂正する）
```

**8 問すべてを本人が選択肢から明示的に選んだ**（未回答 0 / 推奨のまま 0 —— Q6 も「触らなかった」のではなく
選ばれている）。**推奨と違う側を選んだ問いは無い。当初案を覆したものも無い。**

### 決まったことが、どこに効くか

| # | 決めたこと | 効く先 |
|---|---|---|
| Q1 | イベントの種別は**ふるいにかけない**（OS が返すものを全部） | specs（取る範囲）/ design D1 |
| Q8 | 1 件から取る欄も**全部**（共通の 4 つ + 種別ごとの 4 つ） | specs / design D2（`raw` の形の凍結） |
| Q2 | **NFR-13 を訂正**し、アプリ利用を「利用が主語」へ移す | **要件へ戻すもの**（NFR-13 ★）/ ST14 への申し送り |
| Q3 | 初回起動で設定画面へ送る。**拒んでも位置の収集は始める** | specs（権限が無いときの振る舞い）/ design D5 |
| Q7 | **ソースごとに独立**（欠けたソースだけ止め、他は取り続ける） | specs（`device-collection` の MODIFIED）/ design D5 |
| Q4 | 取りこぼした期間を**アプリ利用の記録 1 件**（種別 `gap`）として残す | **要件へ戻すもの**（FR-85 を新設・扉 #14 の関与要件）/ specs / design D4 |
| Q5 | **導入前の集計を全粒度で取り込む**（別の論理ソース） | **要件へ戻すもの**（FR-84 を新設）/ specs / design D3 |
| Q6 | 取得の契機は **30 分** | **要件へ戻すもの**（FR-2 ★）/ specs |

### 要件へ戻したもの（`docs/requirements.md`。★ の印つき）

| 要件 | 何を | 出所 |
|---|---|---|
| **FR-2** ★訂正 | 取得契機を 1 時間 → **30 分**。`[NEEDS CLARIFICATION]`（保持期間が未確認）を、一次情報で取れた事実に置き換えて解消 | Q6 |
| **FR-84** ★追加 | 収集を始めた時点で、取得元が保持している過去の利用の集計を取得して記録を生成する | Q5 |
| **FR-85** ★追加 | 取得の窓の始まりが取得元の保持している範囲より前にあるとき、その期間を「取得元に残っていなかった」として記録する | Q4（spec レビュー R1 で気づいた抜け） |
| **NFR-13** ★訂正 | アプリ利用を「端末が主語」から「**利用が主語**」へ移す | Q2 |
| 扉 #14 ★補足 | 関与要件に **FR-2 / FR-84 / FR-85** を足した（携帯端末では取得元が 10 日でイベントを消すので、FR-78 / FR-80 のどちらにも映らない形がある） | Q4 |

FR-2 を 2 つに割ったのは `check_chain.py` の警告（「SHALL が 2 つ以上ある FR は要件を割る」）に従ったもの。
`docs/stories/stories.json` の ST06 に `FR-84` と完了の判定 2 件を足して `make_story.py` で再生成し、
`INDEX.md` の行も合わせた（`check_chain.py`: chain OK / warn 0）。

### 成果物の独立レビュー（`spec-review`。11 件）

問いの答えを specs / design / tasks に写した後、書いた文脈を持たない目でもう 1 度かけた。
**11 件すべてを直して閉じた**（`review/spec.md`。`review_triage.py` は 23 件すべてに処置を認めて OK）。
重かったのは 4 件:

| # | 何を見つけたか | どう閉じたか |
|---|---|---|
| R1 | **Q4 の決定を受ける要件が無い**（PC 側には FR-82 という同型の前例がある） | FR-85 を ★ 付きで新設し、扉 #14 の関与要件に足した |
| R2 | gap の記録の**出来事の時刻**が spec に無く、受け手はその日を①「記録あり」に塗る | 「期間の終わり」を spec に書き、現状の帰結を `docs/handoff/ST14.md` に明記 |
| R3 | 保持の下限（10 日）は API から読めない見込みなのに、それで窓を**切り詰めよう**としていた。外れると残っているイベントを飛ばす | **窓を切り詰めない**形に変え、gap の範囲を「返った最古のイベント」で閉じた。取り損ねる経路が消えた |
| R4 | 集計の取得契機 24 時間と生存信号 6 時間が噛み合わず、**試行 0 / 成功 1** の信号が契約に恒久的に断られて端末に居座る | 集計の取得契機を 6 時間に揃え、「生存信号の 1 区間に取得契機が 1 回以上入る」を spec の不変条件にした |

**kind を直したものが 4 件ある**（R1 / R2 / R9 を `conflict` → `technical`、R3 を `premise`（loss あり）→ `technical`）。
どれも「選択肢のあいだの矛盾」ではなく成果物の抜けで、R3 は**直し方そのものが失われるものを消した**
（切り詰めなければ取り損ねない）ので、人間に返す問いにならなかった。理由は `review/spec.md` の各 `処置:` に書いてある。

## 先に確かめた事実 —— FR-2 の未決（`UsageStatsManager` の保持期間）

FR-2 には `[NEEDS CLARIFICATION: UsageStatsManager のイベント保持期間が未確認。
一次情報は本文を返さず逐語が取れなかった]` が付いたままだった。**AOSP の実装から逐語で取れた**ので、
この未決は深掘りの問いにせず、事実として問いの前提に置いた（機械が判定できることは機械に決めさせる）。

| 何 | 保持 | 出所 |
|---|---|---|
| 生のイベント（`queryEvents`） | **10 日** | `UsageStatsDatabase.prune()`: `mCal.addDays(-10); pruneFilesOlderThan(mIntervalDirs[INTERVAL_DAILY], …)` |
| 週ごとの集計 | 4 週 | 同 `mCal.addWeeks(-4)` |
| 月ごとの集計 | 6 か月 | 同 `mCal.addMonths(-6)` |
| 年ごとの集計 | **2 年** | 同 `mCal.addYears(-2)` |

`queryEvents` が日ごとの箱だけを読むことも裏が取れた（`UserUsageStatsService.queryEvents` は
`queryStats(INTERVAL_DAILY, …)` を呼ぶ）。範囲は `[begin, end)`（javadoc: "The **inclusive** beginning" /
"The **exclusive** end"）。どちらも 2026-09-18 取得。

→ **要件へ戻すもの**: FR-2 の `[NEEDS CLARIFICATION]` を、この事実で置き換える（★ 訂正）。
取得間隔の値そのものは Q6 の答えで決まる。

## 聞かずに決めた既定（C）—— 扉を開けたままにする側に倒したもの

「片方の選択肢が扉を開けたままにし、費用が小さい」ものは問いにせず、ここに残して design の D 番号にする。

| # | 決めたこと | 扉をどちらへ開けておくか |
|---|---|---|
| C1 | **1 イベント = 1 記録**（1 回の取得を 1 件にまとめない） | まとめると、重ねた窓で配列の中身が変わって冪等キーが動き、再送のたびに行が増える。削除（ST22）・感度（ST24）・時間ごとの件数の粒度も失われる |
| C2 | **アプリの表示名を取得時に解決して `payload` に載せる**（`raw` には入れない） | アプリを消した後でも「何だったか」が残る。`raw` に入れると、アプリの更新や端末の言語変更で冪等キーが変わり同じイベントが行として増える（R2） |
| C3 | 初回と再開の遡りは、**OS が持っている分だけ**（イベントは 10 日、集計は Q5 の答えによる） | 取れるものは全部取る。上限は OS 側が決めるので、こちらで短く切らない |
| C4 | 取得の窓は**重ねて進める**（前回の終わりより少し手前から取り直す） | 境界の取りこぼしを作らない。重複はサーバの冪等が畳む |
| C5 | **`null` と「0 件」を区別し、`null` のときは窓を進めない**（R3） | 解錠していない端末では `queryEvents` が `null` を返す。0 件と同じに扱って窓を進めると、その期間は 10 日で消える |
| C6 | 端末の時計が飛んだときは、**窓を単調な経過時間で押さえ、飛びが直るまで取り直さない**（R4） | OS 側は時計が変わると過去の統計ファイルを丸ごと改名してずらす。ずれた時刻で取り直すと `event_time` が変わって畳まれず、行が増える。`event_time` は凍結されていて後から直せない |
| C7 | 生存信号の **試行 = 契機の数**（区間の長さ ÷ 取得間隔）・**成功 = 取得できた回数**（0 件でも成功） | 位置と同じ数え方。取得率が「眠っていた / 生きていた」を分ける |
| C8 | 生存信号の間隔は**登録簿の想定間隔**（`c01-app-usage` は 6 時間）のまま | ずらすと受け手の途絶判定の窓とずれる |
| C9 | `blockers` は **`permission` と `network`**（アプリ利用に `sensor` は無い） | 契約の語彙をそのまま使う |
| C10 | 感度は**収集側で付けない**（サーバの既定に委ねる） | 契約の「収集側が厳しい側を付けない」に従う |
| C11 | 未送信の置き場は**位置と同じ**（保持の上限は全ソースを通して古い順） | 正典がそう定めている。ソースごとに分けると上限の意味が変わる |
| C12 | 遡って取ったイベントの `tz_id` / `tz_offset_min` は、**取得時点の端末の地域**を付ける（R12） | `tz_id` / `tz_offset_min` は凍結の対象外（DB が明示）なので、後から引き直せる。design の D 番号に（仮）と反転条件を書く |

## 確かめたが問わなかったこと（人間が決めることが無い。tasks / design で塞ぐ）

- **収集側が単一ソース前提で書かれている箇所が 4 つある**（R10）。ログの `source=c01-location` の焼き込み、
  生存信号の満点の刻みが `FIX_INTERVAL_MS` 固定（塞がないと**アプリ利用の取得率が毎回 3 % と読める**）、
  数えの置き場のファイル名が 1 本、常駐通知の文言。tasks で 4 か所とも塞ぐ
- **`Retention` が破棄の報告の範囲の終わりに「置き場全体の最も古い記録」を使っている**（R11）。
  正典は「**同じソースで**残った最も古い記録」。位置 1 本のいまは偶然一致しているが、
  2 本目を同じ置き場に載せた瞬間に、片方の破棄の範囲がもう片方の記録の時刻で閉じる。tasks で塞ぐ
- **要件は直したが正典を直し忘れた前例**（R6）。ST07 が NFR-13 に足した但し書き
  （PC の 2 ソースだけは生存信号が届いた日を分母とする）が `collection-coverage` の spec に入っていない。
  Q2 の答えが NFR-13 を動かすなら同じ型を踏むので、**ST14 への申し送り**に残す（`docs/handoff/ST14.md`）

## 走っている Story との重なり

| Story | capability | 重なり |
|---|---|---|
| ST12（下流） | `external-ingestion`, `collection-coverage` | **重なる。** Q2（NFR-13 の仕分け）と Q4（受け手の格子）と Q5（6 本目のソースの表示）は、どれも `collection-coverage` の変更を誘う。**ST06 はそこに delta を書かない** —— 要件（`docs/requirements.md`）と端末側（`device-collection`）に閉じ、受け手側は ST14 へ申し送る |
| ST08（下流） | `desktop-collection` | 重ならない |

移行は作成時刻の名前（`YYYYMMDDHHMM_<slug>.sql`）。ST12 と番号を取り合わない。
