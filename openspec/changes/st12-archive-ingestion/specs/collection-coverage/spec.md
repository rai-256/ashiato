## MODIFIED Requirements

### Requirement: 稼働状況は 1 年を週に畳んだ格子で見える

WHEN 利用者が稼働状況の画面を開く THE SYSTEM SHALL
ソース × 日 の状態を、**1 年を週に畳んだ格子**として表示する。
THE SYSTEM SHALL **ソースごとに格子を分け、各格子にソース名の文字を添える**。
THE SYSTEM SHALL 1 つの格子の中で、**7 日ぶんを 1 つのまとまり（週）として選べる**ようにする。
THE SYSTEM SHALL 格子のセルが担う区別を **3 段**とする ——
「記録あり」「動いていた・記録なし」「それ以外」。
THE SYSTEM SHALL この 3 段を、**色を使わずに（グレースケールで）区別できる**ように表示する。
THE SYSTEM SHALL 隣り合う 2 段の相対輝度比を **3:1 以上**とする（NFR-23）。
WHEN 利用者が**週**を選ぶ THE SYSTEM SHALL その 7 日ぶんの状態を、
**8 状態それぞれの名前で文字で**表示する。
IF ある日に**その日を丸ごと覆わない破棄**がある THEN THE SYSTEM SHALL 格子のその日のセルに、
**色ではなく形の印**（セルの右下を三角に欠く）を付ける。
THE SYSTEM SHALL その印とセルの相対輝度比を、3 段のどのセルの上でも **3:1 以上**とする（NFR-23）。
WHEN 利用者が週を選ぶ THE SYSTEM SHALL その 7 日のうち破棄がある日について、状態の名前に添えて
**「うち N 件を破棄（時刻〜時刻）」**を文字で表示する（丸ごと覆う破棄の日は件数を添える）。
THE SYSTEM SHALL 件数を持たない区間（時間ごとの件数を持たない破棄の範囲）を、件数を添えずに「破棄（時刻〜時刻）」とだけ表示する。
THE SYSTEM SHALL 破棄の期間と件数を、週の詳細の文字のほかに一覧として出さない。
THE SYSTEM SHALL 格子のセルそのものを操作対象にしない（表示専用とする）。
THE SYSTEM SHALL 幅 360 CSS px でも、操作できる対象が 24 × 24 CSS px 以上であるようにする。
THE SYSTEM SHALL 格子を**縦長**に置く —— 1 行が 1 週（7 日）で、**上から下へ 53 週**。
THE SYSTEM SHALL **新しい週を上**に置く。
THE SYSTEM SHALL 画面を開いた直後、**各ソースについて直近 4 週以上を表示**し、
1 年ぶんはそれを伸ばして表示する。
THE SYSTEM SHALL 開いた直後に、**2 ソース以上の直近 4 週**が同時に見えるようにする。
THE SYSTEM SHALL Must の 5 ソースすべてが、**ひとスクロール以内**に見えるようにする。
THE SYSTEM SHALL この 2 つの高さを、**Must の 5 ソースの格子の前に置く「直近に置いた書庫」の箱の高さ（160 CSS px を上限として）を除いて**数える（箱は `external-ingestion` が定める）。
THE SYSTEM SHALL 退役したソースの格子を、**Must の 5 ソースより後ろに置き、既定で畳む**。
THE SYSTEM SHALL 幅 360 CSS px で、格子が横スクロールなしで収まるようにする。
THE SYSTEM SHALL 週の帯が 24 × 24 CSS px 以上であるようにする。
THE SYSTEM SHALL 5 ソースそれぞれの達成日数と分母、および 5 本すべてが分母の 95 % 以上かどうかを数値で表示する。
THE SYSTEM SHALL その合否が**確定か暫定か**を表示する。
THE SYSTEM SHALL 暫定のとき、**確定までの残り日数**を表示する（確定する日が定まらないときは、
そのソースがまだ収集を開始していないことを表示する）。

導出元: FR-54, FR-61, NFR-13, NFR-19, NFR-23, FR-9, FR-55。
深掘り Q6, 第 4 回 Q15, Q16, 第 5 回 Q20, Q21, 第 6 回 Q25, 第 7 回 Q28, 第 8 回 Q30。ST04 の深掘り Q3。

> **2026-09-15 の変更（ST12 の深掘り 第 2 回 Q9。直した proto で本人が選んだ）。**
> 書庫を置いた人が「入ったか・読めなかったか・読んでいる途中か」を開いてすぐ見られるよう、「直近に置いた書庫」の箱を **Must の 5 ソースの格子の前（達成の下）**に置く。
> 選択肢には「ひとスクロール（1,280 px）と開いた直後の 2 ソース（640 px）を超える —— 選ぶと、その予算を変える判断になる」と書いてあり、本人はそれを読んで選んだ。
> **予算を捨てたのではなく、箱の高さぶん緩めた**と読み、2 つの高さは箱を除いて数える。箱が伸びるほど予算が伸びないよう、除く高さは 160 px を上限とする（仮。ST12 の design D12）。
> **この Requirement の本文は ST04 の下流（PR #49、head 8abafb5）の delta を写したもの**。ST12 の下流は ST04 の archive を待ち、開始時に正典と突き合わせ直す。

> **2026-09-14 の追加（ST04 の深掘り Q3。proto で本人が選んだ）。**
> 丸ごと覆わない破棄は状態を決めないので、印が無いと**長い圏外の両端の日と、上限を小さくして溢れさせた日の破棄が画面から消える**。
> 印は形で持たせる（色を意味の担い手にしない）。**期間と件数は週の詳細だけ**に書き、ソースの見出しの下の一覧は作らない ——
> 一覧の行を足さないので、開いた直後の高さ（5 ソースで 997 px。proto の実測）は変わらず、ひとスクロールの予算に効かない。
> 印の色は段ごとに変える（「記録あり」と「動いていた・記録なし」の上は最も暗い段、「それ以外」の上は控えめな文字の明るさ）——
> 1 色だと「それ以外」の段の上で 1:1 になり見えない。

**「スクロールせずに 5 本」は現物の高さで成り立たなかった**（第 8 回 Q30）。
第 7 回 Q28 の根拠にした 600 px は `5 ソース × 5 行 × 24 px` で**セルだけを積んだ勘定**で、
見出し・余白・「1 年ぶんを見る」のボタン・達成の表を数えていない。
実際に宣言されている箱を積むと **約 1,491 px**（実測）。
本人の答えは「**同時に見える」を 1 スクロール以内に緩める**」で、
第 7 回 Q28（開いた直後は直近 4〜5 週だけ）と NFR-19（週の帯 24 px）は動かさない。

**ひとスクロールは 2 画面ぶん**とする —— 基準の画面は **360 × 640 CSS px**
（NFR-19 が幅に使っている小さい端末に、その端末の高さを合わせたもの）。
開いた直後に 1 画面ぶんが見え、1 回スクロールすると残りが見える。

**退役したソースを後ろに置いて畳むのは ST03 の R63** —— ST03 の運用では退役は 1 本きりではなく
増えるので、退役した格子が上に並ぶと **Must の 5 本が 1 画面から押し出される**。

1 年を見せるのは、完了の判定が「1 か月放置した後に開くと、欠けた日が一目で分かる」であるため。
直近 14 日では 1 か月の放置を見せられない。

**色を意味の担い手にしない。** ソースの区別は各格子に添えたソース名の文字が担う
（`ui-direction.md` の宿題 1 —— 6 色の輝度差が最小 1.009:1 で、グレースケールと 2 型色覚では
区別できない）。

**格子が 3 段までなのは、明度だけでは 7 段を分けられないため**（第 5 回 Q21）。
WCAG SC 1.4.11 は隣接する色に 3:1 を求めるが、7 段を隣接 3:1 で並べると 3^6 = **729:1** が要る。
**sRGB の理論最大は 21:1** で、21:1 を 6 等分しても隣接 1.661:1 にしかならない。
3 段なら 3:1 × 3:1 = 9:1 で収まる。8 状態の区別は**週を選んだときの文字**が担う。

**縦長にするのは NFR-19 のため**（第 6 回 Q25）。横長（週 = 列）だと 1 ソースが 53 列になり、
360 px 幅では週の帯が 6 px で 24 px を割る。縦長なら週の帯は幅いっぱいを取れる。
**新しい週を上**にするのは、開いた直後に出る直近 4〜5 週が新しい側になるようにするため。
完了の判定「1 か月放置した後に開くと欠けた日が一目で分かる」を満たすのは
**初期表示を直近 4〜5 週に絞ること**（第 7 回 Q28）で、
**週の向きだけでは 2 本目以降のソースに届かない**（1 本目の下に 53 行ぶんの距離がある）。

セルを表示専用にするのは、**1 週 7 日を横に並べても 1 セルが 24 px に満たない場合がある**ため。
選ぶ単位を週にすれば、セルの大きさに関わらず NFR-19 を満たせる。

#### Scenario: 格子は 1 行 1 週で、直近の週が一番上に来る

- **WHEN** 稼働状況の画面を開く
- **THEN** 1 行が 1 週（7 日）で、新しい週から古い週へ上から下に並んでいる
- **AND** 直近の週が一番上にある

#### Scenario: ソースごとに格子が分かれる

- **WHEN** 稼働状況の画面を開く
- **THEN** ソースごとに格子が分かれ、それぞれにソース名の文字が添えられている

#### Scenario: 1 年ぶんが一目で読める

- **WHEN** 1 年ぶんのデータがある状態で、あるソースの表示を 1 年ぶんまで伸ばす
- **THEN** 1 年ぶんの日が週に畳まれた格子として読める

#### Scenario: グレースケールでも格子の 3 段が区別できる

- **WHEN** 3 段それぞれのセルの相対輝度を取る
- **THEN** 隣り合う 2 段の比がいずれも 3:1 以上である

#### Scenario: 週を選ぶと 7 日ぶんが 8 状態の名前で出る

- **WHEN** ある週を選ぶ
- **THEN** その 7 日ぶんの状態が、8 状態それぞれの名前で文字で表示される
- **AND** 格子では「それ以外」に畳まれていた日も、どの状態だったかが分かる

#### Scenario: 360 px 幅でも操作対象が 24 px を割らない

- **WHEN** 幅 360 CSS px で画面を開く
- **THEN** 操作できる対象はいずれも 24 × 24 CSS px 以上である
- **AND** 格子のセルは操作対象になっていない
- **AND** 格子が横スクロールなしで収まっている
- **AND** 週の帯の高さが 24 CSS px 以上である

#### Scenario: 5 本の達成日数と合否が出る

- **WHEN** 稼働状況の画面を開く
- **THEN** 5 ソースそれぞれの達成日数と分母が数値で表示される
- **AND** 5 本すべてが分母の 95 % 以上かどうかが表示される
- **AND** それが確定か暫定かが表示される

#### Scenario: 暫定のときは確定までの残り日数が出る

- **WHEN** 5 ソースすべてが収集を開始していて、合否がまだ暫定である
- **THEN** 確定までの残り日数が表示される

#### Scenario: 始まっていないソースがあると残り日数の代わりに理由が出る

- **WHEN** 5 ソースのうち 1 本がまだ収集を開始していない
- **THEN** 残り日数は表示されず、そのソースがまだ収集を開始していないことが表示される

#### Scenario: 開いた直後に 2 ソース以上の直近 1 か月が同時に見える

- **WHEN** 360 × 640 CSS px で稼働状況の画面を開く
- **THEN** 「直近に置いた書庫」の箱の高さを除いて数えると、2 ソース以上について直近 4 週以上が 1 画面（640 CSS px）に収まっている

#### Scenario: ひとスクロールで 5 ソースすべてが見える

- **WHEN** 360 × 640 CSS px で稼働状況の画面を開く
- **THEN** 「直近に置いた書庫」の箱の高さを除いて数えると、Must の 5 ソースすべてが 2 画面ぶん（1,280 CSS px）以内に収まっている

#### Scenario: 箱が上限の高さのとき 2 ソースが 800 px に収まる

- **WHEN** 高さ 160 CSS px の「直近に置いた書庫」の箱がある状態で、360 × 640 CSS px で稼働状況の画面を開く
- **THEN** 2 本目の Must のソースの直近 4 週の下端が、画面の上端から 800 CSS px（640 + 160）以内にある

#### Scenario: 箱が上限の高さのとき 5 ソースが 1,440 px に収まる

- **WHEN** 高さ 160 CSS px の「直近に置いた書庫」の箱がある状態で、360 × 640 CSS px で稼働状況の画面を開く
- **THEN** Must の 5 ソースの最後の格子の下端が、画面の上端から 1,440 CSS px（1,280 + 160）以内にある

#### Scenario: 箱の高さを除く量は 160 px を超えない

- **WHEN** 何らかの誤りで「直近に置いた書庫」の箱の高さが 200 CSS px になった状態で予算を数える
- **THEN** 除かれる高さは 160 CSS px で、Must の 5 ソースの最後の格子の下端は 1,440 CSS px 以内でなければならない

#### Scenario: 退役したソースは後ろで畳まれている

- **WHEN** 退役したソースを含む稼働状況の画面を開く
- **THEN** 退役したソースの格子は Must の 5 ソースより後ろにある
- **AND** その格子は既定で週の行を出していない

#### Scenario: 1 年ぶんは伸ばして見る

- **WHEN** あるソースの表示を伸ばす
- **THEN** そのソースの 1 年ぶん（53 週）が読める

#### Scenario: 一部を破棄した日のセルに形の印が付く

- **WHEN** ある日の `10:00` から `13:00` までの記録 180 件が破棄され、その日に記録が残っている
- **THEN** その日のセルは「記録あり」の段で、右下を三角に欠いた印が付いている

#### Scenario: 丸ごと覆う破棄の日には印が付かない

- **WHEN** 1 日を丸ごと覆う破棄の日がある
- **THEN** その日のセルに印が付いていない

#### Scenario: 破棄の無い日には印が付かない

- **WHEN** 破棄の報告が 1 件も無い日がある
- **THEN** その日のセルに印が付いていない

#### Scenario: 印はどの段の上でも 3:1 以上

- **WHEN** 3 段それぞれのセルの上に置いた印の相対輝度を取る
- **THEN** 印とセルの比がいずれも 3:1 以上である

#### Scenario: 週を選ぶと破棄の件数と時刻が文字で出る

- **WHEN** `10:00` から `13:00` までの記録 180 件が破棄された日を含む週を選ぶ
- **THEN** その日の行に「記録あり」と、「うち 180 件を破棄（10:00〜13:00）」が表示される

#### Scenario: 丸ごと覆う破棄の日は件数が添えられる

- **WHEN** 1 日を丸ごと覆う破棄で 1,440 件が捨てられた日を含む週を選ぶ
- **THEN** その日の行に「破棄された期間」と、件数 1,440 が表示される

#### Scenario: 件数を持たない区間は件数を添えずに時刻だけが出る

- **WHEN** 件数 0 の区間 `10:30〜10:31` を持つ日を含む週を選ぶ
- **THEN** その日の行に「破棄（10:30〜10:31）」が表示され、「うち 0 件を破棄」は表示されない

#### Scenario: 破棄の一覧は出ない

- **WHEN** 破棄がある状態で稼働状況の画面を開く
- **THEN** 格子と週の詳細のほかに、破棄の期間と件数を並べた一覧は表示されていない
