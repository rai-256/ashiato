# ST12 深掘り 第 2 回の独立レビュー

対象: `openspec/changes/st12-archive-ingestion/deep-questions-r2.json`（Q9 visual / Q10 irreversible / Q11 open）と、Q9 が埋め込む `proto-r2.html`。
出所の `review/spec.md` の R9〜R12、第 1 回の `deep-questions.json` / `deep.md`（C1〜C19）/ `review/deep.md` を突き合わせの正本にした。
schema の `deep` 手順 1〜5 を、第 2 回の一覧を開く前に、第 2 回で動いたもの（C17 → C19、D10、D12 の箱、D2 の名前、D9 の写しの設定）について独立にやり直し、差分だけを書く。
**問いの JSON と proto は触っていない。**

実施日: 2026-09-15。ブランチ `docs/st12-upstream`（worktree `ashiato2-up-st12`。clean）。

## 実測・確認の方法

| | やったこと | 結果 |
|---|---|---|
| 実行 | `proto-r2.html` を /tmp に写し、全場面 × 箱の位置 × 読んでいる間の表示を headless Chromium（playwright の `chrome-headless-shell`、幅 360 の画面）で描いて、proto 自身の実測欄（`mMust` / `mBox` / `mHeight`）を読んだ | Must の最後の格子の下端: **後ろ 1,131 px（全場面）/ 前 1,245（初めて・読んでいる間に出さない・100 日忘れた）・1,264（置き直し）・1,286（読めない形）・1,287（読んでいる間に出す）・1,334（専用のフォルダが無い）**。箱の高さ 102〜191 px。「前」のとき 2 本目の Must の格子の 4 週目の下端 611〜700 px（「後ろ」は 497 px） |
| 実行 | 同じブラウザで、`web/src/AchievementPanel.tsx` と同じ inline style の達成の表（見出し + 判定 + 「確定まであと N 日」+ 表 1 + 5 行。`one-scroll.test.tsx:141` が「いちばん高くなる形で測る」とする暫定の形）を描いた | 実物の達成の表は外形 **229 px**。proto の達成（見出し + 3 行。`proto-r2.html:253-256`）は **113 px**。**proto は実物より約 116 px 低い** |
| 実行 | proto の `dayState` を場面ごとに直近 28 日ぶん評価した | 「初めて」: 0〜2 日前が「動いていた・記録なし」、他は記録あり（11・22 日前を除く）。「100 日忘れた」: 28 日ぶん全部「途絶」 |
| grep | `crates/server/src/coverage.rs:774-813`（`decide`）と正典 `openspec/specs/collection-coverage/spec.md:369-385` | 記録も信号も無い日は、実装は「**前か後ろ**の活動が想定間隔以内なら②」（:806-813）。正典は「(8) 直前からの経過が想定間隔を超えれば途絶」→「(9) 前後が想定間隔以内なら②」。proto は「**直前と直後の記録の間**が 60 日を超えれば途絶」（`proto-r2.html:164-165`）で、3 つとも違う。**描いた場面には 60 日を超える中間の空きが無いので、絵の上の差は出ない** |
| grep | `migrations/202609120944_gates.sql:55-60` / `migrations/202609111112_immutable_heartbeat.sql:15` | 収集した行の `logical_source` の書き換えを拒む（Q10 の出典どおり）。`core.heartbeat` は全列の書き換えを拒む |
| grep | `retired_on` を `crates/server/src` で | 格納の側に退役の検査は無い（`coverage.rs` の判定と試験だけ）。退役した日を置いても記録は入る |
| grep | `docs/requirements.md:294-311`（FR-35 / FR-78）、`docs/stories/INDEX.md:172` | FR-78 は「**そのソース**に登録された想定間隔ごとに」稼働を残す。★ に ST12 の記載は無い。INDEX の訂正の表だけが「書庫のソースには送らない（C19）」と書く |
| 読み | Chromium の日本語フォントは Meiryo（`fc-list :lang=ja`）。折り返しは本人の端末と揃わない可能性があるので、数値は数十 px の幅で読む | —— |

## 手順ごとの記録（第 2 回の一覧を見る前）

- **手順 1（要件どうしの衝突）**: (a) FR-78（そのソースの想定間隔ごとに稼働を残す）× C19（書庫のソースには送らない）× FR-35 / FR-80 / 第 1 回 Q7（書き出しを忘れたら途絶と通知）（→ R1）。
  (b) FR-35 の ★（ST03 Q26: 鳴り続ける通知は読まれない）× C19 × 二度と記録が来ない書庫のソース（→ R2）。
  (c) FR-55 の ★「**格子の群の頭**に直近に置いた書庫」× Q9 の選択肢「Must の 5 本の前」× 正典のひとスクロールと「開いた直後に 2 ソース」（→ R8）
- **手順 2（扉の幅）**: 第 2 回で新しく幅が出た扉は無い。扉 #14（動いていた / 壊れていた）の担い手が書庫の各ソースから取り込み器の論理ソースへ移った点は R1 / R4 に含めた
- **手順 3（新たに立つ一方通行）**: 取らない —— 取り込み器の走査の回数を読み手のメモリにだけ持つ（→ R3）。
  鍵の入力 —— マイアクティビティの名前（→ Q10 にある）、**形で見分けて固定の名前へ振り分ける規則**（→ R5）。
  捨てる —— 格納を保留したマイアクティビティを写しが無いときにどう読み直すか（→ R6）、Q11 の 3 つ目（→ R7）。
  C17 → C19 そのものは、**信号は書き換えられないので C17 の側が戻せず、C19 の側は読むときに投影して戻せる**（→ R1）
- **手順 4（日常）**: 毎日目に入る格子 —— 書き出しの間の既定の 4 週（→ R9）、書庫のソースの格子の本数（→ R10）、二度と来ないソースの途絶（→ R2）。
  気づけないこと —— 取り込み器が動いていない（→ R4）
- **手順 5（既存コードと要件）**: `decide` の空白日の判定が正典の (8) と違う（前か後ろ）。書庫のソースは想定間隔が 60 日で中間の空きが長く、差が出やすい（→ R11）。
  `AchievementPanel` の実寸と proto の達成の寸法が違う（→ R8）

---

## R1. C19（書庫のソースに生存信号を送らない）は失われるものが無く問わずに決めてよいが、FR-78 の本文と食い違ったまま要件に戻していない
- 成果物: openspec/changes/st12-archive-ingestion/deep.md（C19）/ docs/requirements.md（FR-78）
- 根拠: docs/requirements.md:308-311（FR-78「WHILE あるソースの収集が有効である THE SYSTEM SHALL **そのソースに登録された想定間隔ごとに**、収集側が稼働していることと、**そのソースを**取得できる状態かを稼働記録に残す」。ST12 の ★ は無い）/ deep.md:195-198（C19。「FR-78 の担い手は取り込み器なので」と読み替えている）/ docs/stories/INDEX.md:172（訂正の表だけに「書庫のソースには送らない」）/ migrations/202609111112_immutable_heartbeat.sql:15（信号は書き換えを拒む）/ design.md:225（書庫のソースの状態は記録だけで決まる）
  失われるものの確認: C19 では置き場の読める / 読めないは `s01-archive-inbox` の信号に残り、書庫の各ソースにとっての「取得できる状態か」はその 1 本と同じ値なので、後から書庫のソースへ投影できる（読むときに導く・移行で足す）。逆に C17 で書庫の各ソースに入れた信号は消せず、「途絶にならない」を戻せなかった。**扉を開けたままにするのは C19 の側**なので、A で止める理由は無い。
  ただし C19 は「要件どうしの矛盾（FR-78 の本文 × FR-35 / FR-80 / 第 1 回 Q7 の意図）を AI が解いた」型で、CLAUDE.md が記録している失敗と同じ形。要件の本文は変わっておらず、INDEX の訂正の表を読まない者には FR-78 違反に見える
- kind: conflict
- 提案: 問いは足さない（C のまま）。FR-78 に ★ 2026-09-15 で「書庫から取り込むソースの稼働は、取り込み器そのものの論理ソースが担う（書庫の各ソースには残さない。ST12 の C19）」を足し、deep.md の「要件へ戻すもの」の表に FR-78 の行を足す。第 2 回の答えを貼り戻す欄で C19 への異論を拾えるよう、JSON の `note` に「異論は Q9 の補足に C19 と書く」を 1 行足す
- 処置: fixed D10 仮 — FR-78 に ★ 2026-09-15 で「書庫から取り込むソースの稼働は取り込み器そのものの論理ソースが担う（書庫の各ソースには残さない）」を足し、deep.md の「要件へ戻すもの」の表に FR-78 の行を足した。JSON の note に「異論は Q9 の補足に番号で」を足した

## R2. C19 で、移行前のロケーション履歴の 3 本は取り込んだ日から永久に「途絶」になり、FR-35 の通知が鳴り続ける
- 成果物: openspec/changes/st12-archive-ingestion/deep.md（C19 / 後続へ送るもの ST14）/ design.md（D2 / D10）/ proto-r2.html
- 根拠: deep.md:35（移行前のロケーション履歴は「手元に保存したファイルだけ」—— 新しい記録は二度と来ない）/ design.md:76,82（`c03-legacy-location` / `-visit` / `-activity` を想定間隔 60 日で登録）/ design.md:225（書庫のソースは記録だけで判定）/ coverage.rs:806-813（前後 60 日に活動が無い日は途絶）/ docs/requirements.md:294-299（FR-35 は最後の記録から 180 日で通知。★ ST03 Q26「退役したソースを除外しないと書庫系は 180 日後から永久に鳴り、**鳴り続ける通知は読まれなくなるので本物の途絶も見逃す**」）。
  C17 のときは信号がこの 3 本を「動いていた・記録なし」に保っていたので起きなかった。**C19 に変えたことで新しく立った日常の影響**で、deep.md の ST14 への申し送り（:252-254）にも proto-r2 の場面（`proto-r2.html:122-129,269-271` は視聴履歴・タイムライン（訪問）・Chrome の 3 本だけ）にも無い。取り込んだ瞬間に最後の出来事が 180 日より前なので、ST14 ができた日から鳴る
- kind: daily
- 提案: B（仮でよい。登録簿の値なので戻せる）として足すか、C として D2 に書く。既定の候補は「移行前の 3 本は、取り込み器が最後の出来事の日の翌日を退役した日として登録簿に置く（格納は退役で止まらない。`retired_on` は格納の側で見ていない）」。proto-r2 に移行前のロケーション履歴の格子を足し、1 年ぶんで途絶 / 退役がどう見えるかを描く
- 処置: fixed D2 仮 — 移行前のロケーション履歴の 3 本を、読み終えたら最後の出来事の日の翌日で退役させる（延ばすだけ）を design D2（仮と反転条件）と deep.md の C21 に置き、spec に Scenario「移行前のロケーション履歴は読み終えると退役する」、tasks 5.3 に割り当てた。proto-r2 に退役して畳まれた 3 本を描いた

## R3. 取り込み器の走査の回数は読み手のメモリにだけあり、PC を毎晩切る運用では毎日その日の回数が消える
- 成果物: openspec/changes/st12-archive-ingestion/design.md（D10（仮））/ specs/external-ingestion/spec.md（生存信号の Requirement）
- 根拠: design.md:226-228（信号は日の最初の走査で 1 件、`attempts` は前回の信号からの走査の回数、「**回数は読み手のメモリに持ち、再起動で 0 から数え直す**」）/ design.md:10（S-01 は自宅 PC でネイティブに動く）/ review/spec.md:104（R8 が「夜に PC を切る運用」を挙げている）/ docs/requirements.md:334（FR-78 ★「**載せなかった期間の取得率は後から作れない**」）/ spec.md:436-439（Scenario は再起動を挟まない 5 回 / 3 回だけ）。
  日の最初の走査で信号を送った後に数えた回数は、次の日の最初の走査で送られる。その間に PC を切ると回数は消え、翌朝の信号は起動後の 1 回だけを載せる。**毎晩切るなら、毎日ほぼ全部の回数が残らない。** 反転条件（「欠けが読みを誤らせると分かったとき」）が成り立つ頃には、過去の回数は取っていない
- kind: irreversible
- loss: uncaptured
- 提案: 問いにしない（C「持つ側」）。回数を `core.archive_sighting` と同じく書き換えてよい表に持ち、再起動をまたいで積む、を D10 の既定にして（仮）を外す。spec に「走査の後で起動し直しても、次の生存信号の回数はその走査を含む」Scenario を足す
- 処置: escalated — 問いにはしない（持つ側は費用が小さい = 扉を開けたままにする既定）。回数を書き換えてよい表 `core.archive_scan_counter` に持ち再起動をまたいで積む、を deep.md の C20 に R3 つきで記録し、design D10 / D14、spec に Scenario「起動し直しても走査の回数は失われない」、tasks 1.1 / 8.1 に落とした。本人へは Q9 の context の C20 で見せた

## R4. 取り込み器が動いていないとき、画面のどこにも出ない（FR-55 の理由がそのまま起きる）
- 成果物: openspec/changes/st12-archive-ingestion/proto-r2.html / design.md（D1 / D12）/ deep-questions-r2.json（Q9）
- 根拠: design.md:62（`ASHIATO_ARCHIVE_USER_ID` が無ければ取り込み器を起こさず、ログに 1 行だけ）/ design.md:84（`s01-archive-inbox` は `c03-` で始まらないので格子に出ない）/ design.md:253,263（箱に出るのは直近の信号の `capturable = false` だけ。`inbox` が `null` のとき・最後の信号が何日も前のときの文言が無い）/ docs/requirements.md:464-465（FR-55 ★「読めなかった書庫が画面に無いと、置いたのに入っていないことに気づけない（書庫は約 7 日で失効する）」）/ proto-r2.html:122-129（場面に「取り込み器が止まっている」が無い）。
  設定の漏れ・異常終了で取り込み器が止まると、箱は前の書庫の「入った N 件」を出し続け、書庫のソースは 60 日まで「動いていた・記録なし」。C19 で取り込み器の稼働の証拠は格子に出ない 1 本に移ったので、それを画面に出す経路は箱しか無い
- kind: daily
- 提案: Q9 の proto に場面「取り込み器が止まっている（最後の生存信号が 3 日前 / 一度も無い）」を足し、箱にそれを文字で出す形を描く。文言と閾値は B（推奨を既定。例「取り込み器の最後の確認: N 日前」、想定間隔 1 日の 3 倍で出す）
- 処置: fixed D12 仮 — 取り込み器の最後の信号が 3 日より前か一度も無いとき箱に「取り込み器の最後の確認: N 日前」を出す、を deep.md の C22、design D12（閾値だけ仮）、spec の SHALL と Scenario「取り込み器が止まっていることが画面に出る」、tasks 10.3 に置いた。proto-r2 に場面「取り込み器が止まっている」を足した

## R5. Q10 の前提「他の 5 つは名前が固定なので、この問いに関わらない」は誤り。固定の名前へ振り分ける形の規則（D3（仮））も確認の前に凍結される
- 成果物: openspec/changes/st12-archive-ingestion/deep-questions-r2.json（Q10 の context）/ design.md（D3）
- 根拠: deep-questions-r2.json:25（「他の 5 つ（タイムライン・ロケーション履歴・YouTube・Chrome）は名前が固定なので、この問いに関わらない」）/ design.md:95-104（「**形で見分け、名前で補う**」。YouTube は「項目が `header` / `time` / `titleUrl` を持つ配列で `titleUrl` が `watch?v=`」、マイアクティビティは「`products` を持ち、**YouTube の 2 つに当たらない**」）/ design.md:108（「マイアクティビティの中の YouTube の項目と YouTube の視聴履歴は同じ出来事を 2 経路で持つ。どちらも入れる」）/ design.md:110（D3 の反転条件は「実物の形が表と違うとき」）/ gates.sql:55-60。
  design 自身が「マイアクティビティに YouTube の視聴の項目がある」と書いており、視聴の項目は動画の URL を持つので、D3 の表の順で見ると `c03-youtube-watch` に振り分けられる（D3:108 の「2 経路で両方残す」と逆）。表の形が実物と違ったと分かっても、既に入った行の論理ソースは直せない —— Q10 と同じ `rewrite-all` が、**名前の固定された 5 つの側にも**、`archive-shape.sh` を回す前の最初の走査で起きる。1 ファイルに視聴と検索が混ざるときの振り分けの単位（ファイルか項目か）も design に無い
- kind: premise
- loss: rewrite-all
- 提案: Q10 の context の一文を直し、推奨の選択肢の範囲を「形の確認の印を置くまで、**書庫の中身の格納を始めない**（台帳と写しには残す）」に広げるか、選択肢を 1 つ足す（「マイアクティビティだけ保留」と「全部保留」を分ける）。D3 に振り分けの単位と、マイアクティビティの中の YouTube の項目をどちらへ入れるかを書く
- 処置: escalated — Q10 の context の誤った一文を直し、問いの範囲を「Takeout の書庫の中身の振り分けの規則」に広げた（推奨: 形の確認の印を置くまで Takeout の書庫の中身は格納しない。選択肢に「マイアクティビティだけ確認を待つ」を足した）。deep.md の Q10 の出所に deep-r2 R5 を記録。design D3 の振り分けの単位は Q10 の答えの後で書く（tasks 5.5 は答えが入るまで着手しない）

## R6. Q10 の推奨（保留して後で写しから読み直す）は、写しを残さない設定のとき保留した分が失われうることを書いていない
- 成果物: openspec/changes/st12-archive-ingestion/deep-questions-r2.json（Q10 の選択肢 1）
- 根拠: deep-questions-r2.json:30-33（「写しと台帳には残す。印を置いたら写しから読み直す」。`irreversible` の欄が無い）/ design.md:57（`ASHIATO_ARCHIVE_KEEP_COPIES=false` で写しを作らない）/ design.md:201-203（読み直しは写しか置き場のファイルから。どちらにも無ければ読み直さない。**取り込み済みへ移した書庫は走査しない**）/ deep-questions-r2.json:47-69（Q11 は写しの設定の意味そのものを問うている）/ docs/requirements.md:836（EXT-B: Takeout の書庫は約 7 日で失効）。
  写しを残さない設定で専用のフォルダに置いた書庫は、保留したマイアクティビティを写しから読み直せず、取り込み済みの書庫を本人が消せば戻らない。Q11 の答え次第で推奨の選択肢の不可逆が変わるのに、Q10 と Q11 が互いに触れていない
- kind: irreversible
- loss: discarded
- 提案: 選択肢 1 に `irreversible` を足す（「写しを残さない設定のとき、保留した分は本人が書庫を置き直すまで読めない。書庫を消せば戻らない」）。または「保留した製品のファイルは設定に関わらず写しを作る」を選択肢 1 の detail に入れて不可逆を消す
- 処置: escalated — Q10 の推奨の選択肢に「確認待ちの書庫のファイルは、写しの設定に関わらず写しを作る」を足し、写しを残さない設定で保留分が読み直せなくなる経路を消した。他の選択肢に不可逆を書いた。deep.md の Q10 に R6 を記録

## R7. Q11 の 3 つ目の選択肢は FR-14 の本文（本人が Q2 で決めた）と逆で、不可逆の記述が無い。2 つ目も `irreversible` の欄が空
- 成果物: openspec/changes/st12-archive-ingestion/deep-questions-r2.json（Q11 の選択肢 2 / 3）
- 根拠: docs/requirements.md:177（FR-14 ★「**置き場にある本人のファイルは消さない**」。第 1 回 Q2 / C14）/ spec.md:269-270（「取り込み済み」は専用のフォルダの中）/ deep-questions-r2.json:66-68（3 つ目の detail は「専用のフォルダが溜まらない。写しは別に残る（写しの設定とは独立）」だけ）/ design.md:212-213（写しは読んだ製品のファイルだけで、書庫そのものは写さない）/ deep.md:22-23（Q1 で「丸ごと写す」は選ばれていない）/ docs/requirements.md:836（EXT-B: 約 7 日で失効・5 回まで）/ deep-questions-r2.json:62-64（2 つ目の不可逆は detail の文にあるが `irreversible` の欄に無い）。
  3 つ目を選ぶと、読まなかった製品（写真など）と、写しを残さない設定のときは読んだ製品も、書庫の失効後は取れない。Q11 を B にしてよい理由（既定が「残す」なので切り替えるまで何も消えない）は成り立つので、分類は変えなくてよい
- kind: irreversible
- loss: discarded
- 提案: 3 つ目に `irreversible`（「書庫の中の読まなかった製品は、Takeout の書庫の失効後は取れない。写しを残さない設定なら読んだ製品も」）と「FR-14 の『本人のファイルは消さない』を改める」を足す。2 つ目の不可逆を `irreversible` の欄に移す
- 処置: escalated — Q11 の 2 つ目・3 つ目に `irreversible` を書き、3 つ目の detail に「第 1 回で決めた FR-14 の『本人のファイルは消さない』を改める」を足した。B のまま（既定が残すなので切り替えるまで何も消えない）。deep.md の Q11 に R7 を記録

## R8. proto-r2 の達成の表は実物より約 116 px 低く、「Must の 5 本の前」は実物ではどの場面でもひとスクロールと「開いた直後に 2 ソース」を破る。FR-55 の本文とも食い違う
- 成果物: openspec/changes/st12-archive-ingestion/proto-r2.html / deep-questions-r2.json（Q9）
- 根拠: 実測（冒頭の表）: proto の達成 113 px / `AchievementPanel.tsx` と同じ形 229 px。proto の「前」の Must の下端 1,245〜1,334 px に 116 px を足すと **1,361〜1,450 px**（予算 1,280 px。正典 `collection-coverage/spec.md:623,731`。ST02 第 8 回 Q30 の本人の決定）。
  「前」の 2 本目の格子の 4 週目の下端 611〜700 px に足すと **727〜816 px**（正典 :622,726「開いた直後に 2 ソース以上の直近 4 週」= 640 px）。「後ろ」は約 1,247 px / 約 613 px で両方に収まる。
  proto の画面は「初めて」「置き直し」「100 日忘れた」の「前」を「収まる」と印字する（`proto-r2.html:277,292`）ので、本人は既に決めた予算を破る側を「収まる」と読んで選べる。
  また FR-55 の ★（docs/requirements.md:462）は「**格子の群の頭に**」と書いており、spec は「書庫のソースの格子の群の頭」（spec.md:505）。「前」を選ぶと要件の本文も変わる
- kind: premise
- 提案: proto の達成を実物と同じ構成（見出し・判定・確定までの行・表 1 + 5 行）で描き直してから渡す。そのうえで「前」の選択肢の d に「ST02 第 8 回 Q30 の 1,280 px と開いた直後の 2 ソースを超える（予算を変える判断になる）」を出すか、軸 1 を問いから外して「後ろ」を既定にする（第 1 回の読み取りと spec のまま）。軸 2（読んでいる間の表示）だけなら画面の構造として残る
- 処置: fixed proto-r2.html — 達成の表を `AchievementPanel.tsx` と同じ構成（見出し・判定・確定までの行・表 1 + 5 行。one-scroll の試験と同じ「確定まであと N 日」）で描き直し、実測に「2 本目の Must の直近 4 週の下端（640 px）」を足した（再計測で「後ろ」は 1,247 px / 620 px で収まる）。軸 1 の「前」の説明に「ST02 第 8 回 Q30 の予算を超える —— 選ぶと予算を変える判断になり、FR-55 の『格子の群の頭に』も直す」を書き、Q9 の context にも書いた

## R9. Q9 の context「ほとんどが『記録あり』の段」は、毎日見る既定の 4 週では成り立たない。proto に書き出しの間の場面が無い
- 成果物: openspec/changes/st12-archive-ingestion/deep-questions-r2.json（Q9 の context）/ proto-r2.html
- 根拠: deep-questions-r2.json:13（「視聴履歴やタイムラインはほぼ毎日記録があるので、ほとんどが『記録あり』の段」）/ proto-r2.html:122-129,148,151（場面の最終日は 3 日前・1 日前・100 日前の 3 つだけ）/ proto-r2.html:164（直後の記録が無い日は直前から 60 日以内なら②）/ coverage.rs:806-813（同じ）/ CoverageGrid の既定は直近 4 週（`proto-r2.html:114` の `INITIAL_WEEKS=4`、正典 :607-622）/ 第 1 回 Q7 の答え（2 か月ごとに書き出す。deep.md:133）/ 第 1 回 Q5 の context は逆に「記録の無い日がほとんどになり『動いていた・記録なし』か『それ以外』の段で埋まる」と書いていた（deep-questions.json の Q5）。
  書庫の中の最新の出来事は書き出した日までなので、書き出してから 28 日を過ぎると**既定の 4 週は全部「動いていた・記録なし」**になり、次の書き出しまでそのまま、60 日を過ぎると全部「途絶」。2 か月ごとの運用では、開いたときに「記録あり」が見えるのは書き出した直後の数週間だけ。週を選んだときの文字も、まだ書き出していない日に「動いていた・記録なし」と出る
- kind: premise
- 提案: context の一文を「書き出した直後は記録ありの段、日が経つと既定の 4 週は『動いていた・記録なし』で埋まり、60 日を超えると途絶」に直し、proto に場面「前の書き出しから 40 日」を足す。書庫のソースの見出しの「まで（N 日前）」がこの読み違いを補えているかを本人が同じ絵で見られるようにする
- 処置: fixed deep-questions-r2.json — Q9 の context の「ほとんどが記録あり」を「書き出した直後は記録あり、日が経つと既定の 4 週は動いていた・記録なしで埋まり、60 日を超えると途絶」に直し、proto-r2 に場面「前の書き出しから 40 日」を足した

## R10. proto-r2 は書庫のソースの格子を 3 本しか描いておらず、実物の本数（固定 10 本 + マイアクティビティの製品ごと）で何が縦に積まれるかを本人が見ていない
- 成果物: openspec/changes/st12-archive-ingestion/proto-r2.html / deep-questions-r2.json（Q9）
- 根拠: proto-r2.html:269-271（視聴履歴・タイムライン（訪問）・Chrome の 3 本）/ design.md:75-80（固定の 10 本: タイムライン 4・移行前 3・YouTube 2・Chrome 1）/ design.md:88-90（マイアクティビティは製品ごとに増える。検索・Discover・Play・マップ・アシスタント…）/ design.md:248（`/coverage` は登録簿の `c03-*` を全部足す）/ tasks.md:158（画面の試験は 12 本で見る）/ deep.md:106（第 1 回の proto の画面全体は 1,850 px）。
  格子 1 本は約 190 px（実測: Must の 5 本で約 960 px）なので、12 本で約 2,300 px、マイアクティビティの製品が増えればさらに伸び、退役したソースはその後ろへ押される。第 1 回で「ソースの扱いを揃える（格子）」を選んだ絵も 4 本だった（proto.html:189-192）。何が同時に見えるか・畳むかは画面の構造で、文字でも問われていない
- kind: daily
- 提案: proto-r2 の書庫のソースを design の登録どおりの本数（固定 10 本 + マイアクティビティ 2〜5 本）で描き、実測欄に「書庫のソースの最後の格子の下端」「退役の先頭の上端」を足す。畳みたいかは Q9 の補足で拾う（畳む形を選ぶなら visual の軸を 1 つ足す）
- 処置: escalated — proto-r2 の書庫のソースを登録どおりの本数（9 本 + 退役した移行前の 3 本）で積み、実測に「書庫のソースの最後の格子の下端」を足した。開いた直後に格子を見せるか畳むかを Q9 の軸 3 として本人に問うた。deep.md の Q9 に R10 を記録

## R11. proto と Q9 の context が述べる空白日の判定規則は、正典の (8) とも実装とも違う
- 成果物: openspec/changes/st12-archive-ingestion/proto-r2.html / deep-questions-r2.json（Q9 の context）
- 根拠: proto-r2.html:165（直前と直後の記録の**間**が 60 日を超えれば途絶）/ deep-questions-r2.json:13・proto-r2.html:54,290（「記録の間が 60 日以内の日は『動いていた・記録なし』、超えた日は『途絶』」）/ collection-coverage/spec.md:381-384（(8) **直前の**記録・信号からの経過が想定間隔を超えれば途絶 → (9) 前後が想定間隔以内なら②）/ coverage.rs:806-813（**前か後ろ**に想定間隔以内の活動があれば②）。
  中間に 100 日の空きがある場合、proto の規則では 100 日全部が途絶、正典では 61 日目以降が途絶、実装では 1 日も途絶にならない（40〜60 日目は後ろの記録に近い）。proto の場面には 60 日を超える中間の空きが無いので絵は変わらないが、本人が読む規則の文が 3 つ目の読みになっている。書庫のソースは想定間隔が長く、視聴しなかった 2〜3 か月（1 年ぶんの表示）で差が出る
- kind: premise
- 提案: context と proto の文を正典の (8) の言い方（「直前の記録から 60 日を超えた日は途絶」）に揃える。正典と実装の食い違い（`decide` が後ろの活動も見る）はこの change の問いにせず、ST02 の後追いとして `docs/handoff/` に書く
- 処置: fixed deep-questions-r2.json — Q9 の context と proto-r2 の判定を正典の (8) の言い方（直前の記録から 60 日を超えた日は途絶）に揃えた。正典と実装（`coverage.rs:806-813`）の食い違いは ST02（archive 済み）の後追いとして `docs/handoff/ST02.md` に書いた


## 処置のまとめ（呼び出し元が付けた）

11 件すべてに処置を付けた。

| 処置 | 件数 | 指摘 |
|---|---|---|
| 問いの範囲・選択肢・軸を直した（本人に問う） | 5 | R5・R6（Q10）/ R7（Q11）/ R10（Q9 の軸 3）/ R3（C20 として Q9 の context で見せる） |
| 仮決めとして design に置いた | 3 | R1（D10 + FR-78 ★）/ R2（D2。C21）/ R4（D12。C22） |
| 問いの前提を直した | 3 | R8（proto の達成の表）/ R9（context と場面）/ R11（判定の文。実装との食い違いは ST02 の handoff） |
