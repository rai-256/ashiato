## MODIFIED Requirements

### Requirement: 稼働記録は新しく入った記録だけを数える

WHEN 記録を格納する THE SYSTEM SHALL その日のその論理ソースが稼働していたことを記録する。
THE SYSTEM SHALL 稼働記録の件数を、**新しく入った記録の数**とし、重複として弾いた分を数えない。
THE SYSTEM SHALL 重複だけが届いた日についても、稼働していたことは記録する。
THE SYSTEM SHALL この「日」を、記録の出来事時刻を **`Asia/Tokyo` に固定して**区切ったものとする。
THE SYSTEM SHALL 記録に付いたタイムゾーンで日を区切らない。

導出元: FR-33, NFR-13。ST01 の design D13。深掘り Q2。

部分失敗のときに成功分を再送するので、**重複の到着は常態**になる。重複まで数えると
件数が実態から離れ、稼働状況の画面がその値を読んだときに誤る。
一方で行そのものを立てないと、「欠損」と「重複だけ届いた」が区別できなくなる。

日の区切りを固定するのは、**NFR-13 の分母がぶれるため**。記録ごとのタイムゾーンで切ると
東西の移動で 1 年が 364 日にも 366 日にもなり、NFR-13 の分母がぶれる。
`Asia/Tokyo` は本人が選んだ（深掘り Q2）。当初の実装は UTC 固定だった。

#### Scenario: 重複は件数に加えない

- **WHEN** 同じ記録を 2 回送る
- **THEN** その日の稼働記録の件数は 1 である
- **AND** その日が稼働していたことは記録されている

#### Scenario: 日本時間の 0 時を境に別の日になる

- **WHEN** 出来事時刻が `2026-03-01T14:59:59Z` の記録と `2026-03-01T15:00:01Z` の記録を格納する
- **THEN** 前者は `2026-03-01` の稼働記録に、後者は `2026-03-02` の稼働記録に入る

#### Scenario: 記録のタイムゾーンが違っても日の区切りは動かない

- **WHEN** 記録に付いたタイムゾーンが `America/New_York` の記録を、出来事時刻 `2026-03-01T15:00:01Z` で格納する
- **THEN** その記録は `2026-03-02` の稼働記録に入る

## ADDED Requirements

### Requirement: 収集側は想定間隔ごとに生存信号を送り、取得できる状態かを併せて報告する

WHILE あるソースの収集が有効である THE SYSTEM SHALL そのソースに登録された想定間隔ごとに送られる生存信号を受け付ける。
THE SYSTEM SHALL 生存信号に、そのソースを**取得できる状態か**（権限・センサ・接続）を含めることを要求する。
THE SYSTEM SHALL 取得できない状態を報告する生存信号について、**何が満たされていないか**を残す。
THE SYSTEM SHALL 生存信号に、**前回の生存信号からの取得の試行回数と成功回数**を含めることを要求する。
THE SYSTEM SHALL 記録が 1 件も届かない日についても、生存信号が届いていればその日の稼働を残す。
THE SYSTEM SHALL 登録簿に無いソースからの生存信号を拒否する。

導出元: FR-78。深掘り Q1, Q5。

FR-33 は記録を生成したときにしか書かないので、**記録が 0 件の日は行が無いだけ**になり、
扉 #14 が求める「動きが無かったのか / 収集が壊れていたのか」を区別できない。区別は遡って作れない。

取得可否を併せて載せるのは、**収集プロセスが生きたまま取れていない**状態があるため。
Android は長期間使っていないアプリの権限を自動で剥がし、位置の権限が剥がれると
プロセスは動いたまま記録が 0 件になる。稼働だけを送っていると、壊れているのに「動いていた」と残る。

#### Scenario: 記録が 0 件でも生存信号があれば稼働が残る

- **WHEN** ある日にそのソースの記録が 1 件も届かず、生存信号だけが届く
- **THEN** 稼働状況を引くと、その日のそのソースが稼働していたことが返る
- **AND** その日の記録の件数は 0 である

#### Scenario: 想定間隔より細かい空きが取得率として残る

- **WHEN** 前回の信号からの間に取得を 360 回試みて 230 回成功したことを示す生存信号が届く
- **THEN** 稼働状況を引くと、その区間の試行回数と成功回数が返る
- **AND** その区間は収集側が生きていたものとして扱われる

#### Scenario: 信号が来ない区間は取得率では埋まらない

- **WHEN** ある区間について生存信号が 1 件も届かない
- **THEN** 稼働状況を引いても、その区間の取得率は返らない
- **AND** 収集側が生きていたことを示すものが無い

#### Scenario: 取得できない状態が理由とともに残る

- **WHEN** 位置の権限が無いことを報告する生存信号が届く
- **THEN** 稼働状況を引くと、その日の状態が「動いていたが取れない状態だった」として返る
- **AND** 何が満たされていないか（権限）が返る

#### Scenario: 登録簿に無いソースの生存信号は拒否される

- **WHEN** ソース登録簿に無い論理ソース名で生存信号を送る
- **THEN** 要求は拒否され、生存信号は残らない

### Requirement: 生存信号の受け口は複数件をまとめて受け取り、1 件ごとの結果を返す

THE SYSTEM SHALL 生存信号の受け口が、複数件を 1 回の要求でまとめて受け取れるようにする。
THE SYSTEM SHALL 1 件だけを裸で送る要求も受け取る。
THE SYSTEM SHALL 一部が不正でも、正しい分は受け付ける。
THE SYSTEM SHALL 1 件も受け付けなかったときにだけ、要求そのものを拒否する。
THE SYSTEM SHALL 拒否の応答に、受け取った値や内部の詳細を含めない。
THE SYSTEM SHALL 取得できない状態を報告しながら**何が満たされていないかを持たない**生存信号を、
受け付けない。
THE SYSTEM SHALL 取得の試行回数または成功回数を持たない生存信号を、受け付けない。
THE SYSTEM SHALL 成功回数が試行回数を超える生存信号を、受け付けない。

導出元: FR-78。ST01 が `record-envelope` に置いた取り込み口と同じ形に揃える。

収集側は部分失敗の後で再送するので、1 件ずつの受け口だと再送のたびに要求数が膨らむ。
理由を持たない「取れない」を断るのは、**それが状態③の証拠にならない**ため ——
「取れない状態だった」とだけ残っても、権限なのかセンサなのか接続なのかが分からなければ、
扉 #14 が求めた区別に届かない。

#### Scenario: 複数件を 1 回で受け取る

- **WHEN** 生存信号を 3 件まとめて 1 回で送る
- **THEN** 3 件とも受け付けられ、1 件ごとの結果が返る

#### Scenario: 一部が不正でも正しい分は受け付けられる

- **WHEN** 3 件のうち 1 件が登録簿に無いソースで、2 件は正しい
- **THEN** 正しい 2 件は受け付けられ、不正な 1 件だけが拒否される

#### Scenario: 1 件も受け付けなかったときだけ要求が拒否される

- **WHEN** 送った生存信号がすべて拒否される
- **THEN** 要求そのものが拒否される

#### Scenario: 拒否の応答に受け取った値が含まれない

- **WHEN** 登録簿に無いソース名で生存信号を送る
- **THEN** 応答に、送ったソース名も内部の詳細も含まれていない

#### Scenario: 理由の無い「取れない」は受け付けられない

- **WHEN** 取得できない状態を示しながら、何が満たされていないかを持たない生存信号を送る
- **THEN** その生存信号は拒否される

#### Scenario: 取得の回数を持たない信号は受け付けられない

- **WHEN** 取得の試行回数を持たない生存信号を送る
- **THEN** その生存信号は拒否される

#### Scenario: 成功が試行を超える信号は受け付けられない

- **WHEN** 試行回数 10・成功回数 11 の生存信号を送る
- **THEN** その生存信号は拒否される

### Requirement: 生存信号は一次記録と同じ保護を受ける

THE SYSTEM SHALL 受け付けた生存信号を後から書き換えない。
THE SYSTEM SHALL 同じ生存信号が複数回届いたとき、行を 1 つだけ残す。
THE SYSTEM SHALL 生存信号について、収集側から受け取った原文をそのまま残す。
THE SYSTEM SHALL 生存信号の受信時刻を**時刻として**（日に丸めずに）残す。
THE SYSTEM SHALL 生存信号に利用者識別子を持たせる。

導出元: FR-78, FR-29, FR-30。深掘り Q13, Q14。

生存信号は「動いていた・記録なし」と「動いていたが取れない状態だった」の**唯一の証拠**で、
扉 #14 が「後から区別する唯一の手段」と呼んだ当のものにあたる。だから証拠として扱う。
収集側は部分失敗の後で再送する（「重複の到着は常態」）ので、冪等キーが無いと再送のたびに行が増える。

受信時刻を丸めないのは、**日境界の決定を行に焼き込まないため**。丸めると
`Asia/Tokyo` という選択が過去の行に固定され、後から引き直せなくなる。

#### Scenario: 同じ生存信号を 2 回送っても 1 行

- **WHEN** 同じ冪等キーの生存信号を 2 回送る
- **THEN** 残る生存信号は 1 行である

#### Scenario: 格納された生存信号は書き換えられない

- **WHEN** 格納済みの生存信号を更新しようとする
- **THEN** その操作は拒否される

#### Scenario: 受信時刻が時刻のまま残る

- **WHEN** 生存信号を格納する
- **THEN** その行は日付ではなく時刻を保持しており、日は集計時に `Asia/Tokyo` で引かれる

### Requirement: ソースごとに収集を開始した日が残る

THE SYSTEM SHALL 各ソースについて、収集を開始した日を
**そのソースのいちばん古い記録が作られた日**（生存信号なら発信された日）として残す。
THE SYSTEM SHALL 記録も生存信号も 1 件も無いソースを、まだ開始していないものとして扱う。
THE SYSTEM SHALL 収集開始日より古い記録が後から届いたとき、**収集開始日をその日まで遡らせる**。
THE SYSTEM SHALL 収集開始日が遡ったとき、その日以降の状態と達成の判定を引き直せるようにする。
THE SYSTEM SHALL 収集開始日より前の日を、そのソースについて「導入前」として扱う。
THE SYSTEM SHALL 収集開始日より前の日を、NFR-13 の分母に数えない。
THE SYSTEM SHALL **登録簿にそのソースの行ができた日より前**の記録・生存信号を、
収集開始日の計算から除く。**記録・生存信号そのものは受け取り、保存する。**
THE SYSTEM SHALL 引き継ぎ元のあるソースについて、**引き継ぎの鎖の根の収集開始日**を
そのソースの収集開始日とする。

導出元: FR-79, FR-61。深掘り 第 4 回 Q11, 第 5 回 Q22, 第 6 回 Q24, 第 7 回 Q26,
第 8 回 Q29, Q31。

**登録簿に行ができた日より前を外すのは本人の決定**（第 8 回 Q29）——
端末の時計が狂った生存信号が 1 件届くと収集開始日がその日まで落ちるが、
**収集開始日は前にしか動かない**（第 7 回 Q26）ので、正しい日を送り直しても戻らない。
実測では 1999 年に落ち、成功条件 1 が「**確定・未達**」で固まった。
**捨てるのではなく、計算から外す** —— 本人の答えは「受けるが、収集開始日の計算から外す」。

**引き継ぎの鎖から引くのも本人の決定**（第 8 回 Q31）——
ソースを分けて古い名前を退役させたとき、新しい名前が収集開始日を自分の登録日から
数え直すと、**名前を分けた日に成功条件 1 の窓が振り出しに戻る**。
1 年の連続性は成功条件 1 そのものなので、鎖の根から引く。

**登録簿に登録した日ではない**（第 5 回 Q22）—— ウィンドウとブラウザ履歴は PC 側の収集が
まだ存在せず、登録だけ先にあると**作っていないのに計測が始まる**。

**受け取った日でもない**（第 6 回 Q24）—— 圏外で溜めて送ると受信は数日後になり、
**記録が実際にある日が「導入前」になって NFR-13 の分母からも落ちる**。

**後から古い記録が届けば遡る**（第 7 回 Q26）。圏外での保持があるので
**いちばん古い記録が最初に届くとは限らない**。遡らせないと同じ誤りが残る。
窓の起点も一緒に動くが、**動くのは入力であって判定式ではない** ——
式が同じなら、同じ入力から必ず同じ答えが出る。

これが無いと、「記録も生存信号も無い日」が**まだ始めていなかった**のか
**収集が壊れていた**のかを区別できず、稼働状況の画面は導入前の期間がすべて不明で埋まる。
**day one から持たないと後から作れない。**

#### Scenario: 収集開始日より前は導入前になる

- **WHEN** あるソースの収集開始日が `2026-04-01` で、`2026-03-20` の状態を引く
- **THEN** その日の状態は「導入前」である

#### Scenario: 1 件も届いていないソースは開始していない

- **WHEN** 登録簿にあるが記録も生存信号も 1 件も届いていないソースの状態を引く
- **THEN** どの日も「導入前」であり、「途絶」になる日は無い

#### Scenario: 最初の信号が発信された日が開始日になる

- **WHEN** あるソースの最初の生存信号が `2026-04-01`（`Asia/Tokyo`）に発信される
- **THEN** そのソースの収集開始日は `2026-04-01` である
- **AND** `2026-03-31` 以前はすべて「導入前」である

#### Scenario: 溜めてから送っても開始日は取得した日になる

- **WHEN** `2026-04-01` から `2026-04-03` に取得した記録を、まとめて `2026-04-03` に送る
- **THEN** そのソースの収集開始日は `2026-04-01` である
- **AND** `2026-04-01` の状態は「導入前」ではない

#### Scenario: 後から古い記録が届くと開始日が遡る

- **WHEN** 収集開始日が `2026-04-05` のソースに、`2026-04-01` に取得した記録が後から届く
- **THEN** そのソースの収集開始日は `2026-04-01` に変わる
- **AND** `2026-04-01` の状態は「導入前」ではなくなる

#### Scenario: 開始日が遡ると達成の判定も引き直される

- **WHEN** 収集開始日が遡る
- **AND** そのソースの達成を引き直す
- **THEN** 分母は遡った開始日から数え直されている

#### Scenario: 導入前の日は分母に入らない

- **WHEN** 収集開始日より前の日を含む期間で達成日数を数える
- **THEN** 導入前の日は分母に数えられていない

#### Scenario: 登録より前の時刻の信号は開始日を動かさない

- **WHEN** 登録簿に `2026-04-01` に行ができたソースへ、`1999-01-01` に発信されたと称する
  生存信号が届く
- **THEN** そのソースの収集開始日は `1999-01-01` にならない
- **AND** その生存信号は保存されていて、読み出せる

#### Scenario: 汚れた収集開始日は引き直せる

- **WHEN** 収集開始日が登録より前の日で埋まっているソースについて、収集開始日を引き直す
- **THEN** 収集開始日は、登録簿に行ができた日以降でいちばん古い記録・生存信号の日になる
- **AND** 登録より前の時刻しか無いソースの収集開始日は、まだ開始していないことを表す

#### Scenario: 引き継いだソースは引き継ぎ元の開始日を継ぐ

- **WHEN** 収集開始日が `2026-04-01` のソースを引き継ぐ新しいソースの収集開始日を引く
- **THEN** 新しいソースの収集開始日は `2026-04-01` である
- **AND** NFR-13 の窓の起点も `2026-04-01` である

### Requirement: 記録も生存信号も来ない期間は途絶として残る

IF あるソースについて、そのソースに登録された想定間隔を超えて記録も生存信号も届かない
THEN THE SYSTEM SHALL その期間を「途絶」として稼働記録に残す。
THE SYSTEM SHALL この判定を、収集側からの報告を待たずに**受け手の側で**行う。
THE SYSTEM SHALL 途絶の判定を、収集開始日以降かつ意図的な停止でない期間についてのみ行う。

導出元: FR-80。深掘り 第 4 回 Q11。

扉 #14 が求める「収集が壊れていた」のうち、**アプリが OS に停止・アーカイブされた経路は
生存信号では原理的に埋まらない** —— 止まったものは自分から報告しない。
FR-35（通知）と対になるが別物で、FR-35 は本人に知らせ、これは稼働記録に残す。

#### Scenario: 想定間隔を超えて何も来ない日は途絶になる

- **WHEN** 想定間隔 6 時間のソースについて、ある日の記録も生存信号も 1 件も無い
- **AND** その日が収集開始日以降で、意図的な停止の期間でもない
- **THEN** その日の状態は「途絶」である

#### Scenario: 停止中は途絶にならない

- **WHEN** 1 日を丸ごと覆う意図的な停止がある日に、記録も生存信号も届かない
- **THEN** その日の状態は「意図的な停止」であり、「途絶」ではない

#### Scenario: 途絶は収集側の報告なしに立つ

- **WHEN** 収集側から何も届かないまま想定間隔を超えて時間が経つ
- **THEN** 稼働状況を引くと、その期間の状態が「途絶」として返る
- **AND** 収集側からは何も送られていない

#### Scenario: 想定間隔を超えない空白の日は途絶にならない

- **WHEN** 想定間隔が 60 日のソースについて、ある 1 日だけ記録も生存信号も無い
- **AND** その前後には記録がある
- **THEN** その日の状態は「途絶」ではなく「動いていた・記録なし」である

#### Scenario: 前後に何も無い空白の日は途絶になる

- **WHEN** 想定間隔が 60 日のソースについて、ある日より後に記録も生存信号も 1 件も無い
- **AND** 直前の記録からの経過が 60 日を超えている
- **THEN** その日の状態は「途絶」である

#### Scenario: 想定間隔を変えると過去の日の判定も変わる

- **WHEN** あるソースの想定間隔を 60 日から 6 時間に変える
- **AND** 変更より前の、記録も生存信号も無い 1 日の状態を引き直す
- **THEN** その日の状態は「途絶」に変わっている

### Requirement: 意図的な停止と破棄は時刻の範囲で残る

WHEN 利用者が収集を停止する THE SYSTEM SHALL 停止したソースと**期間を時刻の範囲として**残す。
IF 端末内の保持の上限を超えて記録を破棄する THEN THE SYSTEM SHALL
破棄した**期間を時刻の範囲として**と、破棄した件数を残す。
THE SYSTEM SHALL これらを自由文ではなく、機械が範囲として読める形で残す。

導出元: FR-34, FR-9。深掘り Q3。

当初の `core.coverage` は (ソース, 日, 状態) が主キーで、期間は `note` の自由文しか持てず、
FR-34 の「期間」も FR-9 の「破棄した期間と件数」も満たしていなかった。
**粗く記録した過去は後から細かくできない。**

#### Scenario: 半日の停止が時刻の範囲で残る

- **WHEN** あるソースを `2026-05-01 09:00` から `2026-05-01 18:00`（`Asia/Tokyo`）まで停止する
- **THEN** その停止は開始時刻と終了時刻を持つ範囲として残っている

#### Scenario: 破棄が期間と件数で残る

- **WHEN** 端末内の保持の上限を超えて記録を破棄する
- **THEN** 破棄した期間が時刻の範囲として残り、破棄した件数が残っている

### Requirement: ソース × 日 の状態は 8 つのいずれかに決まる

THE SYSTEM SHALL 各ソースの各日について、次の 8 つのいずれか 1 つの状態を返す ——
「記録あり」「動いていた・記録なし」「動いていたが取れない状態だった」「意図的な停止」
「破棄された期間」「途絶」「導入前」「**退役**」。
THE SYSTEM SHALL 複数の条件が同時に成り立つ日について、次の順に評価し、
最初に当たったものをその日の状態とする ——
(1) 収集開始日より前なら「導入前」、
(2) **登録簿の退役した日より後なら「退役」**、
(3) その日を丸ごと覆う破棄があれば「破棄された期間」、
(4) その日を丸ごと覆う意図的な停止があれば「意図的な停止」、
(5) 記録が 1 件以上あれば「記録あり」、
(6) 取得できる状態の生存信号があれば「動いていた・記録なし」、
(7) 生存信号があれば（すべて取得できない状態なら）「動いていたが取れない状態だった」、
(8) 直前の記録・生存信号からの経過が想定間隔を超えていれば「途絶」、
(9) そうでなければ「動いていた・記録なし」。
THE SYSTEM SHALL (9) で「動いていた・記録なし」とするのは、
**その日をまたぐ前後の記録または生存信号が想定間隔以内にある**ときに限る。
THE SYSTEM SHALL 「記録が 1 件以上ある」を、**その日に出来事の時刻が属する記録があること**
として判定する（稼働記録の件数ではなく、記録そのものから引く）。
THE SYSTEM SHALL 1 日を丸ごと覆わない停止・破棄について、その日の状態を決めさせない。
THE SYSTEM SHALL 同じ入力に対して同じ状態を返す。

導出元: FR-54, FR-33, FR-78, FR-79, FR-80, FR-34, FR-9, FR-61。
深掘り Q1, Q5, 第 4 回 Q11, 第 5 回 Q19, 第 8 回 Q31。

**⑧「退役」を⑦「導入前」の直後に見るのは、両方が同じ型だから** ——
「その日はこのソースの収集期間の外」。`retired_on` は `collection_started_on` の対で、
**日付であって真偽値ではない**（ST03 の R56。真偽値だと「いま退役しているか」しか問えず、
**退役より前に起きた本物の途絶まで遡って消える**）。
判定は **「退役した日より後」** —— 退役した日そのものはまだ収集していたので、その日は本来の状態で出る。

**「記録あり」を記録そのものから引くのは ST03 の R57。**
稼働記録の件数（その日に新しく入った記録の数）で決めていたときは、
ST03 が外部サービスからの更新経路を開けて**出来事の時刻が別の日へ動いた瞬間**、
記録の無い日が「記録あり」・記録のある日が「途絶」になった（ST03 の実測）。
稼働記録の表は「新しく入った記録だけを数える」を満たし続けるが、**状態の出どころではない**。

**停止と破棄を記録より先に見るのは本人の決定**（第 5 回 Q19）——
「なぜこの日はデータが少ないのか」に画面が先に答える。1 日を丸ごと止めた日に記録が一部
残っていても、その日は「意図的な停止」として見える。

扉 #14 が稼働記録に求めた区別は「動きが無かったのか / 収集が壊れていたのか /
自分で止めたのか / バッファから破棄されたのか」の 4 つで、
3 つの表示ではそのうち「動きが無かった」と「壊れていた」を分けられない。
**③は生存信号が無ければ作れず、⑥は受け手の能動的な検知でしか作れず、
⑦は収集開始日を day one から持たないと作れない。**
**②はその日の生存信号があるか、前後を想定間隔以内の記録・生存信号に挟まれているときに立つ** ——
想定間隔が長いソース（ブラウザ履歴 24 時間、Takeout 系 60 日）では信号の無い日が間に挟まるので、
その日を⑥「途絶」にすると正常な運用が異常に見える。

#### Scenario: 丸ごと覆う停止も破棄も無い日に記録があれば記録あり

- **WHEN** ある日にそのソースの記録が 1 件以上ある
- **AND** その日を丸ごと覆う停止も破棄も無い
- **THEN** その日の状態は「記録あり」である

#### Scenario: 丸ごと覆う停止は記録より優先される

- **WHEN** 1 日を丸ごと覆う停止がある日に、そのソースの記録も 1 件以上ある
- **THEN** その日の状態は「意図的な停止」である

#### Scenario: 破棄は停止より優先される

- **WHEN** ある日を丸ごと覆う破棄と、丸ごと覆う停止が重なっている
- **THEN** その日の状態は「破棄された期間」である

#### Scenario: 生存信号だけなら動いていた・記録なし

- **WHEN** ある日に記録が 0 件で、取得できる状態の生存信号がある
- **THEN** その日の状態は「動いていた・記録なし」である

#### Scenario: 取得できない状態の信号なら固有の状態になる

- **WHEN** ある日に記録が 0 件で、取得できない状態を報告する生存信号だけがある
- **THEN** その日の状態は「動いていたが取れない状態だった」である

#### Scenario: 同じ入力から同じ状態が決まる

- **WHEN** 停止・破棄・生存信号・記録が同じ日に重なる入力を 2 回評価する
- **THEN** どちらも同じ 1 つの状態を返す

#### Scenario: 退役した日より後は退役になる

- **WHEN** `2026-05-10` に退役したソースの `2026-05-11` の状態を引く
- **THEN** その日の状態は「退役」である
- **AND** 想定間隔を超えて何も来ていなくても「途絶」にはならない

#### Scenario: 退役した日そのものは本来の状態のまま

- **WHEN** `2026-05-10` に退役したソースの、`2026-05-10` に記録がある日の状態を引く
- **THEN** その日の状態は「記録あり」である

#### Scenario: 記録の時刻が別の日へ動くと状態も動く

- **WHEN** ある日に出来事の時刻が属する記録が 1 件もなくなり、別の日へ移る
- **THEN** 元の日の状態は「記録あり」ではなくなる
- **AND** 移った先の日の状態が「記録あり」になる

### Requirement: 成功条件 1 の達成日はソースの主語で分けて数える

THE SYSTEM SHALL Must の 5 ソース（位置・アプリ利用・写真・ウィンドウ・ブラウザ履歴）について、
達成日数を次のとおり数える ——
**端末が主語**（位置・アプリ利用）は**記録が 1 件以上ある日**、
**利用が主語**（写真・ウィンドウ・ブラウザ履歴）は**取得できる状態の生存信号があった日**。
THE SYSTEM SHALL 日の区切りを `Asia/Tokyo` に固定する。
THE SYSTEM SHALL 分母から、FR-34 で記録された意図的な停止のうち **1 日を丸ごと覆うもの**だけを除く。
THE SYSTEM SHALL 分母から除いた日を、達成日にも数えない（達成日数が分母を超えないようにする）。
THE SYSTEM SHALL 取得できない状態を報告する生存信号しか無い日を、達成日に数えない。
THE SYSTEM SHALL 合否を **5 本すべてが「そのソースの分母 × 95 %」以上**として返し、
どのソースが落ちたかを残す。
THE SYSTEM SHALL 合否を、**5 ソースすべての窓が閉じた日にのみ確定**とする。
THE SYSTEM SHALL それまでの合否を**暫定**として返す。
THE SYSTEM SHALL 5 ソースすべてが収集を開始しているとき、
**確定する日と、そこまでの残り日数**を返す。
THE SYSTEM SHALL 収集が始まっていないソースがあるあいだ、確定する日も残り日数も返さない。
THE SYSTEM SHALL 数える期間を、**そのソースの収集開始日から 365 日**とする。
THE SYSTEM SHALL 365 日が経っていないソースについて、
**収集開始日から今日まで**を分母として同じ式で途中経過を返す。
THE SYSTEM SHALL 分母をソースごとに数える（収集開始日が違うため）。
THE SYSTEM SHALL 達成日数と分母の両方を返す。
THE SYSTEM SHALL **退役した日より後の日**を、そのソースの分母にも達成日にも数えない。
THE SYSTEM SHALL Must の 5 ソースのうち退役したものについて、
**その名前を引き継いだ、退役していないソース**を代わりに数える。
THE SYSTEM SHALL 引き継ぎ先が無いとき、退役した名前をそのまま 5 本のうちの 1 本として残す。

導出元: NFR-13, FR-61。深掘り Q2, Q3, Q4, 第 4 回 Q7, Q8, Q9, 第 5 回 Q18,
第 6 回 Q23, 第 7 回 Q26, Q27, 第 8 回 Q31。

**判定は割合**（第 5 回 Q18）。絶対値の 350 日だと、**分母から日を除くほど達成が遠のく**
——分母から 1 日除くと分子も同時に 1 日減るのに、350 は動かないため。
導入から 1 年未満では分母が 365 に満たず、350 は原理的に到達できなかった。
365 日フル稼働のソースの線は 347 日（365 × 0.95）にあたる。

「記録が 1 件以上ある日」を 5 ソースに一律で当てると、利用駆動の 3 ソースが
**正常動作時から未達で固定される** —— 写真（FR-3）もウィンドウ（FR-12）もブラウザ履歴（FR-13）も
利用が主語で、PC を開かない日は記録が 0 件になる。95 % は年 18 日しか許容しない。

取得可否を要求するのは、信号が「取れない状態」を報告していても達成に数えると
**権限が剥がれたまま 1 年放置して 365/365 の達成**になるため。
成功条件 1 は「収集の仕組みが動いていた日数」ではなく「**壊れていなかった日数**」を測る。

**判定式は 1 年の計測が始まったら変えられない**（過去の実測値と比較できなくなる）。

**退役した名前を分母から外し、後継が窓を引き継ぐのは本人の決定**（第 8 回 Q31）。
退役した後の日を分母に残すと、**名前を分けただけで毎日 1 日ぶんの未達が積まれる**。
後継を数えないと、退役した瞬間に 5 本のうち 1 本が永久に未達で固まる。
**引き継ぎ先が無いときに黙って 4 本にしない** —— 合否が 4 本の合意になって成功条件 1 が緩む。

#### Scenario: 退役した日より後は分母に入らない

- **WHEN** 窓の途中で退役したソースの達成を引く
- **THEN** 退役した日より後の日は分母にも達成日にも数えられていない

#### Scenario: 退役した名前の代わりに後継を数える

- **WHEN** Must の 5 ソースのうち 1 本が退役し、その名前を引き継ぐソースがある
- **THEN** 達成は引き継いだソースについて数えられている
- **AND** 退役した名前は落ちたソースとしても未開始のソースとしても出ない

#### Scenario: 端末が主語のソースは記録の有無で数える

- **WHEN** 位置について、記録が 1 件以上ある日を数える
- **THEN** その日数が位置の達成日数になる

#### Scenario: 利用が主語のソースは取得できる状態の信号で数える

- **WHEN** 写真について、記録が 0 件で取得できる状態の生存信号がある日を数える
- **THEN** その日は写真の達成日に数えられている

#### Scenario: 取れない状態しか無い日は達成にならない

- **WHEN** 写真について、取得できない状態を報告する生存信号だけがある日を数える
- **THEN** その日は達成日に数えられていない

#### Scenario: 1 日を丸ごと覆う停止だけが分母から抜ける

- **WHEN** 1 日を丸ごと覆う停止の日と、半日だけの停止の日がそれぞれある
- **THEN** 前者は分母から除かれ、後者は分母に残る

#### Scenario: 分母から抜けた日は達成日にも数えられない

- **WHEN** 1 日を丸ごと覆う停止がある日に、そのソースの記録も 1 件以上ある
- **THEN** その日は分母にも達成日にも数えられない
- **AND** 達成日数が分母を超えることはない

#### Scenario: 1 ソースでも分母の 95 % に届かなければ未達

- **WHEN** 5 ソースの分母がすべて 365 日で、達成日数が 360, 355, 352, 351, 340 である
- **THEN** 合否は未達である（線は 346.75 日なので 340 が届かない）
- **AND** 340 のソースが落ちたことが分かる

#### Scenario: 分母が短いソースは線も下がる

- **WHEN** あるソースの分母が 200 日で、達成日数が 195 日である
- **THEN** そのソースは達成である（線は 190 日）

#### Scenario: 窓はソースごとに別の日に始まる

- **WHEN** 位置の収集開始日が `2026-04-01`、ブラウザ履歴の収集開始日が `2026-07-01` である
- **AND** `2026-06-30` に両方の達成を引く
- **THEN** 位置の分母は 90 日、ブラウザ履歴の分母は 0 日である
- **AND** ブラウザ履歴はまだ計測が始まっていないものとして返る

#### Scenario: 365 日が経つ前でも途中経過が出る

- **WHEN** 収集開始日から 200 日しか経っていないソースの達成を引く
- **THEN** 分母 200 日に対する割合で途中経過が返る
- **AND** その合否は暫定として返る

#### Scenario: 窓が閉じきるまで合否は暫定のまま

- **WHEN** 5 ソースのうち 4 本の窓が閉じ、1 本がまだ 300 日目である
- **THEN** 合否は暫定として返る
- **AND** 確定までの残り日数（65 日）が返る

#### Scenario: 始まっていないソースがあると確定日が定まらない

- **WHEN** 5 ソースのうち 1 本がまだ収集を開始していない
- **THEN** 合否は暫定として返る
- **AND** 確定する日も残り日数も返らない

#### Scenario: 全部が始まっていれば確定日が返る

- **WHEN** 5 ソースすべてが収集を開始していて、いちばん遅い窓が `2027-06-30` に閉じる
- **THEN** 確定する日として `2027-06-30` が返る

#### Scenario: 全部の窓が閉じた日に確定する

- **WHEN** 5 ソースすべての窓が閉じている
- **THEN** 合否は確定として返る

#### Scenario: 達成日数と分母の両方が返る

- **WHEN** 達成の判定を引く
- **THEN** 5 ソースそれぞれについて、達成日数と分母の両方が返る

### Requirement: 稼働状況は 1 年を週に畳んだ格子で見える

WHEN 利用者が稼働状況の画面を開く THE SYSTEM SHALL
ソース × 日 の状態を、**1 年を週に畳んだ格子**として表示する。
THE SYSTEM SHALL **ソースごとに格子を分け、各格子にソース名の文字を添える**。
THE SYSTEM SHALL 1 つの格子の中で、**7 日ぶんを 1 つのまとまり（週）として選べる**ようにする。
THE SYSTEM SHALL 格子のセルが担う区別を **3 段**とする ——
「記録あり」「動いていた・記録なし」「それ以外」。
THE SYSTEM SHALL この 3 段を、**色を使わずに（グレースケールで）区別できる**ように表示する。
THE SYSTEM SHALL 隣り合う 2 段の相対輝度比を **3:1 以上**とする（NFR-23）。
WHEN 利用者が**週**を選ぶ THE SYSTEM SHALL その 7 日ぶんの状態を、
**8 状態それぞれの名前で文字で**表示する。
THE SYSTEM SHALL 格子のセルそのものを操作対象にしない（表示専用とする）。
THE SYSTEM SHALL 幅 360 CSS px でも、操作できる対象が 24 × 24 CSS px 以上であるようにする。
THE SYSTEM SHALL 格子を**縦長**に置く —— 1 行が 1 週（7 日）で、**上から下へ 53 週**。
THE SYSTEM SHALL **新しい週を上**に置く。
THE SYSTEM SHALL 画面を開いた直後、**各ソースについて直近 4 週以上を表示**し、
1 年ぶんはそれを伸ばして表示する。
THE SYSTEM SHALL 開いた直後に、**2 ソース以上の直近 4 週**が同時に見えるようにする。
THE SYSTEM SHALL Must の 5 ソースすべてが、**ひとスクロール以内**に見えるようにする。
THE SYSTEM SHALL 退役したソースの格子を、**Must の 5 ソースより後ろに置き、既定で畳む**。
THE SYSTEM SHALL 幅 360 CSS px で、格子が横スクロールなしで収まるようにする。
THE SYSTEM SHALL 週の帯が 24 × 24 CSS px 以上であるようにする。
THE SYSTEM SHALL 5 ソースそれぞれの達成日数と分母、および 5 本すべてが分母の 95 % 以上かどうかを数値で表示する。
THE SYSTEM SHALL その合否が**確定か暫定か**を表示する。
THE SYSTEM SHALL 暫定のとき、**確定までの残り日数**を表示する（確定する日が定まらないときは、
そのソースがまだ収集を開始していないことを表示する）。

導出元: FR-54, FR-61, NFR-13, NFR-19, NFR-23。
深掘り Q6, 第 4 回 Q15, Q16, 第 5 回 Q20, Q21, 第 6 回 Q25, 第 7 回 Q28, 第 8 回 Q30。

**「スクロールせずに 5 本」は現物の高さで成り立たなかった**（第 8 回 Q30）。
第 7 回 Q28 の根拠にした 600 px は `5 ソース × 5 行 × 24 px` で**セルだけを積んだ勘定**で、
見出し・余白・「1 年ぶんを見る」のボタン・達成の表を数えていない。
実際に宣言されている箱を積むと **約 1,491 px**（実測）。
本人の答えは「**同時に見える」を 1 スクロール以内に緩める**」で、
第 7 回 Q28（開いた直後は直近 4〜5 週だけ）と NFR-19（週の帯 24 px）は動かさない。

**ひとスクロールは 2 画面ぶん**とする —— 基準の画面は **360 × 640 CSS px**
（NFR-19 が幅に使っている小さい端末に、その端末の高さを合わせたもの）。
開いた直後に 1 画面ぶんが見え、1 回スクロールすると残りが見える。

**退役したソースを後ろに置いて畳むのは ST03 の R63** —— ST03 の運用では退役は 1 本きりではなく
増えるので、退役した格子が上に並ぶと **Must の 5 本が 1 画面から押し出される**。

1 年を見せるのは、完了の判定が「1 か月放置した後に開くと、欠けた日が一目で分かる」であるため。
直近 14 日では 1 か月の放置を見せられない。

**色を意味の担い手にしない。** ソースの区別は各格子に添えたソース名の文字が担う
（`ui-direction.md` の宿題 1 —— 6 色の輝度差が最小 1.009:1 で、グレースケールと 2 型色覚では
区別できない）。

**格子が 3 段までなのは、明度だけでは 7 段を分けられないため**（第 5 回 Q21）。
WCAG SC 1.4.11 は隣接する色に 3:1 を求めるが、7 段を隣接 3:1 で並べると 3^6 = **729:1** が要る。
**sRGB の理論最大は 21:1** で、21:1 を 6 等分しても隣接 1.661:1 にしかならない。
3 段なら 3:1 × 3:1 = 9:1 で収まる。8 状態の区別は**週を選んだときの文字**が担う。

**縦長にするのは NFR-19 のため**（第 6 回 Q25）。横長（週 = 列）だと 1 ソースが 53 列になり、
360 px 幅では週の帯が 6 px で 24 px を割る。縦長なら週の帯は幅いっぱいを取れる。
**新しい週を上**にするのは、開いた直後に出る直近 4〜5 週が新しい側になるようにするため。
完了の判定「1 か月放置した後に開くと欠けた日が一目で分かる」を満たすのは
**初期表示を直近 4〜5 週に絞ること**（第 7 回 Q28）で、
**週の向きだけでは 2 本目以降のソースに届かない**（1 本目の下に 53 行ぶんの距離がある）。

セルを表示専用にするのは、**1 週 7 日を横に並べても 1 セルが 24 px に満たない場合がある**ため。
選ぶ単位を週にすれば、セルの大きさに関わらず NFR-19 を満たせる。

#### Scenario: 格子は 1 行 1 週で、直近の週が一番上に来る

- **WHEN** 稼働状況の画面を開く
- **THEN** 1 行が 1 週（7 日）で、新しい週から古い週へ上から下に並んでいる
- **AND** 直近の週が一番上にある

#### Scenario: ソースごとに格子が分かれる

- **WHEN** 稼働状況の画面を開く
- **THEN** ソースごとに格子が分かれ、それぞれにソース名の文字が添えられている

#### Scenario: 1 年ぶんが一目で読める

- **WHEN** 1 年ぶんのデータがある状態で、あるソースの表示を 1 年ぶんまで伸ばす
- **THEN** 1 年ぶんの日が週に畳まれた格子として読める

#### Scenario: グレースケールでも格子の 3 段が区別できる

- **WHEN** 3 段それぞれのセルの相対輝度を取る
- **THEN** 隣り合う 2 段の比がいずれも 3:1 以上である

#### Scenario: 週を選ぶと 7 日ぶんが 8 状態の名前で出る

- **WHEN** ある週を選ぶ
- **THEN** その 7 日ぶんの状態が、8 状態それぞれの名前で文字で表示される
- **AND** 格子では「それ以外」に畳まれていた日も、どの状態だったかが分かる

#### Scenario: 360 px 幅でも操作対象が 24 px を割らない

- **WHEN** 幅 360 CSS px で画面を開く
- **THEN** 操作できる対象はいずれも 24 × 24 CSS px 以上である
- **AND** 格子のセルは操作対象になっていない
- **AND** 格子が横スクロールなしで収まっている
- **AND** 週の帯の高さが 24 CSS px 以上である

#### Scenario: 5 本の達成日数と合否が出る

- **WHEN** 稼働状況の画面を開く
- **THEN** 5 ソースそれぞれの達成日数と分母が数値で表示される
- **AND** 5 本すべてが分母の 95 % 以上かどうかが表示される
- **AND** それが確定か暫定かが表示される

#### Scenario: 暫定のときは確定までの残り日数が出る

- **WHEN** 5 ソースすべてが収集を開始していて、合否がまだ暫定である
- **THEN** 確定までの残り日数が表示される

#### Scenario: 始まっていないソースがあると残り日数の代わりに理由が出る

- **WHEN** 5 ソースのうち 1 本がまだ収集を開始していない
- **THEN** 残り日数は表示されず、そのソースがまだ収集を開始していないことが表示される

#### Scenario: 開いた直後に 2 ソース以上の直近 1 か月が同時に見える

- **WHEN** 360 × 640 CSS px で稼働状況の画面を開く
- **THEN** 2 ソース以上について直近 4 週以上が、スクロールせずに同時に見えている

#### Scenario: ひとスクロールで 5 ソースすべてが見える

- **WHEN** 360 × 640 CSS px で稼働状況の画面を開く
- **THEN** Must の 5 ソースすべてが、2 画面ぶん（1,280 CSS px）以内に収まっている

#### Scenario: 退役したソースは後ろで畳まれている

- **WHEN** 退役したソースを含む稼働状況の画面を開く
- **THEN** 退役したソースの格子は Must の 5 ソースより後ろにある
- **AND** その格子は既定で週の行を出していない

#### Scenario: 1 年ぶんは伸ばして見る

- **WHEN** あるソースの表示を伸ばす
- **THEN** そのソースの 1 年ぶん（53 週）が読める

### Requirement: 稼働記録と登録簿は利用者識別子を持つ

THE SYSTEM SHALL 稼働記録・ソース登録簿・生存信号・停止・破棄のいずれの表にも、
利用者識別子の列を持たせる。

導出元: FR-29。扉 #9。

FR-29 は「**すべてのテーブル**に」と定めており、扉 #9 は「単一利用者でも day one から持つ」で
決着している。現在は `core.event` にしか列が無い。ST02 が `core.coverage` を作り直すので、
**列を足す最も安い機会がここ**にあたる。

#### Scenario: すべての稼働記録系の表に利用者識別子がある

- **WHEN** 稼働記録・ソース登録簿・生存信号・停止・破棄の各表の列を調べる
- **THEN** いずれにも利用者識別子の列がある
