# ST02 深掘りの独立レビュー

対象: `openspec/changes/st02-collection-coverage/deep.md` の「## 問うた項目」（Q1〜Q6）と
「## 確かめたが問わなかったこと」を、立てられた問いの一覧として扱った。
`openspec/schemas/ashiato/schema.yaml` の `deep` 手順 1〜5 を、一覧を見る前に独立にやり直し、
そのうえで突き合わせた差分だけを書く。**問いの側は 1 文字も触っていない。**

実施日: 2026-09-10。読んだ時点の main は `ee2b55b`、作業ブランチは `docs/st02-upstream`
（`docs/requirements.md` / `docs/stories/ST02.md` / `proposal.md` は未 commit の変更あり。
2026-09-10 の FR-78 新設と NFR-13 の書き換えを含む状態で読んだ）。

## 依頼された 2 件の事実確認

| 主張 | 判定 | 裏付け |
|---|---|---|
| 欠陥 2「重複再送で `event_count` が水増し」は ST01 の下流が直した（proposal の訂正） | **事実。訂正は正しい** | `crates/server/src/lib.rs:225` が `.bind(i32::from(row.is_some()))`。重複時は 0 加算で、`INSERT ... ON CONFLICT DO UPDATE` の行そのものは立つ（`lib.rs:217-227`）。振る舞いは `openspec/changes/st01-location-ingest/specs/collection-coverage/spec.md` の「稼働記録は新しく入った記録だけを数える」に固定済み |
| 日境界が UTC 固定のまま残っている | **事実。残っている** | `crates/server/src/lib.rs:219` — `VALUES ($1, ($2 AT TIME ZONE 'UTC')::date, 'alive', $3)`。`grep -n "AT TIME ZONE" crates/server/src/lib.rs` の結果は 219 の 1 件だけ |
| PR #15 の Doze 実測（proposal:128-130） | **事実** | `gh pr view 15` の本文 —「245 分 / 164 件 = 取得率 67 %」「3 分を超える空き 9 回 / 合計 77 分」「原因は Doze」「R46 を本人へ返した」。draft のまま |

---

## 手順 1 — 要件どうしの衝突

`satisfies` = FR-33 / FR-54 / FR-78 / NFR-13、扉 #14 の関与要件 = FR-33 / FR-34 / FR-9 / FR-54。
これらに FR-3 / FR-12 / FR-13 / FR-35 / FR-29 / NFR-19 を加えて突き合わせた。
Q1（FR-33 対 扉 #14）と Q4（NFR-13 対 FR-3）は一覧にある。**残り 3 組が一覧に無い** → D1 / D2 / D3。
FR-9（破棄）と NFR-13（分母から除くのは FR-34 の停止だけ）の組も見たが、
Q3 の答え（時刻範囲で持つ）で ST04 側が判断できる形になっており、いま問う必要は無いと判断した。

## 手順 2 — 扉の幅

扉 #14 は決定済（`docs/requirements.md:539-542`）。deep.md は「残る幅は無い」と結論している
（`deep.md:161-162`）。**1 か所だけ幅が残る** → D5（状態⑥が「導入前」と「アプリが殺された」を混ぜる）。
扉 #9（`docs/requirements.md:524-526`）は ST02 の `doors` に無いが、ST02 が表を作り直す以上
必ず触れる → D9。

## 手順 3 — 新たに立つ一方通行（列の型・保存形式・鍵の入力）

ST02 が新設するのは「生存信号の表」、作り直すのは `core.coverage`。
一覧は「別表にする」「1 件の記録として残す」を**技術判断として問わなかった**と書いているが
（`deep.md:155-160`）、**その 2 つの決定に列の型と保護の有無がぶら下がっている** → D7 / D8。
ST01 の `raw` = `jsonb` と同型（`migrations/0003_raw_text.sql:1-13` が事後の始末を書いている）。

## 手順 4 — 日常に影響する選択

Q6（毎日目に入る画面）は一覧にある。**電池と通知が一覧から落ちている** → D4 / D6。
D11 は Q6 の答えそのものが要件と衝突する件。
容量は確かめた: 生存信号 5 ソース × 4 回/日 × 365 ≒ 7,300 行で NFR-5（年 3〜6 GB）に対し無視できる。
deep.md の計算どおりで、問う大きさではない（該当なし）。

## 手順 5 — 既存コードが要件を満たしていない箇所

`grep` と実ファイルで 1 件ずつ当てた。

| 要件 | 該当コード | 判定 |
|---|---|---|
| FR-33 | `crates/server/src/lib.rs:217-227` | 書いている。ただし日境界が UTC（deep.md の欠陥 1、事実） |
| FR-54 | `web/src/App.tsx:19` が `fetch("/api/events")` のみ。`crates/server/src/lib.rs:393-402` に `/healthz` `/ingest` `/events` しか route が無い | **稼働状況の読み出し口も画面も存在しない。** ST02 の本体なので新規の指摘にはしない |
| FR-78 | `collector-android` 側で権限状態を読むのは `MainActivity.kt:61` の `checkSelfPermission` だけ。`LocationService` / `FixCollector` には無い | 未実装。ST02 の本体 |
| NFR-13 | 集計コードは存在しない（`grep -rn "coverage" crates/ tools/` は seed と smoke の `core.source` INSERT のみ） | 未実装。ST02 の本体 |
| FR-29 / 扉 #9 | `migrations/0001_envelope.sql:6-11`（`core.source`）と `:47-54`（`core.coverage`）に `user_id` が無い。`grep -n user_id migrations/*.sql` は `core.event` の 1 件だけ | **「動いているが要件を満たしていない」。deep.md の欠陥 3 件に入っていない** → D9 |

`core.event` の INSERT と `core.coverage` の INSERT は別クエリで、`grep -n "begin()\|transaction" crates/server/src/lib.rs` は 0 件 ——
トランザクションで括られていない。ただし `alive` は `core.event` から再計算できるので指摘にはしない
（生存信号は再計算できないので、その点は D8 に含めた）。

---

## R1. NFR-13 の「写真: 生存信号があった日」は、生存信号が「取れない状態」を報告していても達成日に数える

- 成果物: `openspec/changes/st02-collection-coverage/deep.md`（Q4 / Q5）
- 種別: **抜け**
- 根拠:
  - `docs/requirements.md:345` — 「**写真**: 生存信号（FR-78）があった日」。取得可否の条件が付いていない
  - `docs/requirements.md:169-179`（FR-78）— 取得可否を載せる理由は「**壊れているのに『動いていた』と残る**」を防ぐこと
  - `deep.md:67-68`（Q4 の答え）と `deep.md:82`（Q5 の答え）は別々の問いとして決まっており、突き合わせが行われていない
  - `deep.md:116`（状態③「動いていたが取れない状態だった」）は扉 #14 の「収集が壊れていた」に当てられている。その③の日が NFR-13 で達成に入る
  - 権限が剥がれたまま 1 年放置すると、写真は 365/365 の達成になる
- kind: conflict / irreversible（判定式は計測開始後に変えられない ——`docs/requirements.md:356` が自らそう書いている）
- 処置: escalated — 第 4 回 Q7。生存信号で数える達成日を「取得できる状態の信号」に限るか
- 提案: 「写真の達成日は『生存信号があった日』か『**取得できる状態の**生存信号があった日』か」を問いとして立てる。
  同じ問いは毎日届く 4 つにも裏返しで効く（③の日は記録 0 件なので自動的に未達になる = ソース間で扱いが逆になる）。

## R2. ウィンドウ（FR-12）とブラウザ履歴（FR-13）も利用駆動で、写真と同じ「正常動作時から未達」を踏む

- 成果物: `openspec/changes/st02-collection-coverage/deep.md`（Q4）
- 種別: **抜け**
- 根拠:
  - `docs/requirements.md:105-106`（FR-12）— 「WHEN アプリ名・ウィンドウ題名・URL のいずれかが**変化する**」。PC を触らなかった日は記録 0 件
  - `docs/requirements.md:107-108`（FR-13）— 「前回取得以降の履歴を取得して記録を生成する」。C-02 が起動しない日、履歴が無い日は記録 0 件
  - `docs/requirements.md:344` — この 2 つが「毎日届くはずの 4 つ」に入っている
  - 350/365 は **年 15 日**しか許容しない。旅行・帰省・PC を開かない週末が 15 日を超えると、収集が正常でも未達で固定される
  - Q4 が根拠に挙げた旧プロジェクトの実測（`reference/requirements.md:765`）は、**数え方が「連続 3 日を超えない」で NFR-13 の「350/365」とは別型**。「アクティブウィンドウを毎日届く側に置いた」という前例はそのままは効かない
- kind: conflict / irreversible
- 処置: escalated — 第 4 回 Q8。ウィンドウとブラウザ履歴を「毎日届く」側に置いたままでよいか
- 提案: Q4 の選択肢を「毎日届く 4 つ / 写真」の二分にせず、**5 ソースを 1 件ずつ「端末が主語か / 利用が主語か」で仕分ける**問いに開く。
  少なくともウィンドウとブラウザ履歴について、生存信号側で数えるかを問う。

## R3. NFR-13 の分子が「ソースごとに独立に数える」のか「全 Must ソースが揃った日」なのか、どこにも書かれていない

- 成果物: `openspec/changes/st02-collection-coverage/deep.md`（Q4）
- 種別: **抜け**
- 根拠:
  - `docs/requirements.md:341-347` — 「Must のソース（…5 つ…）について、**ソースごとに達成日の数え方を分けて**日数を数え、**365 日中 350 日以上**」。数え方は分けると書いてあるが、**5 本の数を最後にどう畳むか**が書かれていない
  - 5 本すべてが 350 を超える必要があるのか、AND を取った日を数えるのか、平均か、で成功条件 1 の合否が変わる
  - `deep.md:58-74`（Q4）は「ソースごとに判定を分ける」までしか決めておらず、畳み方に触れていない
  - `docs/stories/ST02.md` の完了の判定は「365 日中の達成日数が数値で出る」と**単数**で書いており、画面に何本の数を出すかも決まっていない
- kind: irreversible
  （NFR-13 の判定式そのもの。計測開始後は変えられない）
- 処置: escalated — 第 4 回 Q9。5 ソースの達成日をどう 1 つの合否に畳むか
- 提案: 「5 ソースの達成日を最後にどう 1 つの合否にするか」を問いとして足す。選択肢は少なくとも
  (a) 5 本すべてが 350 以上 (b) 全ソースに記録があった日を数えて 350 以上 (c) ソースごとに出して合否は本人が読む。

## R4. 生存信号は Doze で潰れる。電池を「問わない」と決めた根拠が実測で否定されている

- 成果物: `openspec/changes/st02-collection-coverage/deep.md`（「確かめたが問わなかったこと」の 1 件目）
- 種別: **抜け** + **分類違い**（daily を技術判断に落としている）
- 根拠:
  - `deep.md:153-154` — 「電池への影響も問わなかった —— 位置は FR-1 が 60 秒間隔で取っており、6 時間に 1 回の生存信号はその 360 分の 1 未満」。**この計算は 60 秒間隔が守れている前提**
  - `gh pr view 15` の本文 — 実機 4 時間で「245 分 / 164 件 = **取得率 67 %**」「3 分を超える空きが **9 回 / 合計 77 分**（4.6〜12.8 分）」「**原因は Doze**」。`deep.md` の実施日（2026-09-09）より後の 2026-09-10 の実測
  - 同 PR は「Doze の除外を要求するか」（R46）を**本人へ返したまま draft で止めている**。ST02 の生存信号がどれだけ確実に届くかは、この未決の答えで決まる
  - `openspec/changes/st01-location-ingest/tasks.md:118` にも別日の実測（465 秒の空き 1 回）が残っている
  - 6 時間間隔の生存信号は Doze の maintenance window にしか乗らないので、静止した休日は丸 1 日欠けうる。そのとき状態は②ではなく⑥「不明」になり、**NFR-13 の写真判定（R1）も同時に落ちる**
- kind: daily / irreversible（載せなかった期間は後から埋められない）
- 処置: escalated — 第 4 回 Q10。生存信号を Doze の外に置くか。ST01 の R46 と連動する
- 提案: 「生存信号を Doze の外に置くか（除外要求 / 前景サービスに相乗り / `AlarmManager` の setExactAndAllowWhileIdle）」を問いとして足す。
  ST01 の R46 と**同じ答えで決まる**なら、ST02 の問いとしては「R46 の答えを待つ」を明記する。

## R5. 状態⑥「不明」が「導入前」「電源断」「アプリが OS に殺された/アーカイブされた」を混ぜる。扉 #14 の幅は残っている

- 成果物: `openspec/changes/st02-collection-coverage/deep.md`（6 状態の表 / 「確かめたが問わなかったこと」の扉 #14 の項）
- 種別: **抜け**（deep.md の「残る幅は無い」が誤り）
- 根拠:
  - `deep.md:119` — 状態⑥は「不明（信号も記録も無い）」で「（どれでもない。導入前 or 完全停止）」と扉 #14 の区別の**外**に置かれている
  - `deep.md:161-162` — 「扉が挙げた 4 つの区別に過不足なく対応がついた。**残る幅は無い**」
  - `docs/requirements.md:539-541`（扉 #14）— 区別すべきものに「**収集が壊れていた**」が含まれる。**アプリが殺された = 収集が壊れていた**の典型で、生存信号では原理的に埋まらない（止まったものは報告しない）
  - `gh pr view 15` の本文 —「**『使用していないアプリを管理する』が既定でオン**だった。権限を削除してアプリを**アーカイブ**する設定で、めったに開かないこの収集アプリには致命的（数か月後に無言で収集が止まる）」。Q5 が想定したのは「プロセスは生きたまま権限が剥がれる」（`deep.md:78-80`）だけで、**プロセスごと消える経路**は 6 状態のどこにも入っていない
  - サーバ側から見ると⑥は「導入前」と区別できない。導入日を持たないと、格子の左端が全部⑥で埋まる
- kind: irreversible
  （区別を作るには day one から列が要る）
- 処置: escalated — 第 4 回 Q11。状態⑥を割るか —— 収集開始日と、殺されたことの能動的な検知
- 提案: 「⑥を割るか」を問いとして立てる。少なくとも**ソースごとの収集開始日**を持てば「導入前」は⑥から抜ける。
  「アプリが殺された」を⑥から抜くには、サーバ側で expected_gap_sec の超過を検知して**⑥に理由を書く**（能動的な検知）が要る。

## R6. FR-78 が参照する「FR-35 の想定間隔」に、写真とウィンドウの値が無い。写真に値を入れると FR-35 が鳴り続ける

- 成果物: `openspec/changes/st02-collection-coverage/deep.md`（「確かめたが問わなかったこと」の「生存信号の間隔」）
- 種別: **抜け** + **分類違い**（daily を技術判断に落としている）
- 根拠:
  - `docs/requirements.md:169-171`（FR-78）— 「そのソースに登録された**想定間隔（FR-35）ごとに**」
  - `docs/requirements.md:165-168`（FR-35）— 想定間隔の初期値は「位置 = 6 時間 / アプリ利用 = 6 時間 / ブラウザ履歴 = 24 時間 / Takeout 系 = 60 日」。**写真とウィンドウが無い**。NFR-13 の写真判定（`docs/requirements.md:345`）は写真の生存信号に依存するのに、その間隔が要件に無い
  - `migrations/0001_envelope.sql:9` — `expected_gap_sec integer NOT NULL`。NOT NULL なので写真ソースを登録した時点で何かの値を入れざるを得ない
  - `docs/requirements.md:165-166` — FR-35 は「あるソースの最後の**記録**からの経過時間」が閾値。写真に 6 時間を入れると、**写真を撮らずに 18 時間経つたびに通知が鳴る**。生存信号を FR-35 の言う「記録」に数えるかは、どこにも書かれていない
  - `deep.md:150-152` — 「同じ値を生存信号の間隔に使う以外の選択は ST14 と辻褄が合わなくなるので、**技術判断として決めた**」。実際には辻褄が合っていない
- kind: daily / conflict
  （毎日鳴りうる通知。かつ FR-78 と FR-35 が噛み合っていない）
- 処置: escalated — 第 4 回 Q12。写真とウィンドウの想定間隔、および FR-35 の「最後の記録」に生存信号を数えるか
- 提案: (a) 写真・ウィンドウの想定間隔を要件に足すこと、(b) FR-35 の「最後の記録」に生存信号を数えるか、
  の 2 点を問いとして立てる。(b) は ST14 の通知が「記録が途切れた」を検知できるかの分かれ目になる。

## R7. 生存信号を別表に置くと、`core.event` に掛けた保護（不変トリガ・冪等・原文保存）が全部外れる

- 成果物: `openspec/changes/st02-collection-coverage/deep.md`（「確かめたが問わなかったこと」の 2・3 件目）
- 種別: **抜け** + **分類違い**（不可逆を技術判断に落としている。ST01 の `raw`=`jsonb` と同型）
- 根拠:
  - `deep.md:159-160` — 「生存信号を `core.event` に入れるか別表にするか —— 別表。`core.event` の `origin` は 3 値で、生存信号はどれでもない。**技術判断**」
  - `migrations/0002_immutable_collected.sql:22-25` — 書き換え禁止のトリガは `BEFORE UPDATE **ON core.event**` にしか付いていない。`grep -n "CREATE TRIGGER" migrations/*.sql` は 0002 の 1 件だけ。**`core.coverage` には現状も保護が無く**、`crates/server/src/lib.rs:220-221` が `DO UPDATE SET event_count = ... + $3` で普通に書き換えている
  - `migrations/0001_envelope.sql:37-40` — 冪等のユニーク索引も `core.event` にしか無い。ST01 の Outbox は部分失敗時に再送する（`openspec/changes/st01-location-ingest/specs/collection-coverage/spec.md`「重複の到着は常態」）ので、**生存信号は再送で重複行が増える**
  - `docs/requirements.md:517-519`（扉 #7「原文をそのまま残す」）— 生存信号にこれが掛かるかが決まっていない。掛からないなら、収集側が送ったバイト列は残らない
  - `migrations/0003_raw_text.sql:1-13` — ST01 が同じ型を踏んだ記録（「`jsonb` は原文を保たない」）。列の型と保存形式は製造準備で決まって誰も論点に挙げなかった、が繰り返されている
  - 生存信号は状態②③の**唯一の証拠**で、扉 #14 が「後から区別する唯一の手段」と言っている当のもの。書き換え自由・重複自由でよいかは可逆な判断ではない
- kind: irreversible / technical
- 処置: escalated — 第 4 回 Q13。生存信号に core.event と同じ保護を掛けるか
- 提案: 「生存信号にも `core.event` と同じ保護（書き換え禁止・冪等キー・原文の素通し）を掛けるか」を問いとして立てる。
  もし問わずに技術判断で通すなら、**掛ける側**に倒した理由を deep.md に書く（外す判断は不可逆）。

## R8. 生存信号と停止・破棄を `date` に丸めると、Q2 の日境界の決定が行に焼き込まれて後から動かせない

- 成果物: `openspec/changes/st02-collection-coverage/deep.md`（Q2 / Q3）
- 種別: **抜け**
- 根拠:
  - `migrations/0001_envelope.sql:47-54` — `core.coverage` の主キーは `(logical_source, day, state)` で `day` は `date`。日境界の計算は書き込み時（`crates/server/src/lib.rs:219`）に済んでしまう
  - `docs/briefs/ST02-deep-r1.html` の Q2 の 3 択は、**どの選択肢の「戻せないもの」にも**「『記録あり』の行は記録から再計算できるが、**停止・破棄の行は再計算できない**」と書いてある。つまり問いの側が「再計算できない行がある」と認識していた
  - Q3（`deep.md:50`）はその再計算できない行のうち**停止と破棄だけ**を時刻範囲に上げた。**生存信号は Q3 より後（Q5）に決まったので、この網から漏れている**
  - 生存信号を `(source, day)` で持つと、Asia/Tokyo という決定がそのまま行になる。`timestamptz` の受信時刻を残せば日境界は後から引き直せる
- kind: irreversible
- 処置: escalated — 第 4 回 Q14。生存信号を時刻で持つか、日に丸めるか
- 提案: Q3 の問いを生存信号にも広げる（「生存信号を時刻で持つか、日に丸めるか」）。
  Q2 の答えを覆すためではなく、**Q2 を後から引き直せる余地を残すか**を問う。

## R9. `core.coverage` と `core.source` に `user_id` が無い。FR-29 / 扉 #9 に反している

- 成果物: `openspec/changes/st02-collection-coverage/deep.md`（「上流で見つかった既存実装の欠陥」）
- 種別: **抜け**（手順 5 の欠陥一覧に無い。人間に問う話ではないので tasks で直す型）
- 根拠:
  - `docs/requirements.md:141`（FR-29）— 「THE SYSTEM SHALL **すべてのテーブル**に利用者識別子の列を持たせる」
  - `docs/requirements.md:524-526`（扉 #9）— 「利用者識別子を全テーブルに持つか / 決定済（**単一利用者でも day one から持つ**）/ 関与要件 FR-29, PERM-1」。決めるものは「記録の有無」＝不可逆
  - `grep -n user_id migrations/*.sql` → `migrations/0001_envelope.sql:15` の 1 件のみ（`core.event`）。`core.source`（`:6-11`）と `core.coverage`（`:47-54`）には無い
  - ST02 は `core.coverage` を BREAKING で作り直し、生存信号の表を新設する（`proposal.md:138`）。**列を足す最後の安い機会**がここ
- kind: technical
  （扉 #9 が既に決めているので人間に問う幅は無い）
- 処置: fixed deep.md — 「上流で見つかった既存実装の欠陥」を 4 件目として追記。扉 #9 が決定済で人間に問う幅が無く、specs 再開時に tasks へ落とす
- 提案: 問いは足さない。deep.md の「既存実装の欠陥」を 3 件から 4 件にして、
  作り直す `core.coverage` と新設する生存信号の表に `user_id` を持たせることを tasks に落とす。

## R10. Q6 が「読み替えた」ui-direction の宿題 1 は、6 状態ではなく **6 ソースの色**を指している

- 成果物: `openspec/changes/st02-collection-coverage/deep.md`（Q6）
- 種別: **前提の誤り**
- 根拠:
  - `docs/ui-direction.md:161-166`（宿題 1）の逐語 —「**種類の区別が色だけになる**。しかも 6 色は等明度・等彩度（色相 52°/84°/116°/148°/180°/212°）で、輝度差は最小 1.009:1 …確定した 3 画面のうち**稼働状況が既にソースを色で分けている**。**行にはソース名の文字があるので情報は失われない**が、色を意味の担い手にする箇所には形かラベルを併せること」
  - つまり 1.009:1 の 6 色は**ソースを分ける色**であって、稼働の 6 状態の色ではない（6 状態という概念は Q1〜Q5 で初めて生まれたもので、ui-direction を書いた時点で存在しない）
  - `deep.md:103-106` — 「そこで挙がっている実際の問題は『輝度差が最小 1.009:1 でグレースケールと 2 型色覚では区別できない』ことで、**明度の段差はその問題を解く**」。**状態に明度を割り当てても、ソースの色の 1.009:1 は解けない**
  - 宿題自身が示していた解（「行にはソース名の文字がある」）は、Q6 が採った「1 年を週に畳んだ格子」ではソース名の行が無くなる可能性がある。`docs/stories/ST02.md` の完了の判定にもソースの区別のしかたは書かれていない
- kind: premise
- 処置: escalated — 第 4 回 Q15。格子でソースをどう分けるか。あわせて deep.md の Q6 の「ui-direction の逐語を読み替えた」という記述を、事実（宿題 1 の 1.009:1 はソースを分ける 6 色であって稼働の 6 状態ではない）に直した
- 提案: 「読み替えた」という記述を「宿題 1 のうち**ソースの色**の部分は未処置」に直し、
  格子でソースをどう分けるか（面を分ける / ラベルを置く / 色を使う）を問いに足すか、扱わないことを明記する。

## R11. Q6 の「1 年を週に畳んだ格子」は NFR-19（触れる対象 24×24 CSS px）と衝突する

- 成果物: `openspec/changes/st02-collection-coverage/deep.md`（Q6）
- 種別: **抜け**（選択肢に不可逆でない制約が書かれていない）+ **不可逆の記述が無い**
- 根拠:
  - `docs/requirements.md:405`（NFR-19）— 「指で触れる対象は **24 × 24 CSS px 以上**とする」
  - `deep.md:98` / `docs/stories/ST02.md` 完了の判定 — 「**セルを選ぶと**状態名が文字で出る」＝**セルは触れる対象**
  - 1 年を週に畳むと横 53 週。53 × 24px = **1272px**。`docs/ui-direction.md:173-174` が文脈に置く幅は「**360〜1440px**」で、**360px 幅では 1 セルが約 6.8px** になり NFR-19 を満たさない
  - `docs/ui-direction.md:17` — S-1 稼働状況は確定した 3 画面の 1 つで、スマホ幅も文脈に入っている
  - `docs/requirements.md:417`（NFR-21）— 「原色・派手な配色・**角張った四角を用いない**」。格子のセルは正面から当たる
  - Q6 の選択肢（`docs/briefs/ST02-deep-r*.html` の該当問い）にも `deep.md:96-106` にも、この制約は書かれていない
- kind: daily / conflict
- 処置: escalated — 第 4 回 Q16。スマホ幅で 1 年をどう見せるか。NFR-19 の 24×24 px
- 提案: Q6 に「スマホ幅で 1 年をどう見せるか」を足す（縦に畳む / 月ごとにページを割る / タップは行単位で受けてセルは表示専用にする）。
  NFR-19 と NFR-21 は決定済の要件なので、答えが要件を割るなら**要件の側を直す**手続きに載せる。

## R12. deep.md / proposal の行番号引用が現物とずれている（指摘の中身は 2 件とも事実）

- 成果物: `openspec/changes/st02-collection-coverage/deep.md`、`proposal.md`
- 種別: **前提の誤り**（軽微。結論は変わらない）
- 根拠:
  - `deep.md:20` `deep.md:34` `deep.md:138` は `crates/server/src/lib.rs:120` / `:121` を指すが、
    `grep -n "AT TIME ZONE" crates/server/src/lib.rs` → **219** の 1 件のみ
  - `proposal.md:151` は `:193`、`proposal.md:154` は `:199` を指すが、
    `grep -n "is_some()" crates/server/src/lib.rs` → **225**
  - `deep.md:20-21` の「既存実装は取り込み時にしか書いておらず」と `deep.md:138` の「日境界が UTC 固定」は**どちらも現物で確認できる事実**。ずれているのは行番号だけ
  - `deep.md:33` は `st01-location-ingest/design.md:176` を「ST02 の担当として申し送った」箇所として引くが、
    `grep -n ST02 openspec/changes/st01-location-ingest/design.md` → 27 / 171 / **399**。申し送りの逐語は **399 行目**（「→ **ST02（収集が動いていたかが日単位で見える）の担当**。そこで決める」）
  - `deep.md:87` の EXT-D は `docs/requirements.md:450` を指すが、現物は **478 行目**
  - ST01 の merge（`043c9dd` 以降）と 2026-09-10 の要件改訂で行が伸びたのが原因
- kind: premise
- 処置: fixed deep.md — deep.md と proposal.md の行番号を現物に合わせ、動きやすい箇所は識別子での引用に替えた
- 提案: 行番号を 219 / 225 に直すか、行番号ではなく識別子（`AT TIME ZONE 'UTC'` / `i32::from(row.is_some())`）で引く。
  下流が古い行を開いて別のコードを直す事故を防ぐ。

---

## 「不要」に当たるもの — 無し

Q1〜Q6 を 1 件ずつ「要件か扉で既に決まっていて幅が無いか」で当て直したが、**削るべき問いは見つからなかった**。

| 問い | 幅が残っていた根拠 |
|---|---|
| Q1 | 扉 #14 は「稼働記録を持つ」までしか決めていない（`docs/requirements.md:539-542`）。生存信号は端末の電池と通信を毎日使うので、要件だけからは決まらない |
| Q2 | ST01 が明示的に ST02 へ送っている（`openspec/changes/st01-location-ingest/design.md:399`）。UTC / 固定 TZ / 記録ごとの `tz_id` の 3 択はどれも要件と矛盾しない |
| Q3 | FR-34 / FR-9 は「期間」としか書いておらず、日単位も時刻範囲も逐語を満たす |
| Q4 | 逐語のままだと写真が未達で固定される（R2 のとおり、むしろ問いが狭すぎた） |
| Q5 | EXT-D（`docs/requirements.md:478`）は「読めるかは実行時点の権限状態で決まる」までで、稼働記録に載せるかまでは決めていない |
| Q6 | `docs/ui-direction.md:166` が「ST02 / ST25 の深掘りで決める」と名指しで送っている |

## 分類（kind）について

`deep.md` は問いごとに `kind` を明示していないが、「なぜ人間の判断か」の見出しがその役を果たしている。
Q1〜Q6 の 6 件について、書かれた分類と実態のずれは見つからなかった。
**ずれているのは「確かめたが問わなかったこと」に落とした 3 件**で、それぞれ R4（daily を技術判断へ）、
R6（daily / conflict を技術判断へ）、R7（irreversible を技術判断へ）に書いた。
