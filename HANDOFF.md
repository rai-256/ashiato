# 引き継ぎ: ashiato2 で要件定義を起こす

`~/dev/ashiato2/` で作業する。**旧 `~/dev/ashiato/` は別物なので触らない**（旧ハーネス付き・別の実験）。

## これは何か

自作ハーネス（claude-harness）が肥大して検証しきれなくなったため、**既存ツールのつぎはぎで作り直した
新ハーネス「harness2」の試走**。題材は ashiato（行動履歴を記録・可視化するライフログ）。
旧 ashiato と同じ題材を新フローで作り直し、結果を比較する。

## 環境

| パス | 何か | 書き込み |
|---|---|---|
| `~/dev/ashiato2/` | 作業場所。GitHub `rai-256/ashiato2` (private) / branch `main` / 初期コミット `afad85b` | する |
| `reference/` | 旧 ashiato からの参考資料 | **しない（読むだけ）** |
| `docs/` | フローの出力先。**いまは空** | する |
| `.claude/skills` → `~/dev/harness2/skills` | skill 5 本（symlink）| harness2 側で直す |
| `scripts` → `~/dev/harness2/scripts` | 検査スクリプト（symlink） | 同上 |

**旧ハーネス（`.harness/`）も hook も無い。** 何も邪魔しない。

## フロー全体

```
/requirements    →  docs/requirements.md            ← いまここ
/stories         →  docs/stories/ST*.md + INDEX.md
【製造準備・1 度だけ】
/ui-direction    →  docs/ui-direction.md + preview.html
/production-prep →  docs/production-prep.md + 実物（A 決める / B 作る / C 仕掛ける）
【Story ごと】
  openspec new change st<NN>-<slug>
  context → deep(grilling) → proposal → specs → tasks → apply → archive
```

skill の本文は `~/dev/harness2/skills/<名前>/SKILL.md`。**必ず読んでから従うこと。**
skill は `requirements` / `stories` / `ui-direction` / `production-prep` / `grilling` の 5 本。
`grilling` は mattpocock/skills (MIT) から commit ピン留めで逐語ベンダリングしたもので、
**問い方の規範の単一情報源**。`/requirements` の Step 2 と OpenSpec の `deep` から呼ぶ。

> harness2 は `~/dev/harness2/` で git 管理されている（remote 未設定）。skill を直したら
> そちらでコミットする。ashiato2 側は symlink なので即反映される。

---

# いまやること: `/requirements` の Step 2 から

**Step 0（入力の見立て）は完了済み。Step 1（広げる）もほぼ埋まっている。** 下記を再読する必要はない。
**次は Step 2（`grilling` skill を呼んで詰める）。**

## 決定済みの方針

- **旧 `reference/requirements.md`（847 行）を下敷きにしない。** 本人が「討議ログから 1 から起こす」と決めた。
  旧要件は 12 巡ぶんの訂正が本文に埋まって読み出しにくく、未処理の指摘が 86 件ある
- **`reference/review-queue/` の 86 件は「これから避ける穴の一覧」として読む。** 矛盾 14 / 前提の誤り 13 /
  参照切れ 11 が中心で、44 件が `rev: 高`（一方通行度が高い）
- 機能要件は **EARS 記法**、未決は **`[NEEDS CLARIFICATION: 理由]`** で要件本文にインライン

## Step 0/1 で分かったこと（本人の発話が出所）

`reference/discussions/20260724_ライフログソフトウェアの概要説明.md` が実質インタビュー記録で、
7 領域すべてを本人が答えている。**以下はすべて本人の言葉**（AI の推測ではない）。

**目的と原体験**
- 全記録を AI に読ませ、自分についてメタ分析し、理解を深め、行動し、幸せになること
- 原体験は「**自分が何が好きか**を分析しようとしたが、自分の記憶や分析能力だけでは限られ、時間もかかった」
- 分析の先に欲しいもの: **自己理解として納得したいが一番**。済んでいれば今後の意思決定がスムーズになる
- 忘れて困るもの: **過去の出来事＋そのとき自分がどう感じたか**、および**断片的な事実**（両方）
- AI に読ませるのは解決手段寄り。ただし「流行りをやりたい」気持ちもある

**ユーザーと現状**
- データ所有・可搬性を最優先にする理由は **「漏洩リスクだけ」。過去に嫌な経験は無い**
- 現状は **Google マップ履歴のみを月 2 回程度**。YouTube や画像は稀。他は散らばって管理しきれていない
- まず「まとめるところ」から始めたい

**ビジョン**
- 1 年後: データをもとに自己認識を深め、分析して意思決定ができる
- 意思決定の具体例は **旅行・買い物・遊び**。転職など大きな判断は将来（1 年後の目標ではない）

**競合**
- 既存サービスの不満は **「記録がめんどくさい」「AI に接続するのが手間」**
- 自作を選んだ決め手は「求めるものが無い」もあるが、**どちらかというと作ること自体が楽しい**

**制約**
- 個人開発 / 週 **15 時間** / 開発費 **月 2,000 円程度まで**
- **完成品を動かすコストは 0 を目指す**（全部ローカルで動かす前提）
- 具体的なスケジュールの縛りは無し
- 旅行記録アプリ（V-02、友人と共同開発）は**今回は記載しない**

**成功の定義（本人の言葉そのまま）**
1. **1 年間データが途切れずに収集され続けている**
2. **AI に質問して満足のいく答えが返ってくる（十分なコンテキストを渡せている）**

**見た目**
- 3 語: **暖かい / のんびり / 落ち着いた**
- 参照アプリ: 思いつかない
- 避けたい: 原色ギラギラ、派手、ガチガチすぎる（角張った四角）
- **ダークモード派**

## 索引が「固まっている軸」として挙げるもの

`reference/discussions/INDEX.md` の要約（**本人の直接の発話かは未確認。詰めるときに確かめる**）。

- 単一の正統データストア（D-01）に全記録を集約し、全アプリがそこを読み書きする（「スキーマがモート」）
- ローカルファースト。外部からは Tailscale 等でアクセス。BaaS を避ける理由は漏洩懸念
- 本体（収集・蓄積・最低限のビュー）と衛星アプリ（旅行実績・ランキング・分析）の分離。
  **本体の第一の消費者は AI と衛星アプリ**
- プラグイン機構で、API を改修せずにデータの入口・出口を追加できること
- 収集は**受動的・ゼロ負担**であること

## 索引が「未決」として挙げるもの

- 属性が変わったときに過去値を残すか
- 正規化をどこまで崩すか、それに伴う DB 選定
- プラグイン導入時の工程をどこまで簡略化できるか（再起動・パッチ適用の許容範囲）
- 日記の暗号化要否、E2E 暗号化の扱い

## Step 2 で必ず 1 巡すること

`requirements` skill が `grilling` に追加で要求している唯一の項目:

> **「記録し損ねると後から足せないもの」を必ず 1 巡する。**
> 列を用意するか / 原文を残すか / 版を持つか / 精度を落とさず持つか / 誰が書いたかを分けるか。
> 後から足しても**過去のデータが埋まらない**。

旧プロジェクトの実測では、**取り返せないものはほぼ全部この時点の要件に書かれていた**
（tz_id / 時計のずれ / 原文の保持 / 版 / 座標型）。要件に無いものは後の工程では足せない。

## 参考にできる資料（必要になったら開く。先に全部読まない）

| ファイル | 中身 |
|---|---|
| `reference/discussions/INDEX.md` | 91 行。**まずこれ**。中核 4 本を名指ししている |
| `reference/discussions/20260826_システムの本質について.md` | 431 行。正規化しない設計の是非、DB 選定 |
| `reference/discussions/20260826_データ追加のタイミングと検討方法.md` | 981 行。**後から足せるデータ / 最初に決めるデータの切り分け**。扉の原型 |
| `reference/discussions/20260825_プラグイン型の拡張可能なデータシステム設計.md` | 184 行 |
| `reference/external-claims.md` | 外部世界の主張。一次情報 URL + 逐語引用 + 確認日 + **再確認期限**つき |
| `reference/skeleton/doors.md` | 旧プロジェクトが立てた一方通行の扉 24 項 |
| `reference/review-queue/` | 未処理の指摘 86 件 |

**討議ログは 6,493 行ある。闇雲に開かない。** 本人の発話だけを抜くならこれで足りる:

```bash
cd ~/dev/ashiato2/reference/discussions && python3 -c "
import re,pathlib,sys
t=pathlib.Path(sys.argv[1]).read_text(encoding='utf-8')
for u in re.findall(r'^## User\n(.*?)(?=^## |\Z)', t, re.S|re.M):
    u=u.strip()
    if len(u)>15: print(u[:800]+'\n')
" <ファイル名>
```

---

## 引き継ぐ未決の設計判断（要件を書き終えた後に効く）

1. **一方通行の扉の正典をどこにするか。** 新フローは「要件の §5 に書く」前提で、骨（skeleton）の工程を
   持たない。旧プロジェクトは要件 20 項 / 骨 24 項で**食い違っていた**。要件に一本化が推奨だが未確定
2. **86 件の指摘を Story ごとに割り振る仕組みを入れるか。** 80 件が `rel: [FR-16, ...]` の形で対象要件 ID を
   持ち、Story は `satisfies:` を持つので**機械的に突き合わせられる**。「86 件の壁」を「1 Story あたり数件」に
   変えられる。`scripts/make_story.py` に十数行。推奨だが未実装
3. `scripts/make_story.py` は扉を `docs/requirements.md` から読む。1 の決着に依存する

## 旧 ashiato での試走で分かっていること（参考）

- **OpenSpec の archive は、後続 Story が既存要件を部分コピーで書き換えると hard block する**（実測）。
  落とされる Scenario 名を列挙してファイル無変更で中断する。横断整合が機械強制される
- **OpenSpec は不変条件（面）と振る舞い（点）を区別しない。** 「常時成り立つ制約」も Scenario が
  1 つあれば通る。1 回通ることしか保証されない
- **EARS の効果は限定的。** 実測で、散文の要件 1 本を Scenario へ翻訳するのに判断が 4 回必要で、
  EARS が消せたのは 1 回だけ（要件を割る判断）。残り 3 回は EARS でも残る
- **実装は spec の穴を開ける。** 1 Story を実装したら、spec に書かれていなかったことが 4 つ出た
  （否定の振る舞い / 適用外の条件 / 差分判定の根拠など）。うち 1 つは**実装が黙って決めていた設計判断**だった

## やってはいけないこと

- `reference/` に書き込む
- 旧 `reference/requirements.md` を下敷きにする（本人が 1 から起こすと決めた）
- 本人の発話を要約して要件にする（要約は判断。逐語で扱う）
- 数値をぼかす（「適切な間隔で」は禁止。決まらないなら `[NEEDS CLARIFICATION]`）
- `~/dev/ashiato/`（旧）を触る
